# 22 · 主线收口与历史代码整理建议

状态：用户确认的整理范围已执行，文件归档、保护哈希、入口链接及交接回归均已验证（LOCAL_VERIFIED）。源路径、21 号进展与保留名单已先修订再执行；结果见文末。本次不提升 M2b-2 整链路或正式产品验收状态。

本轮分类：文档、开发入口与历史文件整理，不改产品业务行为。沿用用户确认的 22 号方案；流程为修订清单 → 冻结路径/哈希 → 更新规则与两个入口 README → 归档 12 项 → 校验哈希/引用及现有回归 → 记录结果。使用 fullstack-quality-gate 的比例验证流程，无新增依赖、无并行代理。

## 为什么应先整理

用户已明确按 19、20 号交接包继续，并要求有疑问先对齐。当前目录同时保留原型新主线、历史 V3 对照稿和前期正式工程；需要先让后续开发能识别各自用途，避免从不同规则拼出第三套方案。

整理前已核实（下表是修改前快照，完成后的状态见文末）：

| 位置 | 当前事实 | 对后续开发的影响 |
| --- | --- | --- |
| 根目录 AGENTS.md | 仍描述 React/Vite、Fastify、SSE、Kysely 模块约束 | 与 19、20 号原生 JS/Express/WS 主线存在适用范围歧义 |
| output/pfc-workbench-prototype/README.md | 当前交接仍指向 09 号，说明无需后端、正式系统仍暂停 | 未区分目前 local/mock 与 API 联调路径 |
| docs/planning/prototype-v3/README.md | 首页仍以 09 号为当前依据，并保留旧技术路线与暂停说明 | 阅读目录入口可能绕过用户指定的最新交接包 |
| index.html | 当前载入 original 下 18 个 JS（含本轮 domain-client 草稿） | 是当前选定入口；未载入 prototype-v3.js |
| index-v3.html / prototype-v3.* / prototype-v3-data.js | 构成另一套独立原型；旧 V3-说明.md 曾将其作为新稿推荐 | 存在误选入口风险，但仍有历史文档和专用验证引用 |
| apps/、packages/、根 scripts/tests | 属于前期 React/Fastify 正式工程，并非自动等于废弃代码 | 当前不按这条实现路径继续，但其退役需另作明确决定 |
| 根 package.json / package-lock.json / node_modules | 原型静态检查使用根 ESLint，浏览器检查使用根 Playwright | 不能随旧工程目录一起删除依赖和环境 |
| Git | 当前分支 feat/cap-pfc-01-m1 没有提交；git ls-files 为空，源码尚未纳入提交历史 | 不应将删除后“可用 Git 恢复”当作回退保证 |

## 建议采用的第一轮范围

采用“明确主线 + 归档旧对照稿 + 前期工程原位保留”。仅加警示说明改动最小，但旧入口仍混杂；整体删除旧工程能缩小目录，却会丢失未提交资产并牵动现有工具链。本建议在可恢复前提下消除明显歧义。

1. 根 AGENTS.md 增加本轮交接范围：当前任务默认读取 19/20/21；原生 JS/Express 规则适用于 `output/pfc-workbench-prototype` 和 `server`；原工程约束继续适用于 `apps/packages`。原有安全、授权、验证、数据政策保留，不把两条路线合并或将原型测试等同于正式验收。
2. 更新两个 README，将 19 号路线图、20 号任务和 21 号未完成检查点放在最前。历史规则、完成数字和旧结论注明日期与范围；保留历史报告原文，不修改当时证据。
3. 归档源目录明确为 **`D:\项目管理\product-full-chain-platform\output\pfc-workbench-prototype\`**，不是 `output\` 根目录。仅将下表 12 项整体归档至 **`D:\项目管理\product-full-chain-platform\archive\prototype-v3-reference-20260912\`**，保持文件名和字节不变。归档前逐项核对存在性、引用和 SHA256；保留归档说明与原路径映射，修正当前索引中的链接。历史正文和历史脚本内的旧路径保留，归档说明解释其原位置与恢复方式，不宣传为当前可直接运行的门禁。
4. `apps/`、`packages/`、旧工程 scripts/tests 原位保留，声明不属于本次交接的默认实现入口；不移动、不删依赖、不改迁移/数据库、不修改旧 POD 台账。后续若明确废弃旧工程，再设计独立拆仓或退役步骤。
5. 保留当前 `index.html`、`original/`、`server/`、sim-bridge、交接包六条回归及其依赖；源码备份、历史证据、其他旧验证脚本按下方明确名单原位保留，不纳入这 12 项归档。未被当前门禁调用不等于无价值，不做扩大删除。
6. 21 号所列待确认接口衔接为 **已实施部分代码、尚未完成整链路验收**，不是未动工。它们是 21 号 Codex 实施产物，已由当前入口调用；`verify-m2b2-model.mjs` 4/4 PASS、`server/verify-m2b2-domain.mjs` 7/7 PASS 是 21 号记录的已执行事实。修改前源码备份为 `output/pfc-workbench-prototype/_backup/m2b2-before-20260912/`。本轮保留源码、测试和备份，不静默回退，也不将本次整理授权解释为额外接口设计获批；完整 UI 链路与范围问题仍待确认。

### 12 项唯一归档名单

下列路径均相对仓库根，目标文件名与源文件名一致。

| 源文件 | 目标文件 |
| --- | --- |
| output/pfc-workbench-prototype/index-v3.html | archive/prototype-v3-reference-20260912/index-v3.html |
| output/pfc-workbench-prototype/prototype-v3.js | archive/prototype-v3-reference-20260912/prototype-v3.js |
| output/pfc-workbench-prototype/prototype-v3.css | archive/prototype-v3-reference-20260912/prototype-v3.css |
| output/pfc-workbench-prototype/prototype-v3-data.js | archive/prototype-v3-reference-20260912/prototype-v3-data.js |
| output/pfc-workbench-prototype/verify-v3.mjs | archive/prototype-v3-reference-20260912/verify-v3.mjs |
| output/pfc-workbench-prototype/verify-v3-check.cjs | archive/prototype-v3-reference-20260912/verify-v3-check.cjs |
| output/pfc-workbench-prototype/verify-v3-gov.cjs | archive/prototype-v3-reference-20260912/verify-v3-gov.cjs |
| output/pfc-workbench-prototype/debug-gov-v3.cjs | archive/prototype-v3-reference-20260912/debug-gov-v3.cjs |
| output/pfc-workbench-prototype/V3-说明.md | archive/prototype-v3-reference-20260912/V3-说明.md |
| output/pfc-workbench-prototype/V3-验证报告.md | archive/prototype-v3-reference-20260912/V3-验证报告.md |
| output/pfc-workbench-prototype/v3-baseline.json | archive/prototype-v3-reference-20260912/v3-baseline.json |
| output/pfc-workbench-prototype/v3-check-shot.png | archive/prototype-v3-reference-20260912/v3-check-shot.png |

### 明确保留原位，不归档或清理

- `output/pfc-workbench-prototype/_backup/` **整个目录**。本次盘点为 7 个子目录、4 个根级历史 HTML、递归共 139 个文件；包括 final-before、fix-before、fix-p2、fix-scope6、m2b2-before、remediation-before、scope7-before 与 index-v1/v2 等。保护范围以整个目录为准，不以目录组数或估算文件数截断。
- `output/playwright/` **整个目录**。本次盘点为 47 张历史图片；ai-ux、m2、ui、local-experience 等证据全部保留。原型目录内其他历史截图与证据目录也保留。
- `output/pfc-workbench-prototype/` 下旧验证/调试脚本原位保留：`verify-fix1.cjs`、`verify-fix2.cjs`、`verify-final2.cjs`、`verify-original.cjs`、`verify-p2.cjs`、`verify-proposal.cjs`、`verify-scope6.cjs`、`verify-ui.cjs`、`verify-diff.cjs`、`verify-browser-matrix.mjs`、`check-g01.cjs`、`check-w02.cjs`、`check-reply-tray.cjs`、`ui-shots.cjs`、`scope6-shots.cjs`。同目录 `final-check.png`、`original-check.png`、`diff-check.png` 等所有不在 12 项名单中的图片也保留。此处保留声明不表示每个历史脚本均已通过当前回归。
- `output/pfc-design-overview.html` 已确认存在，作为早期设计稿原位保留，是否继续使用留待后续评估。
- `apps/`、`packages/`、根 `scripts/`、`tests/`、`.quality-gate/`、根依赖及 lockfile 均原位保留；所有历史报告原文保留。

README 的新入口必须并列列出 **19（路线图）、20（本轮任务）、21（实施中检查点）**，同时链接本清单。第 21 号的部分实现与未验收状态不能被前面的历史完成标记覆盖。

执行前证据目录：`.local/prototype-mainline-cleanup-20260912-133446/`。`before.json` 冻结 12 项归档文件、4 项待编辑文档和 1774 项保护文件；`before/` 保存待编辑文档原文。业务源码、M2b-2 草稿、保留脚本、备份、图片、依赖清单及旧交付台账均在保护哈希内。

归档配套的路径维护：归档前逐项读取 ESLint/Prettier 对这 12 项的实际忽略结果。6 个 JS/MJS/CJS 脚本原本均不进入根 ESLint；8 个文件原本被 Prettier 忽略。`eslint.config.js` 与 `.prettierignore` 仅将这些文件的专用路径转到归档位置，原应用、测试目录及其他文件的检查范围保持一致，不增加整目录豁免。两份配置原文已另存于同一 `before/`，修改清单记录于 `archive-path-config-edits.json`；它们从初始保护名单中转为本轮路径维护项。验证时将逐文件比较迁移前后的忽略结果。

## 执行顺序、验证与回退

- 用户确认上述范围后，先记录待编辑/归档文件清单和原文件 SHA256，再整理规则/索引，最后移动确认的旧 V3 整包。
- Windows 下逐项确认源/目标绝对路径位于本仓库内；使用原生 PowerShell 文件操作，不进行递归删除，不通过清理操作提交或推送。
- 验证当前入口只引用 original；交接六条命令仍可发现并运行；更新后的链接可解析；归档文件移动前后 SHA256 一致；当前应用源码和用户待确认草稿无意外变更。
- 本轮以文件路径/引用与现有原型回归为检查范围，不以根工作区旧工程全量编译结果代替交接包结果，也不通过放宽其门禁掩盖失败。
- 回退依据为文件清单、哈希与修改前副本，将归档逐项恢复到原路径；Git 本次不能充当已有提交快照。任何覆盖用户新增修改的恢复都需先停下核实。
- 不保留持久服务、不使用并行代理。归档使用原生 PowerShell；回归临时启动的独立浏览器、内存服务和模拟 Bridge 在测试后关闭。

本轮不改变 POD-PFC-001 正式 CAP/Unit 状态；原型路线仍为 M2b-2，后续 M2c。此文不代表已接受额外接口设计，也不代表已批准旧正式工程退役。

## 执行与复核结果 · 2026-09-12

用户提出的源目录、实施进度和保留清单要求均已落实。21 号原已在第 2 条列出，本次进一步落实到两个 README 的顶部入口；其 4/4、7/7 已通过事实和未完成状态同时展示。备份/证据数量采用当前盘点（139 文件、47 图片），保护范围按整个目录执行。

| 检查 | 本次结果 |
| --- | --- |
| 12 项明确源路径 → archive 目标路径 | 12/12 已移动；原位置不残留第二套 V3 入口 |
| 归档字节数与 SHA256 | 12/12 与归档前一致 |
| 保护资产 | 1772/1772 文件哈希不变，包括应用源码、21 号草稿与备份、历史证据、其他旧脚本、pfc-design-overview、根依赖及旧台账 |
| 当前入口 | index.html 仍为 18 JS / 20 个本地资源，全部可解析，不引用旧 V3 |
| 更新后的文档入口 | AGENTS、两个 README、归档 README 共 61 个链接可解析，19/20/21 正确置顶 |
| 静态排除路径迁移 | 12 项逐文件验证：ESLint/Prettier 迁移前后的忽略结果相同 |
| 六条交接回归 | 全部 PASS；verify-prototype 6 组，M1 layer 6，M1 E2E 8，server 18，M2 19，M2b 7 |
| 21 号定向回归 | 前端模型 4/4 PASS、领域模型 7/7 PASS |
| 配置与台账 | check:config、check:delivery-governance、eslint.config.js 的 ESLint 全部 PASS；POD 路径不变 |
| 进程退出读回 | 5189/5191/5192/5193 无监听，无本轮测试 Node、模拟 Bridge 或 Playwright 根进程残留 |

首轮原型门禁在 `verify-guide-browser.mjs` 尚未输出用例结果时触发 180 秒进程超时；其他已完成组通过，应用源指纹未变化。保留失败证据并审计进程后，未修改源码/断言/超时，原样重跑六条命令均通过。该偶发超时原因尚未完全定位，不宣称已修复。

归档说明与路径映射：[archive README](../../../archive/prototype-v3-reference-20260912/README.md)、[manifest](../../../archive/prototype-v3-reference-20260912/manifest.json)。详细命令、结果、失败记录、证据与回退见 [整理验证报告](../../quality-gate/reports/prototype-mainline-cleanup-20260912.md)。本地证据位于 `.local/prototype-mainline-cleanup-20260912-133446/`，包括 before.json、verification.json、attempt2-*.log、process-cleanup.json 和文档/配置原文副本。

没有修改业务源码、执行数据库迁移、真实工作区工具调用、外部写入、提交或推送；没有运行或放宽旧工程 quick/core/full。整理已完成，后续返回 21 号的 M2b-2 检查点，保留额外接口范围对齐及完整 UI 验收待办。原正式工程的 CAP/Unit 状态、接受、发布与观察结论均不由本轮整理提升。
