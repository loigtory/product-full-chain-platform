# 48 宿主工具执行链实施与验收

状态：IMPLEMENTING / 人工验收 PENDING。分类：system iteration / strict。

用户以“继续”确认方案 A 的准确范围。31 路径、7 文档、18 命令及数据/调用上限见 [授权](../../quality-gate/reports/ai-tools-host-exec-20260917/CODEx_TEST_AI_HOST_20260917_b601306c-a342-4a04-b27a-82080b615dd9/authorization.json)。此前 R1 保持 PARTIAL/EXEC_BLOCKED。

## 计划与边界

1. P0 固定基线、动态请求关联和原生工具隔离；核验 command/exec Windows 限制。
2. P1 失败回归先行，实现路径/内容/身份/审批/预算/终止检查。
3. P2 隔离 PG/API/页面和工具证据；保留 TEXT/006/007 行为。
4. P3 最多 8 次串行模型调用，失败计数；未证明的能力保持 BLOCKED。
5. P4 18 项适用闸、差异评审、三 schema/进程/文件清理、PG 恢复停止、本地 feature 提交。

不安装依赖、不改 SQL/系统/ACL，不推送、不合并、不启动个人服务。44 D2/D3/D6 及全链路 C4 延后。合成数据和自有 canary，私有工作区忽略 Git，去敏证据保留评阅。回退只还原自有且指纹匹配的文件，冲突保留。

## 进度

19 份源码基线一致；已安装依赖可解析，Node 24.20.0 和 Quick dry-run 已验证。下一步 P0。
