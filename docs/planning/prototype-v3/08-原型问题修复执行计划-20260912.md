# 原型问题修复执行计划

2026-09-12。分类：独立原型行为修复，按严格的状态/权限/版本回归执行，正式平台实施保持暂停。设计依据为07补齐方案、9月12日复核报告及用户在该报告后明确提出“继续解决问题”；该指令确认继续在原版修复所列问题。本轮不重复索要已确认范围的许可。

目标：关闭复核中的7项未解决、6项部分修复及3项文件/交付缺陷，保留9项已修复行为。原型主要布局和入口不变。Parent POD为POD-PFC-001/M2，保持5 CAP/26 Unit及唯一工程路由POD-PFC-001/M2/R3a/artifact-collaboration-tdd；原型验证、正式集成、产品接受和发布分别记录。

选定方案：在原版统一草稿/资产/消息目标和冻结清单，提取附件、会话和下载模块，并用独立回归保护；不采用继续在每个按钮加入临时例外，也不在本轮迁移到React或接真实AI。影响范围为output/pfc-workbench-prototype下自有源码、原型检查及说明，以及本规划/质量报告。沿用用户认可视觉，不迁移apps/web页面。

关键实现决策：

- 所有存储写检查已读revision及冲突；冲突窗口的本地草稿保留，重新载入最新业务状态后恢复，不覆盖其他窗口记录。
- 整批文件固定requirementId；资产统一按ID查找；读取中暂不发送，完成后可继续。读取失败重试重读同一File，刷新后原件丢失要求重新选择；保留完整文本和原图，预览截取不冒充全文。
- 消息/引用/建议按钮绑定精确messageId和版本；发送与重发重验来源使用权限。重发建立新回合并关联旧消息，未执行建议刷新后恢复，过期建议拒绝。
- Unit↔AC显式保存关联；重排不改变ID/关联；提供关联编辑，删除后缺失关联明确展示。
- 产物下载绑定所选版本，代码下载绑定run/path。发布批准冻结完整内容及来源标识；历史交付包仅消费冻结数据，缺失旧快照明确报不可重现。
- 附件队列限高、内部滚动，保留三栏及输入位置；PC三宽度、矮窗口和键盘验证。使用现有样式，不引入外部资产。

执行顺序：①固定当前指纹、备份、环境检查；②将35个复核场景转为项目自有回归，确认预期失败；③拆分职责并修复共享规则；④逐项修复其余下载/追踪/布局；⑤执行原模型11项、原浏览器13项、问题回归及新增规则/架构断言；⑥源码复审、截图核对、文档/门禁及进程清理，交付修复状态。

环境检查：项目Node24.20.0/npm11.19.0，npm ls --depth=0 --offline通过，依赖已安装且可复用，无需重新安装/升级；quick --dry-run返回READY_TO_RUN，非PASS。采用现有Playwright/Edge，Browser插件未列出；无新增网络依赖。正式全仓检查按变化范围评估，既有无关失败应准确报告而不扩张修改。

测试数据：CODEx_TEST_确定性合成文本、图片、文件、需求、版本、失败消息及历史发布；每场景隔离浏览器上下文，并发仅在该上下文内共享两个页面。原型file入口，页面HTTP/HTTPS阻断，不读取.env或凭据。测试脚本及断言入原型目录；截图/下载临时保留，报告/机器证据入docs/quality-gate/reports。原件测试只使用合成数据，关闭上下文清除其中createdIds。

回滚：先保存14文件及原测试的快照/哈希，修改前与最新复核指纹核对；不清理用户演示数据，不用重置掩盖旧格式迁移问题。如出现来源文件被外部改变，先检查差异再整合。单执行者顺序集成，不使用并行代理；短命浏览器测试结束关闭，不保留服务/端口。

退出标准：35个原复核场景及新增关键边界全部通过；11模型/13浏览器主流程无回归；下载内容与对象匹配；冲突/权限不产生错误写；PC多附件布局可用；审查可读，明确未实现规格。人工产品/视觉接受仍由陈立给出，发布/部署未执行。本轮不声称完整生产平台验收，不改变正式台账，也不提交/推送。

延后范围：真实数据库/文件存储/AI/Bridge/Shell/发布，及需要独立确认的多CAP总体编排、完整跨阶段会话、团队身份/多设备租约和生产容量。为关闭具体差异交互，可补部分采纳等直接相关小功能；不以此暗中删减26 Unit。费用/吞吐真实测量在正式接入阶段；本轮附件数/体积/预览和日志采用有界处理。

检查点（2026-09-12）：本轮实现和复审完成，统一原型检查PASS（46问题/边界、13主流程、11模型、8完整性及静态）；17源文件指纹前后一致。项目Quick止于原型/历史备份lint；人工产品与视觉接受仍待陈立确认。完整失败/重跑和残余范围见[修复报告](../../quality-gate/reports/PROTOTYPE-REMEDIATION-20260912.md)及09修复后开发交接。
