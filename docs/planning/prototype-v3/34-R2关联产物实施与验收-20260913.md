# 34 · R2关联产物实施与验收

## 当前结论与范围

> 最新状态：2026-09-13陈立对已推送提交e5dcd31明确回复“验收通过，继续”，本轮升级为scoped ACCEPTED。以下实施过程中的PENDING为当时状态，由末节验收记录覆盖；正式CAP/Unit、业务PG及发布状态不提升。

R2已完成本轮关联产物和阶段承接的本地验证（LOCAL_VERIFIED）。用户明确同意commands.js补入白名单（51→52）后，已按展示的一行差异修复004成员复核；同一批22/22门禁通过，52项源码前后指纹一致，附加检查、历史指纹和独立清理检查均通过。人工验收待陈立针对交付提交评审。

首次用户“同意”已确认33号A方案、D1–D6、原51项源码/测试、7份交接文档、四个精确隔离schema及清理、22条门禁；此前M2c-3 ce2c44c已scoped ACCEPTED，验收收尾d252761和main合并04fd81a已推送并独立读回。R2分支feat/r2-artifacts从04fd81a建立，开工提交2ea9ecd；本记录随R2收工提交交付，并按已有授权推送feature。收工SHA和远程一致性以最终Git回执为准，本文不预写尚未产生的提交号。

父POD-PFC-001 / M2。影响CAP-PFC-01、02、03及UNIT-PFC-01-01～05、02-01～05、03-01～03；仅为影响范围，不升级任何正式Unit。保留全部5CAP/26Unit，唯一POD next route保持POD-PFC-001/M2/R3a/artifact-collaboration-tdd。

## C1–C4计划与进度

| 步骤 | 当前事实                                                          | 状态                               |
| ---- | ----------------------------------------------------------------- | ---------------------------------- |
| C1   | 004迁移、独立版本、候选/采纳、工厂                                | LOCAL_VERIFIED                     |
| C2   | 两次确认、旧入口约束、影响、计划v2、003/004当前成员复核           | LOCAL_VERIFIED                     |
| C3   | 原工作台候选、比较、受控预览、原件与异常恢复；三宽度              | LOCAL_VERIFIED；人工视觉待验收     |
| C4   | 22/22、附加检查、52源码指纹、历史证据、清理均PASS；差异及文档齐备 | 本地出口通过；按23提交/推送feature |

## 实现与差异

- PRD沿用req_versions唯一存储。原型、验收项独立版本，关联组只引用具体版本；只改验收项时复用未变PRD/原型。历史版本、组、确认内容由SQL约束保护。
- 允许想法阶段先保存不完整原型候选。手工、平台模板、文件导入明确标识；JSON读取服务端真实原件，HTML只展示转义源码并下载，不执行上传脚本。
- 候选比较、采纳和两次确认绑定需求revision、当前组、输入指纹。业务确认进入设计，设计确认进入开发，在同一事务记录阶段和输入。旧PRD保存/确认/差异采纳不能绕过关联流程。
- 设计变化只使设计确认失效；业务方案、问题、材料和项目变化按共用影响策略返回相应阶段。旧确认、计划、运行和证据保留。
- 新计划v2冻结三产物版本、确认、实施范围和输入；批准/启动重新检查。旧v1显示只读说明，保留取消路径，不能新启动。
- 四个独立前端模块分别负责读写恢复、成果视图、操作和受控预览。复用原版样式/三栏结构。预览只执行固定渲染器，opaque-origin iframe、CSP、限定状态消息，不接业务写入或本地工具。
- 409保留草稿并要求显式比较新基线；503/写后读失败保留原commandId。后台刷新复用提交后读回，避免成功保存被后台读取抢占；被强制新读取取代时仍保留收据。刷新重开、跨需求晚到响应、预览输入保留和按钮按下时重绘均有回归。领域写入后同步读回关联基线；重绘前捕获聊天焦点与选区、重绘后恢复，避免输入落空。

## 环境、数据和进程

Node24.20.0/npm11.19.0；root/server已安装依赖离线核验通过。Quick dry-run只记READY_TO_RUN；当前原生JS/Express范围按33号22闸验收，不改旧apps/packages或依赖。配置来自忽略的.env.local，证据不含连接串/JWT/真实文件。

仅33号四个目标：codex_test_m2c_20260913_artifacts、codex_test_m2c_20260913_governance、codex_test_m2c_20260912_domain、codex_test_m2c_20260912_storage。前三个≤3000行/100MiB，storage≤200行。每次attempt以CODEx_TEST_前缀和UUID隔离；两租户、两需求、多角色、状态、版本和失败场景由工厂形成。API/SQL和文件独立读回，核对OID/marker/表/行数/路径后清理，不按前缀清库。

串行运行，无子代理。临时API/模拟Bridge只用5189–5196，静态服务为loopback临时端口。Edge使用1280/1440/1920，浏览器插件未提供，使用既有Playwright；移动端不在项目范围。原PG13528保留。最终独立读回：四个测试schema/三个原件目录为零，本轮API/浏览器/测试进程零残留。原有PG13528/5432、旧API31152/3001、Vite37744/5173、Codex内核42552/25260均保留，见质量报告。

一次新增晚到响应测试因缺少等待超时卡住，已精确核实并停止自建runner40172/API44164/Edge5576及子进程；schema OID887498和marker核验后清理124行，文件为零。失败未计PASS，证据为artifacts/aborted-browser-cleanup-1789261500.json。随后增加45秒场景超时、12秒请求等待上限并复测通过。该中断attempt无法恢复整个运行前的外部元数据指纹，仅证实清理操作前后未变；后续正常attempt均由工厂完整验证。

## 测试、自审与问题来源

最终完整批次gates-1789265262932（2026-09-13T02:07:42.932Z至2026-09-13T02:11:37.169Z）为22/22 PASS，52项源码指纹前后一致。新增模型6/API13/浏览器10，旧领域36/PG浏览器15/治理专项15/治理浏览器10均通过。50个JS/MJS语法、51项JS/MJS/HTML格式、32脚本语法/29模块lint、check:config和check:delivery-governance通过。旧20/22及此前失败原文保留；不拼接不同源码结果为PASS。附加运行器曾误用不存在的check-delivery-governance.mjs；已按未变的package.json实际ts命令复跑通过，失败留痕，不涉及源码修复。

实测修复包括：采纳/确认URL目标未进入幂等载荷、旧领域前置适配、开发编辑入口、R1模拟DTO兼容、预览重绘丢输入、点击手势中节点被替换、成功提交的读回被抢占、领域阶段变更后关联基线尚未读回、旧节点被移除后焦点归属判断失效。测试自身修正包括故障注入被后台读取消耗、列表/详情契约断言、滚动后的iframe命中绘制、缺失超时、证据变量绝对/相对路径和浏览器全局引用静态检查。所有失败保留，最终结果只认最新完整批次。

自审检查事务/外键/不可变约束、并发与重放、文件归属/撤权/损坏、当前身份、恢复、前后端契约、历史指纹和范围。本次重新核验477份历史报告/截图与Git blob，文本按Git换行归一化、二进制逐字节，全部一致；历史源码保护另由架构门禁覆盖。脚本通过不代替陈立的业务/视觉/工程/测试/安全验收。三档候选比较截图在artifacts/candidate-compare-{width}.png，自动化与自审已检查排布，人工视觉验收待完成。

## 已确认的唯一补充范围：commands.js

依据33号第五节“需求/契约改变或白名单遗漏须先说明并对齐”，先提出单文件补充，用户现已明确回复“同意”。修复前该文件第26行只对003执行authorizeCommand；升级004后executor可创建owner专属能力（实测201，预期403），成员复核也没有早于收据重放。本次2026-09-13重新运行模型检查复现A13 Missing expected rejection；已保留失败JSON。

已批准仅将此文件补入白名单（51→52），不改接口、依赖或权限语义。确认的最小差异：

```diff
-    if (db.targetVersion === '003')
+    if (['003', '004'].includes(db.targetVersion))
```

已完成33/20/AGENTS授权与架构白名单同步。A13从Missing expected rejection变为PASS；原G05能力配置越权及后续治理断言全部通过。commands.js相对main仅1行增/1行删，authorizeCommand仍在需求工作及收据读取之前，复用原治理锁与成员策略。无其他新增授权路径。R2 main合并仍待具体验收。

## 生命周期与后续交接

implementation=LOCAL_VERIFIED；integration=ISOLATED_PG_VERIFIED（标准业务PG未启用，不等于正式集成完成）；acceptance=PENDING；release/部署/发布后smoke/观察=NOT_EXECUTED。验收人为陈立，同人多角色已接受但各角色结论独立。不启用业务PG，不执行真实AI/工具/CI，不提升正式CAP/Unit或生产就绪。

33号确认的退出条件仍是22条功能门禁、附加静态/配置/治理/范围/历史/清理均通过才能记LOCAL_VERIFIED。质量报告记录具体失败、数据、截图、差异和剩余风险；R2 feature交付后由陈立验收，再决定main。测试验收域、发布观察、日常业务PG与真实本地工具仍为后续范围。

开工证据时间2026-09-13 07:17（Asia/Shanghai）；本地收尾检查完成于2026-09-13T02:17:33.040Z。完整回归共6批，前5批失败、最后1批22/22通过；最末批耗时234.2秒。返工主要来自004跨旧入口适配、浏览器后台重绘和测试时序；失败闸及耗时保留为过程改进依据，不作个人绩效指标。版本增长、多人负载、非Edge兼容、真实模型成本未实测。未来业务库启用需单独迁移/同点文件与数据库备份/回滚/监控观察包，本轮不预先启用。

最终证据入口：[R2质量报告](../../quality-gate/reports/r2-artifacts-20260913.md)。补充授权、完整门禁、自审和清理均已完成；收工仅提交/推送feat/r2-artifacts，人工验收和main合并仍为后续动作。

## 本次恢复检查点

系统迭代 / strict，沿33号已确认方案继续，不另起需求。2026-09-13固定Node24.20.0/npm11.19.0、root/server离线依赖检查和Quick dry-run均通过；dry-run仅READY_TO_RUN。已fetch，origin/main仍04fd81a，HEAD仍2ea9ecd，远程无同名R2分支。源码只补commands.js及既有架构白名单计数，串行验证、无新增依赖或进程常驻计划。补充授权和修复前A13失败均留痕；新的同批22/22及附加检查已经通过，源文件未在门禁后更改。

## 文件级差异与交付评审

以main04fd81a为比较基线，52项允许源码/测试中实际44项变化：18新增、26修改、8项无语义差异；不为凑白名单数量修改无差异文件。没有删除源码。唯一原计划补充是已批准的commands.js；其余交互、数据与测试包按33号。另更新7份交接文档和本轮指定证据，历史输入保持。逐文件行数以[source-diff.json](../../quality-gate/reports/r2-artifacts-20260913/artifacts/source-diff.json)为准。

| 文件                                                               | 类型   | 增/删行   |
| ------------------------------------------------------------------ | ------ | --------- |
| `server/sql/m2c/004-linked-artifacts.sql`                          | ADD    | +121 / -0 |
| `server/src/domain/artifact-policy.js`                             | ADD    | +141 / -0 |
| `server/src/domain/artifact-service.js`                            | ADD    | +558 / -0 |
| `server/src/domain/artifact-confirmation-service.js`               | ADD    | +175 / -0 |
| `server/src/domain/artifact-impact-service.js`                     | ADD    | +138 / -0 |
| `server/src/domain/artifact-dto.js`                                | ADD    | +67 / -0  |
| `server/src/domain/prototype-spec.js`                              | ADD    | +192 / -0 |
| `server/src/persistence/artifacts.js`                              | ADD    | +286 / -0 |
| `server/src/routes/artifacts.js`                                   | ADD    | +72 / -0  |
| `server/src/index.js`                                              | MODIFY | +1 / -0   |
| `server/src/contract.js`                                           | MODIFY | +21 / -0  |
| `server/src/runtime.js`                                            | MODIFY | +8 / -5   |
| `server/src/domain/requirement-service.js`                         | MODIFY | +76 / -1  |
| `server/src/domain/conversation-service.js`                        | MODIFY | +16 / -1  |
| `server/src/domain/material-service.js`                            | MODIFY | +2 / -1   |
| `server/src/domain/execution-plan-policy.js`                       | MODIFY | +48 / -2  |
| `server/src/persistence/messages.js`                               | MODIFY | +45 / -1  |
| `server/src/persistence/runs.js`                                   | MODIFY | +3 / -0   |
| `server/src/persistence/identifiers.js`                            | MODIFY | +6 / -0   |
| `server/src/persistence/migrations.js`                             | MODIFY | +61 / -4  |
| `server/src/persistence/commands.js`                               | MODIFY | +1 / -1   |
| `server/src/files/local-files.js`                                  | MODIFY | +1 / -0   |
| `output/pfc-workbench-prototype/original/artifact-client.js`       | ADD    | +225 / -0 |
| `output/pfc-workbench-prototype/original/artifact-view.js`         | ADD    | +198 / -0 |
| `output/pfc-workbench-prototype/original/artifact-actions.js`      | ADD    | +736 / -0 |
| `output/pfc-workbench-prototype/original/prototype-preview.js`     | ADD    | +252 / -0 |
| `output/pfc-workbench-prototype/index.html`                        | MODIFY | +4 / -0   |
| `output/pfc-workbench-prototype/original/api-client.js`            | MODIFY | +45 / -1  |
| `output/pfc-workbench-prototype/original/domain-actions.js`        | MODIFY | +10 / -0  |
| `output/pfc-workbench-prototype/original/governance-domain.js`     | MODIFY | +13 / -1  |
| `output/pfc-workbench-prototype/verify-static.mjs`                 | MODIFY | +4 / -0   |
| `server/test-data/r2-artifact-fixture.mjs`                         | ADD    | +169 / -0 |
| `server/verify-r2-artifact-model.mjs`                              | ADD    | +294 / -0 |
| `server/verify-r2-artifacts.mjs`                                   | ADD    | +843 / -0 |
| `server/verify-r2-architecture.mjs`                                | ADD    | +171 / -0 |
| `output/pfc-workbench-prototype/verify-r2-artifact-browser.mjs`    | ADD    | +555 / -0 |
| `server/test-data/m2c-domain-fixture.mjs`                          | MODIFY | +31 / -12 |
| `server/test-data/m2c-governance-fixture.mjs`                      | MODIFY | +9 / -4   |
| `server/verify-m2c-domain.mjs`                                     | MODIFY | +14 / -4  |
| `server/verify-m2c-governance.mjs`                                 | MODIFY | +8 / -4   |
| `output/pfc-workbench-prototype/verify-m2c-browser.mjs`            | MODIFY | +70 / -4  |
| `output/pfc-workbench-prototype/verify-m2c-governance-browser.mjs` | MODIFY | +1 / -1   |
| `output/pfc-workbench-prototype/flow-browser-fixture.mjs`          | MODIFY | +1 / -1   |
| `output/pfc-workbench-prototype/verify-flow-architecture.mjs`      | MODIFY | +8 / -3   |

当前三产物协作与受控预览可用于评审后续方案，自动化通过仍不代表任意HTML无损执行、真实AI生成、日常业务PG或生产就绪。下一动作由陈立评审本轮具体提交及三宽度截图；确认后再决定main，继续细化测试/验收/发布/观察缺口。POD唯一next route仍保持原值。

## R2人工验收与主干收尾 · 2026-09-13

- 验收人：陈立。确认方式：针对上一条交付的`e5dcd3180bae6c50de5f3deed9b69b10f3a7fc82`回复“验收通过，继续”。按23号人工评审后的主干流程完成验收留痕、feature提交/推送及main的no-ff合并/推送。原始来源/范围仍是33号A方案与已批准commands.js补充。
- 验收结论：产品/视觉在本轮披露范围内ACCEPTED；工程、测试和安全结论结合已交付的对应自动化及自审证据接受，不代表新增独立评审人员或真实环境审计。原有预览/合成/隔离/容量等限制保留。
- 本次复核：固定Node24.20.0/npm11.19.0，root/server离线依赖和Quick dry-run通过；fresh fetch后本地/远端feature同为e5dcd31、main同为04fd81a。52项源码与同批22/22证据hash一致，未修改任何源码，无须重跑相同源码的数据库/浏览器写测试。另执行本次文档格式/链接、配置及正式交付台账检查。
- 文件范围：AGENTS、19、20、本34、两个README，以及[验收JSON](../../quality-gate/reports/r2-artifacts-20260913/acceptance-e5dcd31-20260913.json)。历史测试原文保留，不改成“本次重跑”。修正入口中的过期权限待确认文字和当前script数（32）。
- 生命周期：implementation=LOCAL_VERIFIED；integration=ISOLATED_PG_VERIFIED；acceptance=SCOPED_ACCEPTED；release/部署/业务PG/发布后smoke/观察=NOT_EXECUTED。正式5CAP/26Unit和POD next route不变。
- 后续：从合并后的最新main准备测试用例、执行结果、缺陷、产品验收与发布输入承接方案；方案具体范围需确认后实施。本次不启动API/浏览器/Bridge/PG，不写测试或业务数据，不删除分支。
- 本文随验收收尾提交；合并后的main SHA由Git回执及下一方案入口记录，不在提交前预写未知SHA。
