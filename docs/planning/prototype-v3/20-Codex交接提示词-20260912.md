# 20 · Codex 交接提示词模板（每轮复用）

> CURRENT HOST-EXEC PARTIAL / COMMAND_BLOCKED 2026-09-17：当前以 [48](48-宿主工具执行链实施与验收-20260917.md) 为准。已确认方案 A 的 31 路径中新增 12、修改 17、未改 2；文件控制/持久化审批/真实模型读写与取消已验证，公共 EXEC 保持关闭。18 项入口经同源码浏览器复跑后 16 PASS / 2 BLOCKED；31 指纹一致、4291 保护文件不变；独立额度 6/8 次、71 秒、未决 0。三个 schema/测试进程已清理，但 Windows RunAs 停止服务被取消，pfc-postgresql-18 仍 Running，等待用户处理管理员弹窗；不得称环境恢复完成或绕过。48 与本轮 evidence 为当前事实；下方 pending/proposal 段落为历史。人工验收 PENDING，44 D2/D3/D6/全 C4 延后。只做已授权本地 feature 收工，不 push/main 合并/系统变更/继续模型调用。

> **2026-09-17 续接核查**：当前源码仍为227ca65。已核实本机动态工具/command-exec协议的七项结构，尚未证明实际隔离与执行。宿主工具补充方案已写入 [47](47-控制与验收修复实施检查点-20260916.md)，31路径/七文档/新8次调用及准确隔离目标续用均为待确认，未改业务源码或启服务；R1验收仍待定。

> **2026-09-17 收工：PARTIAL / EXEC_BLOCKED，待人工评审。** 40 路径的独立修复已验证：综合命令 13 PASS / 2 BLOCKED / 0 FAIL，模型 13、API 12、备份恢复 4、浏览器 8 项通过；真实 TEXT 完成/取消/并行有证据。工具执行前覆盖未证实，EXEC 保持关闭；已用 7/8 次、28 秒、未决 0。三个隔离 schema 和临时进程/文件已清理，PostgreSQL 恢复 Stopped，5188/5199 未启动。详见 [47](47-控制与验收修复实施检查点-20260916.md)；无推送/main 合并、正式启用、发布或整个平台完成结论。下方旧状态均为历史。

> **2026-09-17 已获确认**：执行 46 A1–A5/R1.0–R1.4，当前检查点为 [47](47-控制与验收修复实施检查点-20260916.md)。38 个源码/测试路径、七文档、三个隔离 schema/恢复环境、最多八次新增真实调用和当前 feature 本地提交已授权；无 push/main 合并。下方“待确认”填写表为方案时点快照，不再重复请求包内授权；发生实质范围变化才对齐。

> **2026-09-16 当前任务：[46 · 核查问题修复与主线补齐方案](46-核查问题修复与主线补齐方案-20260916.md)。** 修复方向已同意，精确补充包待确认；当前 `feat/ai-tools-integration / 2ce418c`。本轮仅更新六份方案/入口文档及新方案 JSON，不启动旧模板中的实现或调用。38 个源码/测试路径及其指纹见 46 机器清单；批准后由 47 记录实施。下方旧检查点和示例正文按原日期理解，不能覆盖此处和填写表。

> 当前检查点：PARTIAL / BLOCKED_SCOPE_AMENDMENT。精确全局AGENTS.md外发已确认；首笔真实模型请求通过（1/20次），007隔离迁移和材料/作业持久化已有实测。007复测发现两项旧保护被跳过，修正涉及漏列的三个文件；精确补充见45，未私自修改。HTTP/UI/真实工具及完整43闸仍未完成。

> 历史方案交付说明：先完成真实 AI、材料理解、原型/PRD/AC 联产、本地工具执行与可信测试结果回写，再做完整业务验收和使用观察。44号 A 方案及确切实施包已备齐待确认，尚未开始实现或真实调用。43号只证明空业务启用、首备和隔离恢复就绪；正式 POD next route 不变。

> 用途：复制「=== 提示词开始 ===」与「=== 提示词结束 ===」之间的全部内容，粘贴给 Codex 继续落地。**每轮先填下方"填写表"，把参数代入后整段粘贴。**
> 配套文档：19 号路线图、21 号实施中检查点、23 号防走偏协议。固定开局指令见 23 号第二节。

---

## 每轮填写表（填完代入提示词）

> 本轮是 46 补充方案整理，docs-only/light；后续控制、权限、运维和验收修复按 system iteration/strict 执行。44 既定产品方向不重复确认，新增范围只按 46 一次对齐。

| 参数             | 当前实际                                                                                                                              |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| {本轮任务}       | AI-TOOLS-REMEDIATION-R1 补充方案；修复方向已同意，具体执行包待确认                                                                    |
| {任务代号}       | 当前 feat/ai-tools-integration / 2ce418c；本地 tracking main 领先 72 提交，当前远程未确认                                             |
| {涉及文件白名单} | 方案轮：AGENTS/19/20/两README/46，加 ai-tools-remediation-plan-20260916 两份 JSON；实现候选 38 路径、七文档及新 runId 证据见46        |
| {禁止清单}       | 未批准的源码/真实调用/PG/服务/Git 写；个人5188和验证5199写测试；旧SQL、依赖、历史、正式台账、系统设置、push/main合并、子代理          |
| {验收命令}       | 方案轮：路径与hash/链接/精确格式/git diff --check/config/delivery/Quick dry-run；实现后的新增专项及23九条基础入口见46，均未在本轮执行 |
| {续编文档号}     | 当前46；批准后续编47控制与验收修复实施检查点；45保留外部工具开发历史                                                                  |
| {待确认事项}     | 46 A1–A5、38路径、三隔离schema/恢复环境、8次新增真实调用、现有feature本地提交续接；不重复询问已确认的产品方向                         |

后续先读 46 和机器范围指纹，按确认后的补充包继续；44 为产品目标，45 为历史实现记录。已有 MCP 预检仍须先使用纯配置列表取名单，再启动全部禁用的 App Server。保持现有服务；个人库已经是 007 的事实不能作为新备份、启动、测试写入授权。

---

=== 提示词开始 ===

# 任务：PFC 产品全链路平台 · {本轮任务}

## 一、项目背景

你在继续开发一个面向产品经理的「从想法到上线全链路闭环」作业平台（PFC）。主体交互是对话 chat（类豆包），各阶段所需 Skill / MCP / 工具可自主加载与配置；开发阶段调用 Zed / Codex / VS Code 等本地开发终端并**实时跟踪终端对话流**。21号记录历史中间状态，24为已验收M2b-2，25/26为M2c-1方案与验收，27/28为M2c-2方案与验收；29第九节与30为已验收R1，31/32为已验收治理设计与证据，33为已确认R2设计，34为已验收R2结果，35为已确认R3方案，36为已验收R3记录，37为已确认M2c-4方案，38为已验收发布观察记录，39为能力核对，40为已确认的本地可用基线方案，41已验收，42/43记录已完成本机启用，44为当前真实AI与本地工具闭环方案。方案、合成演练、本地隔离集成和正式验收各自独立，不得从历史完成标记推断。

用户最新要求为“各阶段高效完成、阶段间无缝衔接”。须承接 29 号的 AI 原生协作要求：原型可先行且与 PRD 草稿共同演进，复用同一需求的材料、决定与产物版本，增量更新并保留必要确认和完成证据。R1 已完成本地合成交互验证；真实领域接入和编排机制需另行确认，不从演练结论推断真实集成完成。

## 二、代码库与运行方式

项目根：`D:\项目管理\product-full-chain-platform`（初始 Git 基线 `69ec9eb`；本轮2026-09-14核验main/origin/main为 `a20e16e`；下轮仍须再次fetch核验）

- 前端原型：`output\pfc-workbench-prototype\index.html`（原生 HTML/CSS/JS，无框架；`original\` 下 JS，加载顺序已固定）
- 后端：`server\`（Node + Express 4 + ws + jsonwebtoken + pg；`npm start` 默认 5188，显式 PFC_DB=memory 默认演示；pg 必须先迁移，失败不降级）
- 模拟终端 Bridge：`server\sim-bridge.mjs`（真实 WS 协议客户端）
- 文档：`docs\planning\prototype-v3\`（**必读**：19 路线图 / 21 检查点 / 23 防走偏协议；历史设计见 12-18）
- 旧 V3 对照稿已归档 `archive\prototype-v3-reference-20260912\`，**不属于当前开发入口**

## 三、已完成且已验证（不要重复开发）

1. M1：三模式数据层、后端骨架、前后端握手（PFCAPI：devLogin/hydrate/seed/flush）。
2. M2a：领域层（state-machine/store/service）；reqs 写端点、runs 审批硬门、租约、审计、事件通知真实实现。
3. M2b：`server\src\ws.js` 双通道（/ws/bridge + /ws/web）；前端 `ws-client.js` 订阅层。
4. M2b-2：`original\domain-client.js` 实施草稿（verify-m2b2-model 4/4、verify-m2b2-domain 7/7 通过）；**21 号保留历史草稿状态；M2b-2 已经本地协议/UI 自动化与陈立人工验收，证据见 24 号**。

## 四、本轮任务 {本轮任务}

### 4.1 必改点（依据最新确认包，历史21号只作追溯）

{根据19号及最新交接方案/检查点填写具体任务、文件、已探明事实；当前41已验收、42本轮E1–E4已完成见43；44已确认，45记录C1局部实现和待确认的全局规则外发范围；范围差异确认后按既定包连续实施，不重复询问整个方案；不恢复已完成的旧任务}

### 4.2 范围声明

- 允许动：{涉及文件白名单}
- 禁止：{禁止清单}
- 退出边界：{本轮明确不做的事}

### 4.3 建议验证路径

{本轮的端到端路径：启动顺序 + 操作步骤 + 预期现象}

## 五、硬约束（全程适用，23 号协议一票否决）

1. 无 OSS，文件本地管理。
2. 诚实标注模拟边界；禁止静默假装真实。
3. 零脑补/禁编造；数字、结论须有来源。
4. 技术栈固定：原生 JS + Express 4 + ws + jsonwebtoken + pg；仅允许44已确认的三项固定解析依赖，其他新依赖/升级不得自行扩大。
5. PowerShell 环境：命令不用 `&&` / `||`。
6. 不破坏 local / mock 模式行为。

## 六、验收标准（全部满足才算完成）

1. 本轮功能链路跑通（{具体验收现象}）。
2. {验收命令} 全部通过，**验证输出原文必须附在交付中**。
3. 无 console 错误；local/mock 模式零回归。
4. 续编 {续编文档号}：改动清单 / 验证结果 / 与交接包差异 / 诚实边界 / 下一步。
5. 待确认事项单独列出，等用户答复，不得私自决定。

## 七、完成交付

1. 修改后源码（前端 original\ + 后端对应目录）。
2. 验证输出（JSON/截图路径）+ 改动清单 + 与交接包差异报告。
3. 续编文档 {续编文档号}。
4. git：feature 分支 `feat/{任务代号}` 收工提交并推送，等待用户评审合并 main（23 号协议第三节）。

=== 提示词结束 ===
