# 20 · Codex 交接提示词模板（每轮复用）

> 用途：复制「=== 提示词开始 ===」与「=== 提示词结束 ===」之间的全部内容，粘贴给 Codex 继续落地。**每轮先填下方"填写表"，把参数代入后整段粘贴。**
> 配套文档：19 号路线图、21 号实施中检查点、23 号防走偏协议。固定开局指令见 23 号第二节。

---

## 每轮填写表（填完代入提示词）

> 下表为本轮已确认的 M2c-1 参数；陈立回复“同意”后已在 `feat/m2c-storage` 实施并完成本地验证，待人工评审，结果见 26 号。M2b-2 已验收并合并 main（`12d1733`），历史范围与证据仍见 24 号。

| 参数 | 本轮值（示例） | 本轮实际 |
|---|---|---|
| `{本轮任务}` | M2c：governance/release/projects 域落领域模型 + PG 存储适配 | M2c-1：PG 存储基础隔离验证及两处测试时序改进（25 号已确认；26 号本地验证通过） |
| `{任务代号}` | m2c-governance | m2c-storage |
| `{涉及文件白名单}` | server/src/domain/、server/src/routes/、server/sql/ | 25 号“M2c-1 待确认设计与精确范围”逐项路径，已经确认；实际差异见 26 号 |
| `{禁止清单}` | 不引新依赖 · 不改 12/13 号契约 · 不碰 archive/apps/packages · 不顺手修无关 bug | 不引依赖/不改 12、13/不碰 archive、apps、packages/不改既有业务前端、路由、服务/不写业务 schema/不启用真实工具 |
| `{验收命令}` | 六条回归 + verify-m2b2-model/domain（见 23 号第四节） | 23 号九条现有命令 + server/verify-m2c-pg.mjs，见 25 号；未执行不能填 PASS |
| `{续编文档号}` | 24-M2c治理域落表-{日期}.md | 26-M2c-1存储基础实施与验收-20260912.md（若跨日先明确新文件名） |
| `{待确认事项}` | 需用户答复的接口/范围变更（如有） | 本轮实施已本地验证，待陈立评审/主干合并授权；M2c-2 及其余子阶段另行确认 |

---

=== 提示词开始 ===

# 任务：PFC 产品全链路平台 · {本轮任务}

## 一、项目背景

你在继续开发一个面向产品经理的「从想法到上线全链路闭环」作业平台（PFC）。主体交互是对话 chat（类豆包），各阶段所需 Skill / MCP / 工具可自主加载与配置；开发阶段调用 Zed / Codex / VS Code 等本地开发终端并**实时跟踪终端对话流**。21 号记录历史中间状态，24 号为已验收的 M2b-2；25 号为已确认分轮方案，26 号为 M2c-1 实测交付，不得从历史完成标记推断。

## 二、代码库与运行方式

项目根：`D:\项目管理\product-full-chain-platform`（初始 Git 基线 `69ec9eb`；每轮须核对最新 `origin/main`，M2b-2 合并为 12d1733，下轮须再次核验远程）

- 前端原型：`output\pfc-workbench-prototype\index.html`（原生 HTML/CSS/JS，无框架；`original\` 下 JS，加载顺序已固定）
- 后端：`server\`（Node + Express 4 + ws + jsonwebtoken + pg；`npm start` 默认 5188，无 DATABASE_URL 时内存态降级）
- 模拟终端 Bridge：`server\sim-bridge.mjs`（真实 WS 协议客户端）
- 文档：`docs\planning\prototype-v3\`（**必读**：19 路线图 / 21 检查点 / 23 防走偏协议；历史设计见 12-18）
- 旧 V3 对照稿已归档 `archive\prototype-v3-reference-20260912\`，**不属于当前开发入口**

## 三、已完成且已验证（不要重复开发）

1. M1：三模式数据层、后端骨架、前后端握手（PFCAPI：devLogin/hydrate/seed/flush）。
2. M2a：领域层（state-machine/store/service）；reqs 写端点、runs 审批硬门、租约、审计、事件通知真实实现。
3. M2b：`server\src\ws.js` 双通道（/ws/bridge + /ws/web）；前端 `ws-client.js` 订阅层。
4. M2b-2：`original\domain-client.js` 实施草稿（verify-m2b2-model 4/4、verify-m2b2-domain 7/7 通过）；**21 号保留历史草稿状态；M2b-2 已经本地协议/UI 自动化与陈立人工验收，证据见 24 号**。

## 四、本轮任务 {本轮任务}

### 4.1 必改点（依据 21 号检查点与 19 号路线图，不自行扩展）

{根据 21 号未完成项 / 19 号 M2c-M4 表填写：具体任务、涉及文件、已探明事实}

### 4.2 范围声明

- 允许动：{涉及文件白名单}
- 禁止：{禁止清单}
- 退出边界：{本轮明确不做的事}

### 4.3 建议验证路径

{本轮的端到端路径：启动顺序 + 操作步骤 + 预期现象}

## 五、硬约束（全程适用，23 号协议一票否决）

1. 无 OSS，文件本地管理。
2. 诚实标注模拟边界；禁止静默假装真实。
3. 零脑补/禁编造；数字、结论须有来源。
4. 技术栈固定：原生 JS + Express 4 + ws + jsonwebtoken + pg；不引新依赖。
5. PowerShell 环境：命令不用 `&&` / `||`。
6. 不破坏 local / mock 模式行为。

## 六、验收标准（全部满足才算完成）

1. 本轮功能链路跑通（{具体验收现象}）。
2. {验收命令} 全部通过，**验证输出原文必须附在交付中**。
3. 无 console 错误；local/mock 模式零回归。
4. 续编 {续编文档号}：改动清单 / 验证结果 / 与交接包差异 / 诚实边界 / 下一步。
5. 待确认事项单独列出，等用户答复，不得私自决定。

## 七、完成交付

1. 修改后源码（前端 original\ + 后端对应目录）。
2. 验证输出（JSON/截图路径）+ 改动清单 + 与交接包差异报告。
3. 续编文档 {续编文档号}。
4. git：feature 分支 `feat/{任务代号}` 收工提交并推送，等待用户评审合并 main（23 号协议第三节）。

=== 提示词结束 ===
