# 44号方案差异评审

本轮为 docs-only/light，评审对象是方案与候选范围，产品实现、真实集成和验收尚未发生。评审人为 Codex 自审；产品方案确认和最终业务验收人为陈立，本轮无子代理。

本轮实际修改七份文档：AGENTS、19、20、43、新44、两份README。34cec2f开工带入的30dffd0启用历史含34个路径；42和27份原始证据保持字节不变，这些不是本轮重新执行结果。对比开工提交，新增证据只在本目录，业务源码、SQL、依赖与正式台账未改。

## 评审结论

- 44以真实对话、材料、联合候选、本地执行和可信测试结果为一个交付范围；测试贯穿开发，完整业务验收在真实链路具备之后，使用观察后置。
- 推荐复用本机Codex低层传输及当前Express/PG主线；旧0.153.4生成协议检查与已安装0.154.0真实兼容性明确分开。协议、账号、实际可用模型及Windows沙箱仍待C1验证。
- 对模拟回复、拒绝非模拟事件、scanned:false、仅登记能力、USER_REPORTED入口逐项追到源码；没有把缺失设计功能诊断成实测bug。
- 候选包145条精确路径（108既有、37新增）附既有sha256，七份实施文档、007隔离迁移、三项依赖候选、限定真实调用/数据/目录/进程、原35+新8共43闸与12组场景均有明确范围。源码列表不是本轮写授权。
- 核验了审批请求的实际命令/cwd丢失风险，方案要求新边界保留真实请求与实例级能力隔离；不以字符串前缀或提示词代替权限。
- 新增依赖仅完成公开元数据调查，未安装且未做确切锁依赖漏洞审计；把供应链审查安排在安装之前，失败须提出具体差异，不能宣称候选已安全通过。
- 007要求同步运行时、预检、备份与恢复兼容，并保持001–006不可变；当前个人5188/006不迁移、不重启。首包不擅自扩大到用户其他项目或真实部署。
- 未完成的Zed双向接管、通用MCP、复杂原型/OCR、团队与外部CI发布保留，不以首包完成替代5CAP/26Unit完成；等待用户确认该首包范围。
- 已修正20正文中“42启用仍待确认”的过期提示；43只追加用户路线修正，旧备份/就绪实测保留。

## 验证口径与限制

本轮只运行文档格式、链接/JSON/路径、源与历史指纹、Git差异、配置、正式POD治理和旧协议源完整性检查。具体结果与输出见verification-final.json；旧35闸和新增8闸本轮没有执行，不能从历史PASS推断真实模型或工具通过。Quick dry-run仅为计划。

初次通过Node派生Prettier遭沙箱EPERM，没有实际执行格式化；改用直接调用固定Node的同一Prettier入口通过。初次监听端口查询遭本地权限拒绝；经Auto-review批准只读查询后核验原端口/PID成功，无服务操作。不是通过结果掩盖失败记录，也不是绕过审批拒绝。

当前方案状态为DESIGN_READY_FOR_REVIEW。44 A方案/首包限制、145路径、依赖例外、007与真实调用限额等待陈立确认；未将沉默或“继续整理方案”视作这些动作的授权。按23完成当前feature收尾，不合并main。
