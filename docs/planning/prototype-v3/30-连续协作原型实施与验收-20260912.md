# 30 · 连续协作原型实施与验收

## 2026-09-13 · 验收确认与主干收尾

用户针对“R1 评审通过，同意合并并推送 main”的明确问题回复“确认”。接受对象为原型交付 **2cf9a3fca3a5898c873a96062663d75359bc792b**，包括原型/PRD 共同演进、八阶段合成承接和已披露限制；由陈立以会话确认方式完成本轮视觉/业务接受。该接受不表示真实 AI、Shell、PG 全链路或全平台生产验收。

本轮分类为 docs-only 验收收尾。允许更新 AGENTS、19、20、29、30、两处 README 和 [收尾证据](../../quality-gate/reports/flow-prototype-20260912/acceptance-closeout-20260913.json)，在 feature 提交推送验收记录，然后将 feat/flow-prototype 以 --no-ff 合并并推送 main。不改源码、数据库、依赖、正式台账或历史验证原文，不部署、不删除分支。合并后核对 main 树与已验收 feature 一致、工作区干净、远端 main SHA 相同；最终提交编号以 Git 为准。

本次已 fetch 并核对：main/origin/main=a9eec73，feature/origin feature=2cf9a3f，main 是 feature 的祖先。固定 Node 24.20.0 / npm 11.19.0；根及 server 的 npm ls --depth=0 --offline 通过，Quick dry-run 就绪（不算测试 PASS），check:config 与 check:delivery-governance 通过。14 个源码/测试文件及 30 个入口文件逐项 SHA256 匹配先前通过测试的版本，12 条原始闸的 PASS/exitCode=0/输出均存在。没有行为或基线变化，故未重跑整套浏览器/PG 检查，不将证据复核表述为新一轮测试执行。使用 fullstack-quality-gate 的文档/影响面规则，当前修改无需新增行为测试。

实现状态 LOCAL_VERIFIED；原型视觉/业务接受 ACCEPTED（陈立，2026-09-13）；真实集成、部署与生产发布 NOT_EXECUTED。正式 CAP/Unit、M2 状态及唯一 POD next route 不变。本次没有创建业务/测试数据、启动服务、浏览器或代理；无需清理运行进程。下一步据已接受交互明确治理/真实领域的后续设计范围，再确认实施包。

## 原实施授权与检查点

用户对 29 第九节 R1 包（评审提交 `b0ff6ebbf6e064f2c594cae0ee1bfbaf37dabb52`）明确回复“同意”。本轮为系统迭代 / strict，设计选择 R1，批准范围为 29 的 9.2–9.6；从最新 `origin/main=a9eec73ed65610f09a80f11331edbda1b4252a67` 建立 `feat/flow-prototype`，六文档按精确文件带入。

父级 POD-PFC-001 / 原型 M2 / 连续协作 R1；涉及原型需求/设计/运行/测试/验收/发布/观察的交互演练，正式 CAP/Unit 不升级。实现 LOCAL_VERIFIED；真实集成 NOT_EXECUTED；视觉/业务验收已于 2026-09-13 接受，验收人为陈立；发布 NOT_EXECUTED。POD next route 保持 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`。M2c-3 治理实现、真实 AI/工具、业务 PG 启用均未纳入。

源码/测试精确白名单为 29 号 9.5 的 14 文件（4 旧/10 新）；文档为 AGENTS、19、20、29、30、两处 README；证据仅 `docs/quality-gate/reports/flow-prototype-20260912.md` 及同名目录。禁止 server/SQL/依赖/12/13/台账/旧工程/归档/历史证据修改，原实施包不包含 main 合并或部署；本次 main 合并推送授权见上方 2026-09-13 记录。

## 实施计划

1. 已确认设计与环境检查、开工文档提交，记录原页面参考截图/样式。
2. 先写版本一致性、阶段门控、恢复和隔离的失败测试；实现独立合成工厂与演练模型。
3. 抽取原工作台框架，接入原型/PRD、差异、阶段承接和八阶段演练，保留原布局。
4. 三宽度实际浏览器交互、异常及键盘验证；原九条与新三条门禁串行执行，修复实际缺陷。
5. 差异自审、证据报告、清理回读、feature 提交推送，交陈立视觉/业务评审。

## 环境与测试数据设计

固定 Node 24.20.0 / npm 11.19.0；根/server 离线依赖检查退出 0；Quick dry-run 为 READY_TO_RUN，不是功能 PASS。已读 AGENTS、19/20/21/23/29、质量配置；使用 fullstack-quality-gate、ui-ux-pro-max 和 frontend-testing-debugging。Browser plugin not available（本会话无 browser skill），采用现有 Playwright + Edge，无安装/外部资产。

数据工厂 `CODEx_TEST_FLOW_20260912_<attempt>`，两个合成需求，覆盖正常/空/错误/部分生成、规则 7→3 天、拒绝/采纳、旧版本、owner/viewer 演练身份、等待输入/批准/未知/恢复、恶意文本；每一案例由确定性输入和预期版本/影响/状态断言验证。最多 20 需求、200 产物版本、10 MiB 浏览器存储。演练键为 `PFC_FLOW_DEMO_V1:<runId>`，不进入原需求树、API flush 或数据库；普通需求存储和网络请求独立比对。Web Locks 内重读 revision 并整组保存，无锁/存储失败显式阻断。

静态服务只监听 127.0.0.1 动态端口；原九条使用已存在工厂的临时 memory 服务、空 DATABASE_URL。浏览器新建隔离 context，无用户配置/资料；runner 为 Codex 所有，预期在验证 finally 关闭浏览器/服务，记录 PID/端口和清理结果，不保留后台服务。合成截图/JSON 保留在已确认证据目录用于本轮评审；没有真实敏感数据。

## 验证结果与限制

原九条 + 新 verify-flow-model/browser/architecture 共 **12 条全部 PASS**；另 verify-static、check:config、check:delivery-governance PASS。原文见 [命令记录](../../quality-gate/reports/flow-prototype-20260912/gate-commands.json)、[原六组子闸原文](../../quality-gate/reports/flow-prototype-20260912/original-prototype-subgates.json)、[附加检查](../../quality-gate/reports/flow-prototype-20260912/additional-checks.json)。完整命令、场景映射、截图和限制见 [质量报告](../../quality-gate/reports/flow-prototype-20260912.md)。

新增模型 11 组、浏览器 11 组、架构 4 组通过。三宽度截图和计算样式已检查；原工作台八阶段 × 三面板共 24 种组合的 HTML 内容与 a9eec73 相同，仅新增面板可访问名称。原领域模块/数据层/基础样式指纹未变。合成 PG DTO 视图可读且只读控件禁写，但没有真实 PG 会话复测；后续业务领域接入需补实库浏览器回归。测试和演练不代替真实 AI、服务器事务、安全边界或生产验收。

TDD 先记录工厂/模型缺失的预期失败，再实现；自审发现验收退回时总状态为 FAIL、单项仍全 PASS，增加失败测试后修正并保留历史 PASS。首次浏览器断言未等异步保存结束，截图证明入口已显示，改为等待实际保存状态；未弱化功能断言。新增损坏产物校验、下游变更入口、退出恢复与键盘焦点检查。最终门禁批次全部一次通过，用时 127,523 ms。附加配置检查曾因测试驱动构造了重复大小写 PATH 使用 Node18 失败，按仓库 PowerShell 激活 Node24 后复跑通过；[首次失败原文](../../quality-gate/reports/flow-prototype-20260912/additional-checks-first-attempt.json)保留。

## 本轮改动（文件级清单）

以下路径均在 `output/pfc-workbench-prototype/` 下，无删除：

| 文件 | 实际变化 |
| --- | --- |
| index.html（修改） | 加载 5 个新 JS 和一份限定样式；仍为唯一入口，共 26 个 JS |
| original/app.js（修改） | 场景入口、演练 URL/分派；隔离原保存、输入、附件及 tick；普通模式保留 |
| original/workbench.js（修改） | 两处委托共享框架，不改普通需求的领域逻辑 |
| original/workbench-shell.js（新增） | 原三栏框架、终端/画布/证据容器，普通工作区与演练共用 |
| original/flow-demo-data.js（新增） | 两个合成需求、八阶段与关联产物确定性工厂 |
| original/flow-model.js（新增） | 命令、权限演练、版本/阶段门控、Web Locks 串行保存、冲突/幂等/恢复/清理 |
| original/flow-view.js（新增） | 同一工作区内的当前动作、可点击业务模板、PRD/方案/验收项、差异/证据与双终端 |
| original/flow-actions.js（新增） | 单独保存草稿/滚动/焦点、交互分派、刷新/退出恢复、精确 runId 清理；不调用原 P.save |
| original/flow.css（新增） | 仅 .flow-room 下布局，复用原视觉变量；展开体验仍保留聊天 |
| verify-static.mjs（修改） | 加入新增模块；登记标准 structuredClone 全局，保留原规则和覆盖 |
| flow-browser-fixture.mjs（新增） | loopback 静态服务、隔离 Edge、合成参考、截图/请求与 finally 清理 |
| verify-flow-model.mjs（新增） | 11 组数据一致性、状态、恢复、限额和清理验证 |
| verify-flow-browser.mjs（新增） | 11 组实际界面链路、故障恢复、隔离、键盘和三宽度验证 |
| verify-flow-architecture.mjs（新增） | 精确范围/领域指纹、24 组合渲染、职责与下游变更保护 |

文档为 AGENTS、19、20、29、30、两处 README；证据仅本轮质量报告及同名目录。与 19/20/21 的差异是本次经 29 第九节批准的 R1：在既有原型增加演练，不推进 21/28 的真实领域接入。未改 server、SQL、依赖、12/13、旧工程、正式台账、归档或历史证据。新增“恢复”入口与错误/焦点修正均服务于已批准的中断恢复及可访问性语义，没有新接口或范围外功能。

## 体验与人工评审

直接打开 [index.html](../../../output/pfc-workbench-prototype/index.html)，确认顶部为“本地存储”，点击“交互原型 · 场景切换 → 连续协作演练”。独立 Edge 已验证 file URL 的安全上下文与 Web Locks 可用，见 [文件入口验证](../../quality-gate/reports/flow-prototype-20260912/file-entry-check.json)；不支持锁的浏览器会明确阻断，可改用本地 HTTP。

建议评审路径：生成草案 → 原型与 PRD 切换 → 输入“提醒时间改成提前 3 天” → 比较并采纳 → 确认业务与设计 → 开发/测试 → 人类演练验收 → 批准/执行模拟发布 → 采集合成观察样本。每步沿固定主动作继续，材料和版本无需复制。左侧切阶段只查看，不自动推进。退出保留演练，从场景窗口恢复；清理只删除当前演练。

原交付的评审范围（现已获陈立确认）：原型/PRD 共同演进是否符合预期；阶段间承接与必要确认是否清楚；三宽度、展开画布和双终端是否易用；是否接受当前固定模板、合成角色/执行及 PG 未复测的边界。陈立已于 2026-09-13 确认该范围并授权 main 合并推送，原评审材料保留作为依据。

## 对后续方案与开发的输入

| 本轮已演练的行为 | 后续真实实现需确认的接口/模型 |
| --- | --- |
| 原型/PRD/验收项一次关联采纳 | 独立产物版本、关联基线、原子事务与建议过期规则 |
| 自动承接材料、设计和验收项 | 不可变上下文快照、输入来源、依赖边与版本影响计算 |
| 业务确认、设计确认、验收和发布批准 | 各自授权对象、当前身份/版本重验及审计；不能复用前端演练角色作为权限 |
| UNKNOWN 核验与同一运行恢复 | 真实运行编号、工具回执、幂等、断线回读与取消语义 |
| 原型文件体验与反馈 | 真实生成能力、文件存储、预览执行隔离、附件来源与可信边界 |
| 观察结果形成下一需求建议 | 真实指标来源、观察窗口、证据关联和人工采纳 |

这些是下一轮设计输入，不是本轮新增的已批准契约。治理/项目与真实领域应据此明确范围后实施；不跳过方案确认，也不另建不受控全阶段编排。

## 差异、回退与下一步

严格承接 29 第九节；没有顺带实施治理 62 文件。阶段内授权连续有效时不重复询问，但业务/技术确认、模拟发布审批各有对象和记录。回退以 a9eec73 与本轮精确源码差异为依据，经授权恢复对应文件；演练清理只删除自身 runId，不清空用户 localStorage。无部署目标、生产观察窗或发布后 smoke；真实工具成本、生产吞吐与长期存储均未测。

开工提交 9bf5724，时间 2026-09-12 21:40:22 +08:00；收工提交/推送按 23 的 feature 纪律，最终 SHA 以 Git 为准。无并行代理或新增依赖。测试生成的数据全部来自工厂，隔离浏览器/临时 memory 服务关闭后丢弃；合成截图和报告作为本轮评审证据保留，不设自动删除。原回归的临时目录和 .local 证据按原规则保留。进程归属与端口独立回查见 [清理记录](../../quality-gate/reports/flow-prototype-20260912/process-cleanup.json)。

R1 已获陈立评审确认，本轮完成授权的主干合并收尾；治理/真实领域实施及业务 PG 启用仍按后续明确范围授权。POD next route 仍为 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`。
