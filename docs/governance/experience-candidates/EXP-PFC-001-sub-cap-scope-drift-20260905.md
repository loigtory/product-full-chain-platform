# EXP-PFC-001 子 CAP 完成被误作平台进度

| 字段       | 内容                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------ |
| 触发来源   | 用户复核平台能力时发现 Zed、Skills、MCP、产物版本、团队权限和发布闭环均不可见              |
| 问题模式   | 连续按单一 CAP 的 Txx/UI-Rxx 推进，完成后没有返回父 POD；临时 fixture 被长期保留           |
| 影响面     | 范围控制、里程碑、能力说明、数据真实性、UI 优先级和用户信任                                |
| 根因       | 项目规则有生命周期和安全门禁，但没有机器可读的父 POD 范围、跨 CAP 退出条件和下一路线校验   |
| 规则层级   | 项目 `AGENTS.md` + quality gate + 交付台账                                                 |
| 提议变化   | 强制 5 CAP/26 Unit、M1-M3、状态分层、PostgreSQL 事实源、fixture test-only 和 POD 下一路线  |
| 反回归证据 | `delivery-governance.test.ts` 负向用例、`check:delivery-governance`、quick/core            |
| 试行窗口   | 从 GOV-R1 起覆盖 M1、M2、M3 三个里程碑                                                     |
| 指标       | 偏离当前 POD 路线的迭代数；fixture 被误计为集成/验收次数；完成子任务后缺少父级下一路线次数 |
| 晋级条件   | M1-M3 均无上述误报，且门禁没有造成错误阻断                                                 |
| 降级条件   | 连续两次合法范围变更被门禁错误阻断，且无法通过调整台账结构解决                             |
| 退役条件   | 平台自身交付治理模块覆盖同等或更强校验，并有迁移和回归证据                                 |
| 当前结论   | `TRIAL / LOCAL_GATE_PASS`；仅在本项目生效，不写入 Codex Memory，不升级为全局规则           |

## 验收结论

GOV-R1 定向测试 7/7、最终 quick 174/174、core 集成测试 56/56 均通过；测试 schema 清理读回为 0，生产构建和本地 PostgreSQL 连通性通过。本结论只证明治理机制在当前仓库生效，不替代产品、代码、测试或安全责任人验收。
