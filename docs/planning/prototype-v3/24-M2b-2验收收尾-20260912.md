# 24 · M2b-2 验收收尾

> 人工验收更新（2026-09-12）：陈立回复“评审通过，继续”，接受分支 `feat/m2b2-acceptance` 的 `6dedc41`、本报告差异、实际创建入口和已列明的本地模拟边界。本轮原型状态提升为 **ACCEPTED（限 M2b-2 本地协议与 UI 范围）**，按 23 号以 no-ff 合并 main；不提升正式 CAP/Unit、PG、生产权限、真实执行或发布状态。下文“待评审”保留为收工时记录，当前以本条更新为准。

## 本轮改动（文件级清单）

| 文件 | 实际改动与用户可见结果 |
| --- | --- |
| `original/workbench.js` | 区分模拟 Bridge / 服务端模拟 / 待核验来源；未知 Git/租约和未计量预算不再显示为已确认；远程作业不显示未接入的模拟预览/输入/移交入口 |
| `original/domain-client.js` | 无服务端租约时显示未知，不能由 Bridge 在线推断租约有效 |
| `original/app.js` | 右侧真实滚动容器跟随日志尾部；向上阅读时保留位置；演示异常按钮不能改写远程作业 |
| `original/model.js`、`run-actions.js` | 阻断远程作业进入本地模拟控制、核验、预览 |
| `server/src/domain/service.js`、`server/src/ws.js` | 取消、取消回执、拒绝、失败、断线及离线完成更新已有 revision；重复取消不重复派发，旧状态消息不能覆盖新状态 |
| `verify-m2b2-browser.mjs`、`m2b2-browser-fixture.mjs` | 新增确定性 UI 工厂与 19 项本地整链路验证，独立 context、模拟 Bridge、截图、finally 清理 |
| `verify-m2b2-model.mjs`、`server/verify-m2b2-domain.mjs` | 模型从 4 增至 6 项；领域从 7 增至 8 项，补旧事件与取消重复请求回归 |
| AGENTS、20/21/23、本 24 号、两个 README、质量报告 | 校准 Git/命令/七类闸、填写本轮参数、保留历史检查点、续编当前证据 |

逐文件增删行数见 [file-changes.tsv](../../quality-gate/reports/m2b2-acceptance-20260912/file-changes.tsv)。前端路径均相对 `output/pfc-workbench-prototype/`；未列出的白名单文件没有为凑范围而修改。没有删除文件。

收工状态：**LOCAL_VERIFIED / READY_FOR_REVIEW**；人工验收仍待陈立评审，未合并、未发布。开工时为 IN_PROGRESS。2026-09-12 用户对“先完成 M2b-2 浏览器全链路验收，再进入 M2c；feature 分支留痕、协议文字校准、24 号差异评审”的建议回复“同意”。本轮按系统迭代、严格验证执行；采用 21 号已有原生 JS / Express 草稿收尾方案，不另起架构。需求创建、草稿登记/确认、阶段推进作为现有草稿验收对象；新增契约语义须先与用户对齐。

父级 POD-PFC-001；原型里程碑 M2b-2，涉及 CAP-PFC-01/02/03 的交互参考，不提升正式 Unit 台账。正式 POD next route 保持 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`。现有草稿的本地 UI/内存 API/模拟 Bridge 链路已验证；正式 PostgreSQL 集成、人工验收和发布未完成；原型评审通过后返回 19 号 M2c。

分支 `feat/m2b2-acceptance`，起点 `495e38965fd815b655fca0bd023b66afe0aaed41`；已 fetch 并确认本地/远程 main 一致、工作区干净。开工/收工各一次提交，仅推送本任务分支；main 合并由用户评审后决定。

文件白名单（仅在复现的收尾问题需要时修改）：

- `AGENTS.md`：Git 事实及本轮授权入口。
- `docs/planning/prototype-v3/20-Codex交接提示词-20260912.md`、`21-M2b-2前端切领域API-20260912.md`、`23-Codex防走偏协议-20260912.md`、本 24 号及该目录 `README.md`：填写参数、保留历史检查点、校准协议、续编结果。
- `output/pfc-workbench-prototype/README.md`：当前交接入口。
- `output/pfc-workbench-prototype/original/domain-client.js`、`ws-client.js`、`api-client.js`、`actions.js`、`run-actions.js`、`model.js`、`workbench.js`、`app.js`：现有 API 动作、远程状态隔离、双终端与恢复。
- `server/src/domain/service.js`、`store.js`、`server/src/routes/reqs.js`、`runs.js`、`server/src/ws.js`、`server/sim-bridge.mjs`：仅修复本链路已实现协议的可复现问题。
- `output/pfc-workbench-prototype/verify-m2b2-model.mjs`、新增 `verify-m2b2-browser.mjs`、`m2b2-browser-fixture.mjs`，`server/verify-m2b2-domain.mjs`：定向回归和确定性测试工厂。
- `docs/quality-gate/reports/m2b2-acceptance-20260912.md`、同名证据目录：命令原始输出及合成证据。`.local/m2b2-acceptance-*` 和 `output/playwright/m2b2-acceptance-*` 仅本地忽略证据，不提交。

禁止修改 archive/apps/packages、依赖/锁文件、SQL 迁移、12/13 契约、根工程质量规则和交付台账；保留所有历史备份/图片/脚本。禁止 M2c、真实 Shell/Codex/Zed、外部业务写入、部署及 main 合并。

选择最小收尾方案：复用既有动作与领域接口，先写失败验证，修复确认缺陷，再执行七类闸。整体重写或直接进入 M2c 都不在本轮选择内。局部调整以原有责任模块为边界，不拆迁无关页面。

实施顺序：环境预检 → 开工范围提交 → 新浏览器用例/复现 → 必要修复 → 九条验证入口 → 差异自审/原始输出/截图 → 收工提交推送 → 人工评审。无并行代理。

## 验证结果（命令 + 输出摘要 + 证据路径）

九条入口均取得 PASS。完整命令输出与首次失败原文见 [质量报告](../../quality-gate/reports/m2b2-acceptance-20260912.md)、[最终闸结果](../../quality-gate/reports/m2b2-acceptance-20260912/final-gate-results.json)。验收闸为原型七类，不等于旧工程 full 或生产验收。

| 命令文件 | 本轮结果 |
| --- | --- |
| `verify-prototype.mjs` | PASS：6 组；18 脚本语法、13 模块 ESLint；完整性 8、模型 11、定向 46、浏览器 13、scope7 12 |
| `verify-m1-layer.mjs` / `verify-m1-e2e.mjs` | PASS：6 / 8 |
| `server/verify-server.mjs` / `verify-server-m2.mjs` / `verify-m2b.mjs` | PASS：18 / 19 / 7 |
| `verify-m2b2-model.mjs` / `server/verify-m2b2-domain.mjs` | PASS：6 / 8 |
| `verify-m2b2-browser.mjs` | PASS：19；1280/1440/1920 × 960，9 张合成截图 |
| `npm run check:config` / `npm run check:delivery-governance` | PASS：Node 24.20.0；5 CAP / 26 Unit，正式 next route 不变 |
| 固定 Node `--check` 两个修改的 server 文件、`git diff --check` | PASS |

首次整闸最后一条因测试关闭弹窗误选遮罩失败，修正定位到 button 后只复跑受影响的新浏览器脚本，19/19；前八条验证的业务源码未变。首轮结果保留，未覆盖失败记录。更早红灯报告证明来源/模拟入口、滚动、取消 revision、模拟控制和异常场景污染被实际复现。

截图：[运行中的双终端](../../../output/playwright/m2b2-acceptance-20260912/running-dual-1440.png)、[长日志与阅读位置](../../../output/playwright/m2b2-acceptance-20260912/long-output.png)、[1280](../../../output/playwright/m2b2-acceptance-20260912/complete-1280.png)、[1920](../../../output/playwright/m2b2-acceptance-20260912/complete-1920.png)。文件在本地忽略目录保留；版本库收录路径/哈希及原始 JSON，见 screenshots.json。Codex 已查看上述截图；不代替产品负责人的视觉接受。

清理：最后一轮 Node server PID 13732、sim PID 528 已退出，Edge context 关闭；独立系统读回 5189/5191/5192/5193/5194 无监听，无匹配本轮验证/Playwright helper。普通沙箱首次 CIM 查询拒绝访问，随后经系统只读查询确认，证据为 process-cleanup.json。四个新增合成需求 R-1201～R-1204 的内存数据已随服务退出销毁。

以下为开工环境和测试设计记录：

开工环境：固定 Node 24.20.0、npm 11.19.0；根/server `npm ls --depth=0 --offline` 均退出 0，已安装依赖齐全，不重复联网安装；`npm run test:gate:quick -- --dry-run` 为 READY_TO_RUN，仅为计划检查，不是 PASS。已阅读 AGENTS、交接 README、19/20/21/23、现有质量配置。旧工程 Quick/Core/Full 由 Active Handoff Scope 限定，本轮使用下列原型七类闸。

Browser plugin not available；使用现有 Playwright + Edge，无依赖变化，PC 1280/1440/1920，移动端不在项目范围。

七类闸实际为九条入口：

```powershell
node output/pfc-workbench-prototype/verify-prototype.mjs
node output/pfc-workbench-prototype/verify-m1-layer.mjs
node output/pfc-workbench-prototype/verify-m1-e2e.mjs
node server/verify-server.mjs
node server/verify-server-m2.mjs
node server/verify-m2b.mjs
node output/pfc-workbench-prototype/verify-m2b2-model.mjs
node server/verify-m2b2-domain.mjs
node output/pfc-workbench-prototype/verify-m2b2-browser.mjs
```

执行前激活项目固定 Node PATH；测试进程显式 `PFC_DB=memory`、`DATABASE_URL=''`，不读取本地数据库凭据。新测试由确定性工厂通过 UI 创建前缀 `CODEx_TEST_M2B2_20260912` 的需求，回答澄清、确认版本、推进开发；正常/拒绝/重复/取消/断线/离线/长日志/只读场景复用同一有效前置链，断言 UI 与独立 API 回读一致。模拟 Bridge 与测试 JWT 仅在进程内；不保存 token 或客户数据。记录实际 createdIds、预期状态、断言、清理结果和失败证据。

新测试使用 loopback 临时服务、隔离 browser context、模拟 Bridge；父测试负责 finally 关闭和端口读回，服务退出即销毁内存数据。默认端口 5194；预期仅运行本轮 gate 期间。长输出有界，不做性能或费用承诺。证据仅合成内容，保留供当前评审，忽略目录不提交。

退出条件：UI 创建→版本确认→阶段推进→计划批准→模拟 Bridge 实时双终端→SUCCEEDED，拒绝/恢复等定向用例及七类闸全部有原文；无未解释前端异常；用户视觉/业务验收作为后续单独结论。

## 与交接包的差异（超范围项必须列出）

21 为历史草稿事实，本轮仅续编而不抹去当时“待确认/未验收”。本轮没有新增接口路径、改变 12/13 契约或引入依赖。按既有控件完成“登记新需求→开始澄清→确认版本”；19 的“对话创建需求”没有被解释为新增自由文本自动建需求或真实 AI 调用，评审需确认这一实际入口符合本期口径。19 的“落库”在当前测试中是内存保存，不是 PG 持久化；其 ID 不冲突的假设已由 21 修正。协议校准：Git 已有历史；PowerShell 逐条命令；明确白名单暂存；“7 项”指七类而非七条命令。

## 诚实边界（模拟/未做/待确认）

不把模拟 Bridge、离线回退、内存保存视为真实工具执行或持久化。M2c/M3/M4 的 PG、治理、生产认证、多租户和真实 Bridge 延后。当前没有已私自落地的新契约。源码复核发现服务端写路由仍主要校验 JWT，未完整强制执行角色权限；本轮只读用例仅验证前端体验阻断。M3 权限/租户/WS 生产化应优先补齐，不能对外或接真实工具后沿用该安全边界。

工程/测试执行人为 Codex；产品、工程、测试、安全及验收负责人陈立。通过自动化后只记 LOCAL_VERIFIED / READY_FOR_REVIEW。没有 ACCEPTED/RELEASED 或生产就绪结论；没有发布观察、生产 smoke 或真实负载验证。

回滚：未合并 feature 分支可整轮不采纳；逐文件依据 Git 审阅回退，不覆盖用户改动。无运行环境迁移，不存在生产数据回滚。资源异常停止新增进程，保留失败输出并审计本轮 PID。

## 下一步（下轮任务参数建议）

请陈立评审文件差异、实际创建入口、双终端截图及模拟/权限边界。用户确认前不标 ACCEPTED、不合并 main、不开始 M2c。下一轮按 20 号模板单独填写 M2c 的治理/发布/项目及 PG 范围。

版本留痕：开工提交 `d4499a3`，14:37:51 +08:00；最后浏览器验证 15:00，15:02 完成系统清理读回。实施与验证约 25 分钟（不含前置阅读和最终报告/提交）。保留 5 份红灯报告，包含产品缺陷和测试工厂/定位修正，不能将报告数当作缺陷数；正式整闸首次 1 条测试失败，修正选择器后通过。回顾：以后终端测试先识别真实滚动容器；关闭对话框明确定位按钮；新增异常用例独立断言，避免前例失败导致后例条数误判。无发布后指标或观察数据。


## 人工评审后的合并复核（2026-09-12）

陈立回复“评审通过，继续”，已接受 `6dedc41` 的 M2b-2 本地协议/UI 范围，授权按 23 号 no-ff 合并 main。合并增补仅为本次验收状态、路线图/入口及原始复核证据；业务源码和测试脚本均与已评审提交一致。

Git 切换分支时转换了部分 LF/混合换行，旧原始字节哈希不再直接相等。逐一对照 11 个已测文件与 `6dedc41` 的 Git 内容，统一 CRLF/LF 后全部一致；另记录合并工作区原始字节哈希，并在该工作区重新执行全部九条验收命令。最终九条退出码均为 0，浏览器 19/19。[合并复核原文](../../quality-gate/reports/m2b2-acceptance-20260912/merge-verification/results.json)与同目录日志、JSON、源码清单保留供复查。

复核不是首轮全绿：普通沙箱首先因 `spawnSync EPERM` 无法创建 Node 子进程，经已授权本地测试的自动预审后正常执行。随后 M1 握手在固定 2.5 秒等待后未读到登录 token，原断言诊断副本和原脚本复跑均 8/8；诊断登录响应为 686ms，首次失败原因尚未复现。M2b-2 浏览器首次 18/19，失败在实时重绘时截图滚动节点脱离 DOM；原脚本一次复跑 19/19。没有修改源码、等待时间或断言，也没有覆盖首次失败原文；两处测试稳定性问题保留为下轮候选，不能称为已修复。

15:54 独立系统读回：5189/5191/5192/5193/5194 无监听，匹配的本轮 Node/Edge helper 为零；最后浏览器子进程 30812/39584 已退出，四个合成需求随内存服务销毁。PostgreSQL 服务仍为 Stopped，本次没有数据库写入、真实工具或部署。人工接受的范围保持不变；M2c 详细方案另行确认。
