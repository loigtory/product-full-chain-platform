# 20 · Codex 交接提示词模板（每轮复用）

> 用途：复制「=== 提示词开始 ===」与「=== 提示词结束 ===」之间的全部内容，粘贴给 Codex 继续落地。**每轮先填下方"填写表"，把参数代入后整段粘贴。**
> 配套文档：19 号路线图、21 号实施中检查点、23 号防走偏协议。固定开局指令见 23 号第二节。

---

## 每轮填写表（填完代入提示词）

> 当前参数：M2c-2 已验收并合并推送 main（a9eec73）。当前按“继续”准备 M2c-3 治理与项目方案，分支 feat/m2c-governance-plan、续编 29 号。只做方案与差异核对；具体功能、角色策略及隔离 PG 测试范围待确认，日常业务 PG 启用仍按 28 号单独处理。

| 参数 | 本轮值（示例） | 本轮实际 |
|---|---|---|
| `{本轮任务}` | M2c：governance/release/projects 域落领域模型 + PG 存储适配 | M2c-3 治理与项目方案、契约差异、逐文件范围和验证设计；不实施 |
| `{任务代号}` | m2c-governance | m2c-governance-plan |
| `{涉及文件白名单}` | server/src/domain/、server/src/routes/、server/sql/ | AGENTS、19、20、29、两处 README，共 6 个文档，精确路径见 29 号 |
| `{禁止清单}` | 不引新依赖 · 不改 12/13 号契约 · 不碰 archive/apps/packages · 不顺手修无关 bug | 不改源码/SQL/依赖/12/13/台账/旧工程/归档/原始证据；不写库、不启用业务 PG、不执行真实工具、不合并 main、不部署、不删分支 |
| `{验收命令}` | 六条回归 + verify-m2b2-model/domain（见 23 号第四节） | 方案轮：固定 runtime、两处离线依赖、Quick dry-run、check:config、check:delivery-governance、文档链接/范围及 git diff --check；实施门禁另见 29 号候选包 |
| `{续编文档号}` | 24-M2c治理域落表-{日期}.md | 29-M2c-3治理与项目方案及范围确认-20260912.md |
| `{待确认事项}` | 需用户答复的接口/范围变更（如有） | 29 号方案、成员即时授权、计划快照/项目关联语义、逐文件实施及测试包；当前尚未批准实施 |

---

=== 提示词开始 ===

# 任务：PFC 产品全链路平台 · {本轮任务}

## 一、项目背景

你在继续开发一个面向产品经理的「从想法到上线全链路闭环」作业平台（PFC）。主体交互是对话 chat（类豆包），各阶段所需 Skill / MCP / 工具可自主加载与配置；开发阶段调用 Zed / Codex / VS Code 等本地开发终端并**实时跟踪终端对话流**。21 号记录历史中间状态，24 号为已验收的 M2b-2；25 号为已确认分轮方案，26 号为 M2c-1 已验收交付，27 号为已确认的 M2c-2 设计，28 号记录当前实施、实测、限定范围验收及 Git 授权，不得从历史完成标记推断。

## 二、代码库与运行方式

项目根：`D:\项目管理\product-full-chain-platform`（初始 Git 基线 `69ec9eb`；本次合并前远程 main 为 `05af5de`，验收实现为 `11d1fdf`；合并结果以 Git 及远程读回为准，下轮须再次 fetch 核验）

- 前端原型：`output\pfc-workbench-prototype\index.html`（原生 HTML/CSS/JS，无框架；`original\` 下 JS，加载顺序已固定）
- 后端：`server\`（Node + Express 4 + ws + jsonwebtoken + pg；`npm start` 默认 5188，显式 PFC_DB=memory 默认演示；pg 必须先迁移，失败不降级）
- 模拟终端 Bridge：`server\sim-bridge.mjs`（真实 WS 协议客户端）
- 文档：`docs\planning\prototype-v3\`（**必读**：19 路线图 / 21 检查点 / 23 防走偏协议；历史设计见 12-18）
- 旧 V3 对照稿已归档 `archive\prototype-v3-reference-20260912\`，**不属于当前开发入口**

## 三、已完成且已验证（不要重复开发）

1. M1：三模式数据层、后端骨架、前后端握手（PFCAPI：devLogin/hydrate/seed/flush）。
2. M2a：领域层（state-machine/store/service）；reqs 写端点、runs 审批硬门、租约、审计、事件通知真实实现。
3. M2b：`server\src\ws.js` 双通道（/ws/bridge + /ws/web）；前端 `ws-client.js` 订阅层。
4. M2b-2：`original\domain-client.js` 实施草稿（verify-m2b2-model 4/4、verify-m2b2-domain 7/7 通过）；**21 号保留历史草稿状态；M2b-2 已经本地协议/UI 自动化与陈立人工验收，证据见 24 号**。

## 四、本轮任务 {本轮任务}

### 4.1 必改点（依据 21 号检查点与 19 号路线图，不自行扩展）

{根据 21 号未完成项 / 19 号 M2c-M4 表填写：具体任务、涉及文件、已探明事实}

### 4.2 范围声明

- 允许动：{涉及文件白名单}
- 禁止：{禁止清单}
- 退出边界：{本轮明确不做的事}

### 4.3 建议验证路径

{本轮的端到端路径：启动顺序 + 操作步骤 + 预期现象}

## 五、硬约束（全程适用，23 号协议一票否决）

1. 无 OSS，文件本地管理。
2. 诚实标注模拟边界；禁止静默假装真实。
3. 零脑补/禁编造；数字、结论须有来源。
4. 技术栈固定：原生 JS + Express 4 + ws + jsonwebtoken + pg；不引新依赖。
5. PowerShell 环境：命令不用 `&&` / `||`。
6. 不破坏 local / mock 模式行为。

## 六、验收标准（全部满足才算完成）

1. 本轮功能链路跑通（{具体验收现象}）。
2. {验收命令} 全部通过，**验证输出原文必须附在交付中**。
3. 无 console 错误；local/mock 模式零回归。
4. 续编 {续编文档号}：改动清单 / 验证结果 / 与交接包差异 / 诚实边界 / 下一步。
5. 待确认事项单独列出，等用户答复，不得私自决定。

## 七、完成交付

1. 修改后源码（前端 original\ + 后端对应目录）。
2. 验证输出（JSON/截图路径）+ 改动清单 + 与交接包差异报告。
3. 续编文档 {续编文档号}。
4. git：feature 分支 `feat/{任务代号}` 收工提交并推送，等待用户评审合并 main（23 号协议第三节）。

=== 提示词结束 ===
