# PFC Server · M1 环境变量（复制为 .env 或直接设置环境变量）
PORT=5188
# PostgreSQL 连接串（留空或不设置 → 降级内存态，仅演示；正式请配置）
DATABASE_URL=
# 开发鉴权密钥（正式环境必须替换并妥善保管）
JWT_SECRET=pfc-dev-secret-change-me
# 强制存储模式：auto（有 PG 用 PG，无则内存）| memory | pg
PFC_DB=auto
