# Product Full Chain Platform - Agent Instructions

- CURRENT C1 PARTIAL / BLOCKED_SCOPE_AMENDMENT 2026-09-14: exact global AGENTS context exception is USER_CONFIRMED. First real synthetic text PASS (1/20 turns,5 seconds);007 isolated migration/queue/material evidence passes6 groups. Actual007 compatibility tests FAIL2: unregistered member accepted,closed-phase guard skipped. Three exact missing whitelist files and6 version-only edits are PROPOSED_NOT_APPLIED in scope-amendment-007-guards.json; user async confirmation pending. Do not edit commands.js,requirement-service.js,release-guard.js before that response. Windows unelevated restricted-read probe FAILED; elevated AppServer path unverified, no setup/system changes.18 JS static checks/protocol9/model10/material13 pass; full43/HTTP/UI/tools/acceptance incomplete. Preserve006 personal5188,original scope/limits/protected history; no push/main merge.45 owns current continuation; all older context/provider pending paragraphs are historical.

These rules apply to the implementation repository at `D:\项目管理\product-full-chain-platform`.

## Active Handoff Scope - 2026-09-14

- C1 CURRENT PARTIAL / BLOCKED_CONTEXT_CONFIRMATION:44 already authorizes the existing Codex connection, and the redundant provider question is withdrawn. Real non-generating preflight passes at pinned connection fingerprint b585d723d61d18a815b77a2d81627cd71038d1f8cf7f4446f56e48eba4937292. Text preparation still loads one global AGENTS.md despite zero project-doc budget, disabled53 Skills, memories/tools/MCP. Exact extra context is28443 bytes, SHA256 b77f388f6dcbbc729550c5a3ac73d91a68264e33421173ba02c337390d3f892c.44 permits synthetic-only; the specific global-rule exception in45/context-exception-proposal.json is PENDING. Zero turns/DB writes; no007/API/UI integration. Nine new JS paths plus serverpackage/lock are partial; protocol9, materials13, text-policy7 pass. Complete checkpoint lint/format/protected/process checks and local WIP commit; no full gate/acceptance/push claim. Earlier provider-pending sentences below are superseded historical observations, not a second open decision.

- AI-TOOLS-INTEGRATION IMPLEMENTATION CONFIRMED: user replied “继续” to the exact44 optionA/section6 execution package at2f90fa1. Execute D1-D8/C1-C4 with145 exact paths,seven implementation documents including45,007 isolated migration,three fixed parser dependency candidates subject to audit,20 real turns/synthetic-workspace tool limits,three new and seven explicitly renewed legacy isolated schemas,43 gates. Scope fingerprint, data/cleanup and authorization are recorded in ai-tools-integration-20260914/authorization-20260914.json. Branch feat/ai-tools-integration from main a20e16e verified via same-origin ls-remote after two network-failedfetches; carry43 prior document/evidence paths without mutation except seven current docs. No subagents, personal5188 restart/007 activation, main merge, root dependency/oldSQL/generated-code/ledger/history changes or external deployment. Prior proposal push auto-review denial remains recorded; no bypass. Open/close feature commits under23; each outbound payload needs applicable exact authorization and review. Current C1 PARTIAL/WAITING_PROVIDER_DECISION: two isolated modules and factory/two gates exist; protocol unit7 and material13 checks pass. Actual CLI0.154.0 handshake passes and startup MCP/apps/hooks isolation is now verified via config-only mcp-list before launching. Local configured provider is custom/apiKey/modelgpt-6-astra, connection fingerprint b585d723d61d18a815b77a2d81627cd71038d1f8cf7f4446f56e48eba4937292; user choice is pending and zero model turns ran. Three approved parser deps installed without scripts/optional binaries; actual server audit reports two existing moderate express/qs entries, no previous dependency version changed. No007/schema/HTTP/UI integration yet. Auto-review rejected the earlier discover-after-start probe; safer list-before-start probe subsequently approved/passed configuration isolation. Partial WIP checkpoint may be committed with precise verification; no full completion/acceptance/push claim. Preserve original services; formal POD-PFC-001/M2/5CAP26Unit next POD-PFC-001/M2/R3a/artifact-collaboration-tdd unchanged. Earlier PROPOSED_NOT_AUTHORIZED describes the prior design phase and is superseded only for this confirmed package.

- AI-TOOLS-INTEGRATION DESIGN_READY_FOR_REVIEW: user prioritizes real AI/tools before end-to-end acceptance and usage observation.44 recommends local Codex App Server + current Express/PG/original UI, D1-D8, exact145 candidate source/test/dependency paths, seven implementation documents including45, three new isolated schemas plus seven explicitly renewed legacy test targets, bounded20 real turns/synthetic workspace execution,43 gates. This package is PROPOSED_NOT_AUTHORIZED; provider/limits/dependency exceptions and007 require confirmation. Current feat/ai-tools-integration-plan from main a20e16e, opening34cec2f is docs-only/light: AGENTS,19,20,43,new44,two READMEs and new ai-tools-integration-plan-20260914 JSON/Markdown evidence. Installed CLI0.154.0 versus old0.153.4 adapter remains integration-unverified. No product source/SQL/dependency/personal-runtime writes or real calls, observation, main merge, subagents. Preserve5188 API19412/controller14924 and PG13528/API31152/Vite37744. Carry30dffd0 activation history without altering42 or original evidence. Complete document/source/protected/config/delivery/protocol-source verification and scoped feature closeout commit/push under23. Formal POD-PFC-001/M2/5CAP26Unit and next POD-PFC-001/M2/R3a/artifact-collaboration-tdd unchanged. Prior owner-use-next instructions are superseded by this development-first steering.

- LOCAL-ACTIVATION LOCAL_ENABLED / READINESS_VERIFIED: user-authorized42 E1-E4 and necessary E6 completed on feat/local-activation;43 and local-activation-20260914 evidence own current facts. Opening d9dfa7a uses accepted e3f5d45 source. Actual5432/pfc_local/pfc_workbench has58 tables,6 migrations,one tenant/Owner and zero other business rows/files. One completed backup0aa1731a-d522-4b91-8151-9821df8b92df and one isolated5548/5198 restore passed; source unchanged and owned temporary roots/helpers independently removed.5188 loopback API19412/controller14924 retained as authorized; health/API/Edge1440 readiness and logout pass. Original PG13528/API31152/Vite37744 preserved. Finish seven-document/named-new-evidence checks and scoped feature closeout commit/push with remote readback; no main merge. Source68/protected3009/original9 evidence unchanged. Actual owner use is AWAITING_OWNER_USE; E5 workday observation and second backup excluded. No source/SQL/dependency/formal-ledger/history edits, real AI/Shell/Bridge/CI/deployment or subagents. Implemented source remains LOCAL_VERIFIED/SCOPED_ACCEPTED; formal POD-PFC-001/M2/5CAP26Unit next POD-PFC-001/M2/R3a/artifact-collaboration-tdd unchanged. Earlier proposal/execution paragraphs are historical, superseded only for this narrow authorized run.

- LOCAL-ACTIVATION PROPOSAL READY: accepted e3f5d45; acceptance ec234759500469dd3c980b1a70584e448b9b1a4a and main a20e16e53fa0d5661bd81f830749864a31dba3fd pushed/read back. Current feat/local-activation-plan opening97c1026 is docs-only/light: AGENTS/19/20/42/two READMEs plus new local-activation-20260914 JSON/Markdown evidence.42 E1-E6 is a concrete PENDING USER CONFIRMATION package: fixed accepted source; empty5432 pfc_workbench initialization/three personal roots; one empty-backup5548/5198 restore with ownership cleanup;5188 personal startup/retention; one actual owner workday observation and at most two completed cold backups; seven execution documents including43 and scoped feature commits/push. Current target absent, ACL tools ready but actual new-target ACL unverified; parent.local has other inherited principals. No business actions ran this proposal round. Offline dependencies ready and Quick dry-run only; final document/source/protected/config/delivery checks required before feature closeout. No source/SQL/dependency/ledger/history edits, real tools/deployment, subagents or new retained helpers. POD-PFC-001/M2/5CAP26Unit next remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier acceptance-closeout instructions are completed history; earlier pending implementation paragraphs are superseded.

- LOCAL-USE ACCEPTANCE CLOSEOUT: user “已验收，继续” accepts delivered e3f5d455d274cb95af4493138031e86da0236953 within40/41. Under23 acceptance-to-main flow, record SCOPED_ACCEPTED in AGENTS/19/20/41/two READMEs and new local-use-baseline-20260913/acceptance-20260914.json, commit/push feature, merge --no-ff refreshed main and push/read back. Current source68/protected3009 and immutable35-gate evidence rechecked; no behavior changes or data-dependent rerun. Classification docs-only/light. Then prepare42 concrete local activation proposal on feat/local-activation-plan from refreshed main; exact proposal scope AGENTS/19/20/new42/two READMEs plus new local-activation-20260914 redacted JSON/Markdown evidence. Read-only target/config/ACL/process preflight, offline bootstrap and document checks; no business initialization/start, real AI/Shell/Bridge/CI/deployment, SQL/dependencies/formal ledger/history edits or subagents. Activation requires the concrete package specified by40 section7. Original PG13528/API31152/Vite37744 remain. POD-PFC-001/M2/5CAP26Unit and next POD-PFC-001/M2/R3a/artifact-collaboration-tdd stay unchanged. Earlier PENDING/no-main-merge statements describe the completed implementation phase and are superseded only for this accepted Git closeout.

- LOCAL-USE LOCAL_VERIFIED: final gates-1789317348153.json is a single35/35 PASS run,621763ms,68 source hashes unchanged. New model6/API5/ops9/architecture4/browser5 pass;3009 protected files and original67 authorization/3010 baseline unchanged. User-confirmed verify-static supplement changes only42-to43. Source diff22 added/32 modified/14 untouched; R3 verifier readiness/HTTP isolation corrections retain every original assertion, and failed runs/legacy timing risk remain in evidence. Independent cleanup-final/process-final PASS: seven test schemas plus business schema absent, seven roots empty,5188-5198/5548 free,no owned helpers; original PG13528/API31152/Vite37744 preserved. Current41/quality report/readiness-final/diff-review-final own final evidence. Complete authorized scoped feature closeout commit/push and remote SHA readback; human review PENDING. No main merge/business enablement/real tools/deployment or formal ledger advancement. Formal next remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier checkpoints are historical, not open scope questions.

- LOCAL-USE BASELINE IMPLEMENTATION CONFIRMED: the user replied “确认” to proposal40 option A at f1470eb. Execute D1-D8/C1-C4, its exact67 source/tool/test files, seven documents including41, named new evidence root, seven disposable5432 schemas and one owned5548 restore cluster/files/process cleanup package,35 gates. Branch feat/local-use-baseline starts from refreshed origin/main102cbbe with the approved six proposal documents. Checkpoint41 and local-use-baseline-20260913/authorization-20260913.json own the approved normalized design fingerprint, work and test evidence. Opening/closing feature commits and push follow23. No main merge, business pfc_workbench enablement, real AI/Shell/CI/deployment, SQL/dependency/ledger/historical-evidence edits or subagents. Keep pre-existing PG13528/API31152/Vite37744; close owned helpers. Formal POD-PFC-001/M2/5CAP26Unit next remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Proposal-pending paragraphs below are historical and superseded by this confirmation.

- LOCAL-USE BASELINE PROPOSAL: M2c-4 source2642524 is scoped ACCEPTED; acceptance3894b96 and main102cbbe6b253a2e24b43d24e39af0ecc1b01fc18 are pushed and independently verified. Continue user-confirmed39 option A as docs-only / light on feat/local-use-baseline-plan from that main. Exact six files: AGENTS,19,20,new40,and two handoff READMEs. Inspect current runtime/auth/migration/files and read-only candidate target metadata; do not expose secrets or create business/test data. Proposal40 is DESIGN_READY_FOR_REVIEW: option A/D1-D8,67 exact candidate source/tool/test paths,seven implementation documents,new evidence root,seven disposable5432 schemas and one owned5548 restore cluster,35 gates. The concrete implementation package is PENDING USER CONFIRMATION; business enablement needs a later concrete activation package after implementation acceptance. Complete proposal checks and feature closeout. Opening/closing feature commits and push follow23; no proposal main merge or source/SQL/config/dependency/ledger/history changes, real tools/deployment, subagents or retained helpers. Formal POD-PFC-001/M2/5CAP26Unit next remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier acceptance-closeout action instructions are completed historical records.

- M2c-4 ACCEPTANCE CLOSEOUT CONFIRMED: after being asked explicitly to accept2642524, merge/push main and prepare40 local-use baseline design, the user replied “确认”. Record scoped ACCEPTED for26425240340ab813b8d9e0f60765df02f11f1140 in AGENTS/19/20/38/39/two READMEs and new acceptance-20260913.json; original30 gate/source/protected evidence remains immutable. Commit/push the current feature, merge --no-ff into refreshed main and push, independently read back SHA. Then create a docs-only / light proposal branch from latest main; exact six proposal files are AGENTS,19,20,new40,and the two READMEs. Write the concrete local-use configuration/identity/storage/backup/recovery/test package before asking for implementation authorization. No source/SQL/config/credentials/data writes, business PG enablement, real tools/deployment, dependency changes, ledger changes, branch deletion or subagents. Formal POD-PFC-001/M2/5CAP26Unit next route remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier PENDING_REVIEW/no-main-merge paragraphs below are superseded for this accepted delivery.

- M2c-4 LOCAL_VERIFIED / PENDING_REVIEW: the user-confirmed historical acceptance recovery is implemented. The complete30-command run passed; final visual QA then fixed a cache-miss recovery bug and isolated the historical-member browser session. Affected static/architecture/browser checks passed again. gates-recovery-final.json records all30 passing commands with explicit unchanged-source/retest provenance and current63 source hashes; release model11/API20/browser14 and architecture5/2806 protected files pass. Ten source/test files changed from31f50e7, inside the original63 scope; no new SQL/dependencies/API routes. Current instructions and evidence are37/38 and the updated quality report. The prior business decision is closed; only concrete human visual/business acceptance remains. Six exact schemas and five file roots independently read back empty, owned test helpers closed. Finish scoped feature commit/push and remote SHA verification; no main merge/business PG/real execution. Formal POD next route unchanged. Earlier CONFIRMED/DECISION_PENDING paragraphs below are historical checkpoints, not open decisions.

- M2c-4 RECOVERY RULE CONFIRMED: user replied “同意” to the concrete question after checkpoint31f50e7. A current Owner may re-review the same unchanged frozen plan and reuse its historical acceptance to reconcile the original UNKNOWN attempt when the old approver loses access. Continue on feat/m2c-release-observation within the original63 source/seven-document/six-isolated-target package. Only historical member validity is relaxed in that server-authorized reconciliation path; current actor/reviewer permission, unchanged business/version fingerprint, evidence, time, prior-event and same-attempt checks remain mandatory. New attempts and ordinary release inputs retain live validation. Record the design supplement in37 and progress in38, add failing API/browser regressions, implement, verify affected paths plus the30 required commands, review differences and commit/push feature. No main merge or acceptance claim from this rule confirmation. The following DECISION_PENDING checkpoint is historical.

- M2c-4 VERIFIED CHECKPOINT / DECISION_PENDING: implemented behavior passes all30 required commands with explicit unchanged-source/retest provenance; model10/API17/browser13/new architecture5 and2806 protected files pass. Six schemas and five file roots independently clean, owned helpers closed, pre-existing PG13528/API31152/Vite37744 preserved. Exact63 scope is27 new/35 modified/1 unchanged; no new dependencies or ledger/history changes. Checkpoint38 and its quality report are current. One business decision remains: when UNKNOWN coincides with loss of the old approver AND invalid historical product acceptance/upstream named inputs, whether a current Owner may reuse the unchanged frozen acceptance snapshot to reconcile the same attempt. This exception is not implemented; do not infer consent or mark the whole iteration accepted. Re-review when product inputs remain valid already works. Finish scoped feature checkpoint commit/push under23, clearly marked rule pending; no main merge/business PG/real execution. After the decision, continue the same approved file/data scope with focused regression and concrete acceptance. Formal POD next route remains unchanged.

- M2c-4 IMPLEMENTATION CONFIRMED: user replied “确认” to 37 option A delivered5ee58fa; D1-D8/C1-C4, exact63 source/test paths, seven documents including38, named new evidence root, six exact disposable local PG schemas/files/limits/start-restore/cleanup and30 gates are authorized. Approved normalized design SHA256 7b72c4e044bfbfdc067581ff43d5d48f34f8ee473c4b0d1fa542003d4a1ef6ab. Branch feat/m2c-release-observation starts from refreshed origin/main0689723 with the approved six proposal documents. Checkpoint38 and m2c-4-release-observation-20260913/authorization-20260913.json own progress and scope. Feature opening/closing commits/push follow23; main merge, business PG enablement, real tools/CI/deployment, new dependencies, ledger/history evidence changes remain excluded. No subagents; preserve existing PG13528/API31152/Vite37744; close owned helpers. Formal POD-PFC-001/M2/5CAP26Unit next route remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier proposal-pending instructions are historical and superseded by this confirmation.

- M2c-4 PROPOSAL ONLY: R3 b07dadf is scoped ACCEPTED; acceptance f6da3cc and main merge068972367660285e5dc5665b6021c33edb60f75f are pushed and independently verified. Continue docs-only / light on feat/m2c-release-observation-plan from that main. Exact scope: AGENTS,19,20,new [37](docs/planning/prototype-v3/37-M2c-4发布观察与最终复盘方案及范围确认-20260913.md),and the two handoff READMEs. Prepare release preparation review, USER_REPORTED outcomes, observations and final acceptance/retrospective; Candidate option A / D1-D8 specifies63 exact source/test paths, seven implementation documents, the named new evidence root, six exact disposable PG targets with limits/start-restore/cleanup, and30 gates. The full package remains PENDING USER CONFIRMATION. Feature opening/closing commits and push follow23, no proposal main merge. No source/SQL/dependency/business PG/real tools/deployment/ledger/history evidence edits; no test data, subagents or retained helpers. Formal POD-PFC-001/M2/5CAP26Unit next route stays POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier R3 closeout action instructions are completed historical records.

- R3 ACCEPTANCE CLOSEOUT: the user said “已验收完毕，继续” after delivery of b07dadf0b8482149119e2b16a37cb4c46b9134ec. Record scoped ACCEPTED in AGENTS/19/20/36/two READMEs and testing/acceptance-20260913.json. All59 accepted source hashes and2589 protected hashes match the26 passing gate evidence. This docs-only / light closeout follows23: commit/push feat/r3-test-acceptance, merge --no-ff into refreshed main and push, independently verify remote SHA. No branch deletion, source/SQL/dependency/history evidence changes, business PG, real tools or deployment. Then prepare a concrete M2c-4 release/observation proposal on a separate feature branch; implementation still needs confirmation of that new package. Formal POD-PFC-001/M2/5CAP26Unit next route remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier pending R3 acceptance and no-main-merge statements are historical and superseded for this accepted delivery.

- R3 LOCAL_VERIFIED: final run gates-final passes26/26 on unchanged59 source fingerprints. R3 model9, isolated PG18, browser9 and architecture4 checks pass. Actual source diff27new/31modified/1unchanged, seven documents and named new evidence only;2589 protected files unchanged. Five schemas/four file roots and owned gate processes independently read back zero. Current checkpoint36 and quality report own results. Complete authorized feature closeout commit/push and remote SHA readback; human visual/business acceptance remains PENDING. No main merge, business PG, real tools, deployment or formal ledger advancement. Preserve pre-existing PG13528/5432,API31152/3001,Vite37744/5173; no new retained helpers. Older proposal-pending instructions below are historical.

- R3 IMPLEMENTATION CONFIRMED: the user replied “同意” to proposal35 option A delivered as d4641b8. Execute D1-D7 and C1-C4, exact 59 source/test files, seven handoff documents including36, named new evidence roots, five exact disposable local PG schemas/files/limits/start-restore/cleanup and 26 gates. The normalized approved proposal SHA256 is 24816eae365c84026fb70e25941eb43f8bf904dacf38206830fcf1d3804d4b64. Branch feat/r3-test-acceptance starts from refreshed origin/main 2afce7a and carries the approved six proposal documents. Record progress in36 and authorization-20260913.json. Opening/closing feature commits and push are authorized under23; main merge, business PG, real AI/tools/CI, production/release, dependencies, ledger and historical evidence changes are not. No subagents. Preserve pre-existing processes; close only owned test processes. Formal POD-PFC-001/M2/5CAP26Unit next route remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. Earlier proposal-pending records below are historical and superseded by this confirmation.

- Current R3 proposal round after accepted R2: R2 e5dcd31 is scoped ACCEPTED; acceptance closeout138258f and main merge2afce7a683c08cf6c318f8668581539be5aa09ea are pushed and independently verified. The user’s “继续” now continues docs-only / light preparation on feat/r3-test-acceptance-plan from that main: only AGENTS,19,20,new [35](docs/planning/prototype-v3/35-R3测试验收与发布承接方案及范围确认-20260913.md),and the two READMEs. Proposal35 now specifies option A: early AC-derived test drafts, versioned manual results, defect regression, Owner-only product acceptance and read-only release inputs. Its D1-D7, exact 59 source/test files, seven implementation documents, new evidence root, five disposable PG targets and 26 gates remain PENDING USER CONFIRMATION; no source/SQL/dependency/config/ledger/history evidence edits or test/PG/service writes. Feature proposal opening/closing commits and push follow23; no proposal main merge. Candidate implementation remains unapproved until user confirms the concrete package. No subagents or persistent helpers. Formal POD-PFC-001/M2/5CAP26Unit next route stays POD-PFC-001/M2/R3a/artifact-collaboration-tdd. This paragraph supersedes earlier acceptance-closeout action instructions below.

- R2 acceptance closeout: the user said “验收通过，继续” after delivery of e5dcd3180bae6c50de5f3deed9b69b10f3a7fc82. This is scoped ACCEPTED for proposal33 plus the approved commands.js amendment. Follow protocol23 acceptance workflow: record acceptance in AGENTS/19/20/34/two READMEs and the named acceptance JSON, commit/push the feature, merge --no-ff to refreshed main and push; no branch deletion or deployment. Current source matches all 52 fingerprints of the 22/22 passing run. This docs-only closeout does not alter source, SQL, dependencies, existing reports, business PG or formal CAP/Unit status. After merging, prepare the next test/defect/product-acceptance handoff design on a separate proposal feature; new implementation still requires confirmation of the concrete package. The earlier pending R2 acceptance paragraphs below are historical.

- R2 is now explicitly CONFIRMED by the user: proposal [33](docs/planning/prototype-v3/33-R2关联产物与阶段承接方案及范围确认-20260913.md) option A, interactions and D1–D6, exact 52 source/test files (original 51 plus the explicitly approved commands.js amendment), seven handoff documents, named evidence roots, four exact disposable local PG schemas/files/limits/start-restore/cleanup and 22 gates. The approved proposal normalized SHA256 is d17d5c7383d3c4d51e342de265e855c630eda44f35a5894751683a2b72769bfe. Branch feat/r2-artifacts starts from refreshed origin/main 04fd81a28e16abe2d92ff87799a78750fbd497b4 after accepted M2c-3 merge/push. Execute C1–C4 continuously; checkpoint [34](docs/planning/prototype-v3/34-R2关联产物实施与验收-20260913.md) owns implementation and validation state. Feature opening/closing commits and push are authorized under23; R2 main merge is not. No arbitrary HTML execution, real AI/tools/CI, business PG enablement, dependencies, historical evidence/001–003 changes or formal ledger advancement. R2 precedes M2c-4; tests/acceptance/release/observation remain explicit later scope. Preserve original PG13528, close only this round’s processes. Formal POD-PFC-001/M2/5CAP26Unit next route remains POD-PFC-001/M2/R3a/artifact-collaboration-tdd. The prior scope paragraphs below are historical, superseded for this task. R2 is now LOCAL_VERIFIED under the explicitly approved commands.js addition (51 to 52). Latest full gate run gates-1789265262932 passes 22/22 with all 52 source fingerprints unchanged; supplemental syntax/format/static/config/delivery checks pass, 164 protected source fingerprints and 477 historical evidence files match baseline. Actual source diff is 18 new + 26 modified; eight allowed paths have no semantic diff. Four disposable schemas and three file roots are independently empty; all round-owned processes closed. Perform the authorized feature closeout commit/push and verify remote SHA; do not merge R2 into main without acceptance of the delivered commit. Checkpoint34 and the current quality report own results. Human acceptance=PENDING, business PG/formal CAP completion/release remain unadvanced. Pre-existing services remain.

- M2c-3 acceptance closeout: the user explicitly replied “同意” to accepting ce2c44c, merging/pushing main, and then implementing proposal 33 option A. Record scoped acceptance in AGENTS/19/20/32/two READMEs and the named acceptance JSON; commit/push the feature, merge --no-ff into refreshed main and push. No source changes in this closeout. R2 begins on a separate feature from that main with its confirmed 51-file/four-schema/22-gate package. The previous paragraph below is historical implementation authorization; its pending acceptance label is superseded by this explicit confirmation.

- The user replied “同意” to the concrete [31](docs/planning/prototype-v3/31-R1后续治理衔接方案与范围确认-20260913.md) proposal `42c0641`: implement option A, D1–D6 plus S1–S5, its 63 exact source/config/test files, seven handoff documents including [32](docs/planning/prototype-v3/32-M2c-3实施与验收-20260913.md), named report/evidence roots, and its three exact disposable PG schemas/files/temporary start-and-restore/cleanup package. Branch `feat/m2c-governance` starts from refreshed origin/main `c3f6c18` and carries the six proposal documents. Work C1–C4 continuously with 18 gates and G01–G17 evidence; scoped feature opening/closing commits and push follow 23. No business pfc_workbench enablement, real AI/tools/CI, main merge, deployment, new dependencies, historical evidence changes or formal ledger advancement. The Windows service is Stopped, but a pre-existing manual PostgreSQL process PID 13528 is running on loopback; preserve that original process. Close only this round's test processes. This confirmed system iteration is LOCAL_VERIFIED: final 18 gates, 15 governance integration checks and 10 governance browser checks passed. There are 61 changed source/config/test files inside the 63-file whitelist, seven handoff documents and the named synthetic evidence. All three disposable schemas and this round’s test processes were cleaned and independently read back. Post-delivery review of 3a2bd81 fixed the G16 plan rejection draft loss within the same approved package: governance-domain.js plus its browser regression, no API/SQL change; latest full gate run 1789252092676 passed 18/18 and isolated cleanup readback is zero. Checkpoint 32 is current; scoped human acceptance and main merge are pending, not implied by automated PASS.

### Historical authorization records

- Current continuation after main `c3f6c1869d1dad06c9b316ff61cbce359eba8358`: the user's “继续” continues preparation of the next reviewable design following accepted R1. This is docs-only / light, on `feat/m2c-governance-replan`, limited to AGENTS, 19, 20, new [31](docs/planning/prototype-v3/31-R1后续治理衔接方案与范围确认-20260913.md), the planning README and prototype README. Complete the R1-to-M2c-3 design, exact candidate scope, test/evidence compatibility and remaining domain route; then request confirmation of the concrete implementation package. Feature opening/closing commits and feature push follow 23. No source, SQL, dependency, business PG, real AI/tool, main merge, deployment, ledger or historical evidence changes in this proposal round. The candidate package in 31 is not yet implementation authorization. Formal POD next route stays `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`.

The current R3 proposal scope at the top takes precedence for this round; the records below preserve the completed R1 authorization and earlier decisions.

- R1 acceptance closeout: on 2026-09-13, after being asked explicitly whether R1 commit `2cf9a3fca3a5898c873a96062663d75359bc792b` was accepted and could be merged/pushed to main, the user replied “确认”. This accepts the scoped prototype interaction and disclosed synthetic/PG limitations, and authorizes seven handoff-document updates (AGENTS, 19, 20, 29, 30, two READMEs), the named acceptance JSON, feature commit/push, `--no-ff` merge of `feat/flow-prototype` into main and main push under 23. See [30](docs/planning/prototype-v3/30-连续协作原型实施与验收-20260912.md). The reviewed source and 30 entry-file fingerprints still match the prior 12 passing gates. This is docs-only acceptance closeout, not a new source iteration or deployment. R1 is ACCEPTED within its local prototype scope; formal CAP/Unit status and POD next route remain unchanged. No PG writes, real AI/tools, governance/domain implementation or branch deletion. After closeout, any new implementation requires its own confirmed design and exact scope.

- R1 approval: the user replied “同意” to the concrete R1 package in 29 section 9 (reviewed proposal `b0ff6eb`). Implement only its 14 exact source/test files, the seven handoff documents including [30](docs/planning/prototype-v3/30-连续协作原型实施与验收-20260912.md), and the named synthetic report/evidence directory. Branch `feat/flow-prototype` starts at verified `origin/main=a9eec73`; the six proposal documents are carried from `b0ff6eb`. The scope includes local-only rehearsal, isolated browser/temporary memory tests and cleanup, feature opening/closing commits and push under 23. No M2c-3 governance implementation, PG writes, real tools/AI/CI, main merge, deployment or formal ledger advancement. Current status: R1 prototype interaction LOCAL_VERIFIED (12 commands plus static/config/delivery checks PASS); visual and business acceptance remains pending. Source entry remains index.html, 26 scripts. The current handoff is 30; do not treat the historical proposal notes below as the active task.

- Current continuation after `c181af2`: finish the concrete continuous-collaboration interaction specification and a reviewable next prototype scope inside the same six Markdown files. Proposal 29 section 9 will describe the existing-workbench interaction, exceptions, validation and candidate R1 files. The user's “继续” continues this design work; it does not approve the new source whitelist, local rehearsal behavior, M2c-3 implementation or a main merge.

- Latest product steering after proposal 29: the user requires AI-native collaboration, early or concurrent business prototyping and PRD drafting, efficient completion within every stage, and seamless handoff between stages. Record the proposed continuous workspace, versioned context reuse, incremental artifact updates, decision handling and recovery criteria in 29. This is an amendment to the same six-document proposal scope, not approval of M2c-3 or new prototype/orchestration implementation. Resolve the interaction design and implementation scope before coding; do not silently defer these gaps or change the formal POD next route.

- Latest request: the user said “继续” after M2c-2 was accepted, merged and pushed to main as `a9eec73ed65610f09a80f11331edbda1b4252a67`. The current round is docs-only M2c-3 governance/projects proposal preparation on `feat/m2c-governance-plan`, tracked in [29](docs/planning/prototype-v3/29-M2c-3治理与项目方案及范围确认-20260912.md). It may edit only AGENTS, 19, 20, 29 and the two handoff READMEs; opening/closing feature commits and feature push follow 23. Candidate contracts, migrations, role policy and test targets in 29 require user confirmation before implementation. Do not activate business PG, edit source/SQL/dependencies, import old data, execute real tools, merge main, deploy or change the formal POD ledger in this proposal round.

- Prior M2c-2 approval: after receiving the concrete M2c-2 delivery `11d1fdf1ed57a3fb6fdaf965b626eb9dc4fd2eff` and the explicit question about merging and pushing main, the user replied “同意” on 2026-09-12. This accepts the scoped local prototype delivery under [28](docs/planning/prototype-v3/28-M2c-2实施与验收-20260912.md), including its disclosed limitations, and authorizes acceptance documentation, feature commit/push, a reviewed `--no-ff` merge of `feat/m2c-domain` into main, and main push under 23. The closeout is complete in `a9eec73`. Standard business integration and formal CAP/Unit acceptance remain unchanged. Applying the future Express-only `pfc_workbench` schema, real tools, old data import, deployment, branch deletion and M2c-3 implementation were outside that approval.

- Historical proposal checkpoint after M2c-1 merge `05af5de`: the user asked “继续” after being told M2c-2 requires solution confirmation. The active round is docs-only preparation on `feat/m2c-domain-plan`, documented in [27 - M2c-2 proposal](docs/planning/prototype-v3/27-M2c-2接入方案与范围确认-20260912.md). This round may edit only AGENTS, 19, 20, 27, the planning README and prototype README; no business source, dependency or database changes. Proposal 27 includes an explicitly unconfirmed `pfc_workbench` schema exception, API additions and test limits. It does not activate that exception or authorize implementation, business schema application, old data import, deployment or main merge. The existing `pfc` policy remains in force until the proposed exception is explicitly approved.

- The current user-confirmed development path is the original HTML/JavaScript prototype plus the Express server. Start with [19 - Roadmap](docs/planning/prototype-v3/19-开发路线图-20260912.md), [20 - Task template](docs/planning/prototype-v3/20-Codex交接提示词-20260912.md), and [21 - Implementation checkpoint](docs/planning/prototype-v3/21-M2b-2前端切领域API-20260912.md). Read all three: roadmap completion labels do not supersede the checkpoint's incomplete implementation, verification, or scope decisions. Unresolved differences must be aligned with the user before expanding implementation.
- Active frontend: `output/pfc-workbench-prototype/index.html` and `original/`, plain HTML/CSS/JavaScript without a framework or build-system migration. Active backend: `server/`, Node 24 + Express 4 + ws + jsonwebtoken + pg. Use the existing dependencies and pinned `.tools/node-v24.20.0-win-x64` runtime.
- The React/Vite, Fastify/SSE, Kysely, `packages/ui`, workspace commands, and module rules below continue to govern the retained `apps/`, `packages/`, and their `scripts/` and `tests/`. These are prior engineering assets, not the default implementation path for this handoff. Do not retire, migrate, delete, or resume that implementation based solely on prototype work. Shared root dependencies remain necessary for prototype ESLint and Playwright checks.
- M2b-2 local protocol/UI work under 24 was accepted by the user on 2026-09-12 (reviewed commit `6dedc41`). The current prototype scope is M2c-1 under roadmap 19 and [25 - M2c proposal](docs/planning/prototype-v3/25-M2c方案与范围确认-20260912.md). M2b-2 was merged and pushed to main as `12d1733`. The `feat/m2c-plan` round was docs-only. The user subsequently confirmed the M2c-1 paths and isolated PG test limits in 25; implementation `af709eb` on `feat/m2c-storage` is LOCAL_VERIFIED and its scoped delivery was accepted by the user under 26 on 2026-09-12. Business API/WS persistence has not switched to these new components. The unresolved scope in 21 records the historical checkpoint; the later user approval in 24 covers verifying and completing that existing draft. Approval to organize files under [22 - Cleanup scope](docs/planning/prototype-v3/22-主线收口与历史代码整理建议-20260912.md) alone never authorized API expansion. M2c and the old POD ledger's next route are distinct records; do not silently update either scope or status.
- Preserve the original desktop interaction. For this standalone frontend, inspect its `original/` styles and the approved original-prototype reference, then use the existing browser-aware static and real PC browser verification. Do not import `@pfc/ui` or a framework merely to match the older workspace rules.
- The handoff's six verification entry points are `output/pfc-workbench-prototype/verify-prototype.mjs`, `verify-m1-layer.mjs`, `verify-m1-e2e.mjs`, and `server/verify-server.mjs`, `verify-server-m2.mjs`, `verify-m2b.mjs`. The two M2b-2 focused checks are `output/pfc-workbench-prototype/verify-m2b2-model.mjs` and `server/verify-m2b2-domain.mjs`. Run these with the pinned Node runtime. Passing existing regression scripts does not establish the new UI flow's acceptance or replace root Quick/Core/Full for changes to the older workspace.
- The nine legacy handoff gates use synthetic browser contexts and loopback memory servers with `PFC_DB=memory` and an empty `DATABASE_URL`. M2c-2 implements explicit PG runtime and domain/file persistence under 27/28, adds old PG, architecture, domain and PG browser gates (13 total), and never runs automatic DDL or falls back to memory. Only the exact approved disposable targets may be written this round. Future business `pfc_workbench` and `.local/pfc-workbench-files` are prepared configuration targets, not activated data. Sim-bridge and conversations remain synthetic. Browser code must not execute Shell, Git, Codex, Zed, or database commands directly.
- `archive/prototype-v3-reference-20260912/` contains the old V3 reference only. Read its README and manifest for original paths; its historical scripts are not current gates. Preserve all `output/pfc-workbench-prototype/_backup/`, `output/playwright/`, other old verification scripts/images, and `output/pfc-design-overview.html` in place. Only the 12 named files in 22 are authorized for this archive move.
- All existing authorization, lifecycle, secrets, local binding, test-data, external-effects, release, and process-cleanup requirements still apply. Git is now authoritative for version recovery: initial baseline `69ec9eb`, with the current remote head checked at each task start. Read [23 - Development protocol](docs/planning/prototype-v3/23-Codex防走偏协议-20260912.md); use scoped feature branches, explicit file staging, opening/closing commits, and user review before merging main. Historical manifests and backups remain preserved.
- The user approved M2b-2 acceptance closeout on 2026-09-12. Its scope, file whitelist, verification and review status are recorded in [24 - Acceptance closeout](docs/planning/prototype-v3/24-M2b-2验收收尾-20260912.md). Exercise the existing 21 draft's creation/version/stage/run flow and fix reproduced defects within that scope; align any new contract semantics first. The subsequent user message “评审通过，继续” authorizes the reviewed M2b-2 no-ff merge and main push under 23, plus preparation of the next M2c scope. New M2c design choices, database schema application, and real workspace execution are not implied by accepting M2b-2.

- The user subsequently replied “同意” to proposal 25, authorizing M2c-1 only: the exact listed storage/test/document files, one disposable `codex_test_m2c_<runId>` schema in loopback `pfc_local` (at most 200 synthetic rows), its cleanup/readback, and temporary PostgreSQL start/restore if needed. Current checkpoint: [26 - M2c-1](docs/planning/prototype-v3/26-M2c-1存储基础实施与验收-20260912.md). Existing business routes/frontend/SQL and `pfc`/`public` remain excluded. The earlier auto-review rejection of main merge/push is historical: after receiving the concrete `af709eb` delivery and the explicit question about merging and pushing main, the user replied “同意” on 2026-09-12. This authorizes the reviewed M2c-1 no-ff merge and main push, with acceptance records; it does not authorize M2c-2 implementation, application database migration, deployment, or branch deletion.

## Purpose And Lifecycle

- Build the PC Web product-requirement lifecycle platform defined by the approved `product-full-chain` methodology.
- Every system iteration follows requirements -> design -> development -> testing -> acceptance -> release -> observation -> retrospective.
- The current bootstrap establishes tooling and empty application boundaries only. It does not authorize CAP business behavior.
- Do not implement, commit, push, merge, publish, deploy, or write to an external environment without explicit scope-specific authorization.

## Runtime And Stack

- Node.js: project-local Node 24.x; npm 11.x; use the pinned runtime under `.tools/` for all project commands.
- Monorepo: npm workspaces.
- PC Web: React 19 + Vite 8. Mobile layouts and mobile acceptance are out of scope.
- API: Fastify 5 modular monolith, bound to loopback by default.
- Persistence: PostgreSQL 18 + Kysely with explicit SQL migrations.
- Browser updates: HTTP plus SSE. Browser code must never invoke Shell, Git, Codex, Zed, or the database directly.

## Module Boundaries

- `apps/web`: PC SPA, routes, interaction, and SSE client.
- `apps/server`: HTTP/SSE boundary, authorization entry, configuration, and module assembly.
- `packages/domain`: product lifecycle objects, state machines, invariants, and domain errors.
- `packages/contracts`: versioned DTOs, error codes, SSE events, and cross-CAP ports.
- `packages/persistence`: repositories, migrations, transactions, and outbox support.
- `packages/test-data`: deterministic synthetic fixtures and factories only.
- `packages/ui`: shared desktop UI primitives and design tokens; never authoritative business state.
- Modules do not edit another module's tables directly. Cross-module behavior goes through application services or domain events.
- Independently routed frontend pages belong in separate feature directories. `App.tsx` composes routes and cross-page state; shared feature UI, hooks, and formatters live in an explicit feature-owned shared module instead of accumulating multiple pages in one large file.
- Backend features keep transport routes, application services, domain policy, and repository ports in separate modules. A file has one primary responsibility; do not combine multiple independently changeable pages, API boundaries, or persistence concerns merely to reduce file count.
- When an existing oversized file is touched for a system iteration, include the smallest safe responsibility split in the design and protect the resulting boundary with an architecture test.

## POD Delivery Governance

- `.quality-gate/delivery-plan.json` is the implementation repository's machine-readable progress ledger for the confirmed `POD-PFC-001` scope. It must retain all 5 CAPs, all 26 Units, M1-M3 membership, current status, real dependency source, and exactly one POD-level next route while delivery is incomplete.
- The adjacent methodology repository remains authoritative for product scope and confirmed behavior. Update the local ledger only from a confirmed authority version; never use the ledger to invent, remove, or silently defer a CAP or Unit.
- Every iteration design, checkpoint, gate report, and completion handoff must state parent POD, milestone, CAP/Unit scope, implementation status, integration status, acceptance status, deferred scope, and POD-level next route. A final Txx or CAP task returns control to the parent POD instead of implying that the platform is complete.
- M1 cannot complete from `CAP-PFC-01` alone. It also requires `UNIT-PFC-02-01` and `UNIT-PFC-04-01` through `UNIT-PFC-04-03` with standard local PostgreSQL persistence and real local integration evidence. Equivalent M2 and M3 scope is enforced by the delivery ledger.
- `LOCAL_VERIFIED`, `INTEGRATED`, `ACCEPTED`, and `RELEASED` are distinct conclusions. Fixture, mock, unavailable adapter, generated report, dry-run, or isolated test result cannot be counted as real integration, product acceptance, release, or external effect.
- UI redesign work may address confirmed usability defects but must not silently replace the current POD next route. Before starting another UI-only iteration, record why it blocks the active milestone; otherwise continue the active CAP/Unit plan.
- Run `npm run check:delivery-governance` through every quick/core/full gate. A mismatch between the confirmed POD scope, milestone claim, persistence policy, dependency source, or next route fails closed.

## Runtime Data Policy

- Standard local product operation persists business facts in PostgreSQL database `pfc_local`, schema `pfc`, through reviewed migrations and repositories. In-memory state and `pfc_experience` data are not product facts and cannot satisfy CAP, milestone, acceptance, or release evidence.
- Fixtures and deterministic factories are test infrastructure only. They may write only to disposable `codex_test_*` schemas or remain process-local, must use the `CODEx_TEST_` prefix, and must be cleaned and read back after tests.
- `PFC_EXPERIENCE_MODE` and `pfc_experience` are legacy visual-evaluation paths pending retirement before M1 exit. They remain disabled by default, must be visibly identified when temporarily used, and cannot be used for business acceptance.
- New user-visible workflows that require persistence must include a PostgreSQL migration/repository/API readback plan in their confirmed design. Do not substitute seeded demonstration records because a persistent model has not yet been designed.

## UI Design System

- `packages/ui` is the only source of visual tokens, shared controls, and cross-page interaction patterns. Inspect the local `/ui-kit` catalog before creating or changing a page.
- Business pages in `apps/web` compose `@pfc/ui` components and may own only domain layout, data binding, and business-specific state mapping.
- New visual values or interaction states must first be added to the three token layers and the UI catalog with tests. Do not add page-local colors, font families, font sizes, line heights, radii, shadows, or motion durations.
- Run `npm run check:ui-design`; bypassing or allowlisting a failure requires an explicitly confirmed design-system change, not a page-local exception.
- Preserve the dense PC operational model. Do not turn tables into marketing cards, introduce decorative hero content, or add external fonts/assets without a separately confirmed design.
- Visual quality must not be traded for implementation speed. A technically complete page is not complete when its visual hierarchy, interaction feedback, or reference fidelity is still unverified.
- Reference-led UI work must complete a durable reference study before implementation. Sample all relevant page types and states available in the reference, including navigation, list or workbench, detail, data-dense content, empty/loading/error where available, keyboard focus, hover, filtering, and URL/state preservation.
- The reference study must extract evidence-backed primitive, semantic, and component tokens; typography roles; layout grid; surface hierarchy; icon language; motion; interaction states; page templates; and explicit adaptations for this platform. A few generic similarities such as “blue, rounded, centered” are insufficient.
- Record the reference study under `docs/design-system/`, then obtain explicit user confirmation of the platform visual baseline before changing business pages. Implement one real representative page first and compare side-by-side at 1280, 1440, and 1920 widths; do not migrate other pages until that sample is visually accepted.
- Browser screenshots, computed styles, interaction evidence, and the local `/ui-kit` are required visual evidence. Automated tests and quality gates do not substitute for human visual acceptance, and no completion claim may conflate the two.
- If required design, browser, font, icon, or inspection tooling is unavailable or incomplete, stop the affected phase. Enable or install the smallest suitable tool only after dependency and supply-chain review plus any required authorization; never silently downgrade to a lower-fidelity workflow.
- Dense operational design means compact, scannable information with clear hierarchy. It does not mean flat styling, undifferentiated rows, one-note color, missing overview context, or the absence of purposeful cards and visual emphasis.

## Commands And Quality Gates

Activate the project runtime in PowerShell before running npm commands:

```powershell
$env:Path="$PWD\.tools\node-v24.20.0-win-x64;$env:Path"
```

- Quick: `npm run test:gate:quick`
- Core: `npm run test:gate:core`
- Full: `npm run test:gate:full`
- Gate planning: append `-- --dry-run`.
- Quick covers lint, formatting, type checking, configuration checks, and unit tests.
- Core adds isolated migration/repository integration tests, production builds, and local PostgreSQL connectivity.
- Full runs core plus real PC browser, permission, concurrency, recovery, security, local performance-smoke, and dependency-audit checks, and fails closed on any failed step. A dry-run is not a PASS.
- Use TDD for behavior changes when practical: confirm the expected failure, implement the smallest change, then rerun proportional gates.

## Test Data And Evidence

- Use only synthetic local fixtures during development. Test identifiers use the `CODEx_TEST_` prefix plus a runId.
- Before data-dependent tests, record scenario, business validity, boundaries, permission/state combinations, assertions, isolation, created IDs, cleanup, and sensitive-data handling.
- Create a deterministic factory when setup exceeds three steps or when scenarios cover CRUD, state, permissions, versions, boundaries, concurrency, or negative paths.
- Store durable, redacted gate reports under `docs/quality-gate/reports/`. Do not treat generated console output as business acceptance.

## Database And Migrations

- Local PostgreSQL is `pfc-postgresql-18`, manually started, loopback-only, database `pfc_local`, application role `pfc_app_local`.
- The Windows runtime uses the current-user junction `%LOCALAPPDATA%\PFCPlatform` because PostgreSQL initialization cannot safely consume the Chinese physical path. All binaries and data remain physically inside this repository.
- Schema changes happen only through reviewed migration files. Application startup must not silently mutate schema.
- Validate every migration against an isolated local database. Production DDL/DML and real-environment changes require separate authorization and review.
- `npm run db:migrate` applies pending migrations to the configured local database; do not run it against the default application schema without a scope-specific local schema-change authorization.
- Never print database passwords or connection URLs in test output, screenshots, logs, or reports.

## Secrets And Generated Artifacts

- Local configuration comes from ignored `.env.local`; CI and deployment values come from their secret providers. Only key names and safe examples belong in `.env.example`.
- Required runtime keys are validated at startup. Fixture adapters are allowed only when `APP_ENV` is `local` or `test`.
- Never commit credentials, tokens, cookies, customer data, prompts containing restricted material, private URLs, or real operational logs.
- Do not edit generated bundles, coverage, lockfile internals, database data, or vendored artifacts manually. Change source and run the owning command.

## External Effects And Release

- Local services bind to `127.0.0.1` unless a separately approved design says otherwise. Do not add firewall rules.
- No SIT, staging, production, customer system, remote database, Git remote, Codex/Zed bridge, or external publication is available by default.
- Release requires an approved artifact source, environment and owner, rollback, smoke/readiness checks, observation window, monitoring, and explicit authorization.
- Stop and preserve evidence on credential exposure, permission bypass, duplicate lifecycle advancement, stale-baseline corruption, unexplained dependency scripts, or resource errors.

## Ownership And Process Hygiene

- 陈立 is the product owner, development/engineering owner, code-review owner, testing owner, and security owner for `POD-PFC-001`.
- The product owner has explicitly accepted same-person ownership for the local development lifecycle. Missing or independent personnel must not block delivery; each role conclusion still requires its own evidence and must not be inferred from another role's result.
- Parallel work requires non-overlapping ownership, stable interfaces, independent verification, and an integration order.
- Close short-lived dev servers, browsers, watchers, and test runners before handoff. Report any retained process with PID, port, purpose, and stop command.
