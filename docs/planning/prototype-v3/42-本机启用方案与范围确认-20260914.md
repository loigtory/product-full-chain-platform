> 2026-09-14执行结果：用户明确要求“初始化空业务库、完成首次备份及隔离恢复检查、启动5188供你使用”。本轮E1–E4及必要E6已完成运行核验，LOCAL_ENABLED / READINESS_VERIFIED；空业务库、首备及隔离恢复PASS，5188保留运行。E5工作日观察和第二备份不在本轮范围。下文保留原方案评审时事实及限制，当前结果以[43号](43-本机启用与观察记录-20260914.md)为准。

# 42 · 本机启用方案与范围确认

> DESIGN_READY_FOR_REVIEW。本地可用基线e3f5d45已由陈立回复“已验收，继续”。验收收尾ec23475和main合并a20e16e均已推送、独立核对远端SHA。本文是待确认的首次本机启用包，业务初始化、启动和观察尚未执行。

## 本轮范围与计划

分类docs-only / light。分支feat/local-activation-plan从最新origin/main a20e16e53fa0d5661bd81f830749864a31dba3fd创建。写入仅限AGENTS、19、20、本42、两份交接README，以及新证据根docs/quality-gate/reports/local-activation-20260914下脱敏JSON/Markdown。先核对验收与Git，再只读核对目标元数据、配置键、ACL和进程，形成具体影响、验证、回退与观察方案，最后完成文档复审和feature提交推送。

固定Node24.20.0/npm11.19.0，根及server现有依赖离线核验通过，无缺包，不执行安装。新分支Quick dry-run为READY_TO_RUN，仅为门禁计划。本轮无新服务、浏览器、子代理、测试数据或业务写入；网络仅23号Git收尾和方案feature提交推送。源码、SQL、依赖、历史证据、正式台账均不改。本42暂不合并main。

父POD-PFC-001 / 正式M2，5 CAP/26 Unit保持。直接支持UNIT-PFC-01-01/02/03/05、UNIT-PFC-02-01至04、UNIT-PFC-04-01/02；UNIT-PFC-03-02/05及UNIT-PFC-05-01至05仅使用已有记录与恢复能力，其余Unit保持。实现LOCAL_VERIFIED、隔离集成ISOLATED_LOCAL_PG_VERIFIED、本轮交付SCOPED_ACCEPTED；业务启用NOT_AUTHORIZED，观察NOT_EXECUTED。正式唯一next route仍为POD-PFC-001/M2/R3a/artifact-collaboration-tdd。

## 当前检查点

源文件68项及保护文件3009项与最终35闸证据一致。合并检出产生61项换行转换，先证实Git内容树与验收分支相同，再按原SHA256精确恢复；3份混合换行文件也恢复原值，索引内容未变。既有原授权和最终证据保持原指纹，不重新运行数据闸。

只读CLI preflight返回INITIALIZATION_REQUIRED，业务schema不存在、原件根不存在、5188空闲。三个人目录均不存在；父.local ACL含其他继承主体，因此不能将父目录权限称为私密权限。实际新目录ACL须在批准初始化后独立确认；现在不创建探针目录，也不改父ACL。具体元数据与待确认包见下文，未提前执行目标ACL或业务验证。

## 一、启用目标与方案选择

让陈立从固定入口持续使用已验收工作台，需求、材料原件和关联产物保存在自己的本机空间。继续采用40号已确认的单人本机A方案，本42只确认实际运行操作及其影响，不再新增产品规则或开发另一套界面。

推荐先初始化空工作空间，在导入个人材料前完成一次空基线冷备和独立恢复检查，再交给本人使用。这样首次交付就有实际目标的恢复依据，代价是启用前多一次短时恢复检查。仅启动而不核验备份的方式不作为本次推荐路径；当前没有业务备份，不能提前声称具备实际业务恢复保障。

入口为http://127.0.0.1:5188/，一个Owner“陈立”、一个“陈立的工作空间”。初始化不创建需求、消息、材料、项目或产物，不导入演示记录。真实AI对话、材料语义理解、原型/PRD/AC模型生成、Shell/IDE Bridge、CI与实际发布仍为后续设计；当前页面继续显示模拟来源。

## 二、实际基线与只读证据

| 项目       | 2026-09-14本轮实测                                                                                            | 启用时的要求                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 源码       | e3f5d455d274cb95af4493138031e86da0236953已验收；main a20e16e53fa0d5661bd81f830749864a31dba3fd包含相同产品源码 | 固定这份已验收源码，68项原SHA与历史35闸一致；后续仅文档提交可不同，记录实际HEAD  |
| 已执行门禁 | 最终35/35 PASS、621763ms；本次指纹复核相同                                                                    | 引用原运行，不把本轮文档检查说成重新执行35闸                                     |
| 数据库     | PG18.6，127.0.0.1:5432 / pfc_local / pfc_app_local；READ ONLY；具有建schema权限                               | 首次只创建pfc_workbench；不创建5432数据库、角色，不改pfc/pfc_experience          |
| 目标       | pfc_workbench及七个旧测试schema均不存在；三个人目录均不存在                                                   | 初始化前再次核对，已有非本次对象即停止；恢复/接管已有数据需另审                  |
| 旧结构     | pfc/pfc_experience关系及列目录843项已取摘要                                                                   | 操作后相同口径独立比对；本摘要不冒充旧业务数据全量快照                           |
| 本地配置   | ignored .env.local有DATABASE_URL，其余个人profile/users/模式/schema/原件/JWT/端口键未设置                     | 启动器从私密profile注入参数，不改旧.env.local，不输出值                          |
| ACL        | 当前用户SID可解析、pwsh7/icacls可用；父.local有其他继承主体；新目标未创建                                     | 初始化仅收紧新目录到当前用户/SYSTEM/Administrators，并独立读回；未通过不开放入口 |
| 资源       | 磁盘可用504GiB；5188/5198/5548无监听；既有junction指向本仓库                                                  | 启用前最低3GiB；不占用已有进程，不新增junction或防火墙规则                       |
| 进程       | 原PG13528/5432、API31152/3001、Vite37744/5173，均loopback                                                     | 保留原服务；只管理本包明确创建的进程                                             |
| 备份       | 业务备份目录不存在，现有业务备份为零                                                                          | 首次空基线备份和独立恢复通过后才交付本人使用                                     |

数据库/路径/迁移文件指纹见[本轮只读预检](../../quality-gate/reports/local-activation-20260914/preflight-proposal.json)，ACL/进程见[元数据证据](../../quality-gate/reports/local-activation-20260914/acl-process-proposal.json)。业务验收来自[41号追加记录](41-本地可用基线实施与验收-20260913.md)与[验收回执](../../quality-gate/reports/local-use-baseline-20260913/acceptance-20260914.json)，35闸及既有风险见[质量报告](../../quality-gate/reports/local-use-baseline-20260913.md)，Git收尾见[远端读回记录](../../quality-gate/reports/local-activation-20260914/git-closeout.json)。这些元数据在实际启用前都要刷新，不能沿用当前空目标假设执行。

## 三、提交确认的操作范围 E1–E6

确认后按system iteration / strict的运行启用流程执行，沿用40号已实现的initialize/lifecycle/backup/restore-check，不修改源码、SQL、依赖或正式台账。授权仅覆盖本次首次启用及其后一个本人工作日观察收尾；停止指令、来源漂移、目标变化或任一闸失败即暂停后续写入。

### E1：启用前复核与记录

再次fetch并核对main、当前分支、实际HEAD、68源码和保护文件、001–006文件及迁移器LF规范化校验和。源码必须与已验收e3f5d45相同；远程新增产品改动、工作区他人改动、目标已被初始化或PG已停止时，先说明事实，不静默接管、同步新产品行为或自启原PG。

复核5432身份/权限、三目录空目标、5188/5198/5548空闲、磁盘≥3GiB和ACL工具。记录启用runId、操作前元数据、原进程及零业务数据事实。43号记录本次批准文本、方案commit/指纹和每步退出码；操作脚本及截图不记录密钥、Cookie、连接串或业务正文。

### E2：只初始化一个空空间

执行既有CLI initialize，仅在127.0.0.1:5432的pfc_local中以pfc_app_local创建pfc_workbench、ownership marker，运行既有001–006。影响为57张SQL声明表加schema_migrations共58张表及迁移声明的索引、序列、函数、触发器；不扩展已批准SQL。记录6条迁移、1条tenant和1条Owner，预期所有其他业务表为空。

仅创建.local/pfc-workbench和.local/pfc-workbench-files。私密配置包括profile.json、users.json和access-key.txt；随机密钥只落访问文件，服务端使用盐/scrypt摘要。初始化阶段为PREPARED→SCHEMA_CREATED→MIGRATED→OWNER_CREATED→READY，记录attemptId/ownedOid/marker。中断后仅在本次attempt、OID、marker与新建目录确权一致时允许一次同命令续接；不能删库重建或覆盖外来文件，身份不符立即停止。

独立只读检查58表、6条迁移的实际校验和、唯一tenant/Owner、零需求/消息/材料/产物以及原件为空；不得只相信initialize返回的数量。使用Get-Acl独立读回新目录、profile/users/key权限；密钥长度与存在性可在本机进程内验证，但不能输出内容。ACL不符合当前用户/SYSTEM/Administrators范围时，保持服务关闭并保留现场，不修改父.local权限。

### E3：首个空基线备份与恢复检查

确认服务STOPPED、目标空读回通过后，执行backup，创建.local/pfc-workbench-backups下一个登记完成包，包含PG和空原件清单；核对包状态、58表摘要、文件引用、manifest和源前后相等。首次备份完成前不导入个人需求/材料，备份目录同样独立核对ACL。预计一个完成包，失败只保留partial及安全错误类别，不覆盖任何历史包。

仅对此次空包运行一次restore-check --backup-id实际UUID。临时PG固定127.0.0.1:5548、临时API5198，临时库/角色及pfc_workbench都属于独占新cluster，不是在5432再创建库或角色。根为.local/local-use-baseline-20260913/restore/CODEx_TEST_M2C_20260913_localuse_加工具生成UUID；先验证该绝对路径在精确restore父目录内、无链接且不存在，保存ownership。该恢复副本仅含此次空初始化的空间身份和元数据，不能称为纯合成fixture；不含个人需求或材料正文。

恢复临时文件总量最多1GiB、执行预算120秒；仅串行一组临时PG/API。检查恢复后的全部表/结构/序列与空包相等、原件零项，新会话能读取Owner/空列表，未持有会话的请求被拒绝；来源5432无写。先正常停临时API/PG，再按本次ownership/PID/路径清理并独立读回5548/5198空闲、runId根不存在和源前后相等。失败保留脱敏诊断；归属无法确定时不强行删除。禁止把临时恢复切回或覆盖个人来源库。

### E4：启动及独立就绪检查

前序通过后执行start，隐藏启动控制进程窗口，服务只监听127.0.0.1:5188；记录控制进程和服务实际PID、可执行文件、开始时间和实例ID。start可能执行已有作业/会话恢复写，纳入本次操作权限；空基线不应出现业务恢复记录。启动器20秒未就绪或身份/版本异常即停止本次实例，不反复盲重启。

分别读回CLI status、/api/health、登录后/api/auth/me、空需求列表和/api/local-status，确认PG/006、Owner和工作空间、实际commit、产品源码无修改、原件可读/零占用、最近完成备份。首次HTTP会话仅用于本机只读验收，不自动新建需求、上传材料或推进阶段。密钥/Cookie仅在本机进程内使用，输出仅保留状态/断言；浏览器不截图输入中的密钥，关闭测试context并撤销其会话。

本人入口显示三栏工作台空态、只限本机、模拟来源及本地状态。自动smoke使用既有Edge/Playwright独立context，检查1440宽度、键盘登录/退出和无console错误；三宽度全量证据继续引用已验收35闸。如新环境行为不同，停止交付并报告实测差异，不在此运行包内改代码。

### E5：本人使用与一个工作日观察

就绪通过后向陈立交付入口，保留5188控制/服务进程供本人使用；这是本包请求的明确保留项。首次密钥由本人在本机私密文件中查看并填写，禁止发到聊天。本人可使用已有新建需求、材料、产物/版本和阶段记录功能；Codex不代填真实材料，不把模拟模型/作业/发布当真实执行。

观察从本人实际开始使用起，至少跨一个本人工作日，记录真实开始/结束时刻；不把自然时间经过、夜间空闲或自动脚本通过当观察完成。节点为交付、首次本人使用后、工作日收尾。检查服务健康/身份、保存刷新后读回、原件可读/容量、最近备份、错误类别及是否仍为模拟。本人反馈是验收输入；未发生的业务场景明确NOT_OBSERVED，不造样本。陈立负责试用和确认，Codex只在本次任务及回来收尾时检查，不创建定时器或后台监控任务。

观察收尾时先确认本人无未提交输入，再停止本次5188实例，创建第二个冷备并读回已登记完成；首包与收尾包均保留，不自动删包。本包最多两个完成包，沿用系统总上限7包/1GiB、原件10MiB/项与100MiB总量、并发上传2、日志5MiB×3。若本人已保存材料，备份只留本机私密目录；不上传Git、不做第二次实际数据恢复演练。本轮观察期间其他人工修改/删除或额外备份由本人操作，不扩大Codex权限。

收尾可在确认无异常后再次启动同一实例配置并保留供本人继续使用，列实际PID和关闭命令。存在丢失/串用、原件摘要不符、越权、来源不明、备份失败、磁盘不足或UNKNOWN恢复异常时立即停止写入和试用，保留记录；不升级为业务启用验收通过。

### E6：文档、Git和退出

执行分支为feat/local-activation，从执行时刷新后的main创建；带入本42已批准文档，不带入新产品代码。可写文档精确为AGENTS、19、20、42、新43-本机启用与观察记录-20260914.md、两份README，以及本轮证据根下脱敏JSON/Markdown；运维文件仅上述三个人目录和E3独占恢复目录。原41/35闸/验收回执不改，旧工程与正式台账不改。

按23号开工/收工提交推送feature，记录实际远端SHA；业务材料、PG数据、密钥、Cookie、连接串、原日志和备份均不得提交。未完成本人工作日观察时，43保持LOCAL_ENABLED / OBSERVING，不能将运行启用、全部产品能力或平台生产就绪写成ACCEPTED/RELEASED。本操作轮main合并留待启用与观察结果评审；不授权远程环境、模型调用、发布/部署或分支删除。

## 四、可执行命令与验收闸

下列命令均从仓库根使用固定Node逐条执行，每条检查退出码；除了preflight，其余目前都未获业务目标执行批准。

```powershell
& '.\.tools\node-v24.20.0-win-x64\node.exe' server/scripts/local-workbench.mjs preflight
& '.\.tools\node-v24.20.0-win-x64\node.exe' server/scripts/local-workbench.mjs initialize
& '.\.tools\node-v24.20.0-win-x64\node.exe' server/scripts/local-workbench.mjs backup
# 使用上一条实际返回的完成包UUID，不能复制占位文本执行。
& '.\.tools\node-v24.20.0-win-x64\node.exe' server/scripts/local-workbench.mjs restore-check --backup-id '<实际完成包UUID>'
& '.\.tools\node-v24.20.0-win-x64\node.exe' server/scripts/local-workbench.mjs start
```

start为前台控制命令；交付本人时用Start-Process -WindowStyle Hidden启动同一固定Node/脚本，仅记录PID和无秘密启动摘要，不另写启动服务或开机自启配置。另一终端可检查/停止，正常stop应同时让控制进程退出：

```powershell
& '.\.tools\node-v24.20.0-win-x64\node.exe' server/scripts/local-workbench.mjs status
& '.\.tools\node-v24.20.0-win-x64\node.exe' server/scripts/local-workbench.mjs stop
```

| 闸   | 场景、前提及执行                             | 可观察通过条件                                                             | 数据、自动化与验收归属                                  |
| ---- | -------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------- |
| E-G1 | 启用前执行E1 preflight和只读目录/PG检查      | 空目标、来源一致、精确身份/端口/权限/磁盘符合                              | 无新数据；CLI已实现，Codex独立读回、陈立确认            |
| E-G2 | initialize后以独立PG只读事务、文件/ACL检查E2 | 006六校验和、58表、唯一空间/Owner，其他表零业务记录，ACL私密               | 首次运行初始化数据，非fixture；Codex执行、陈立接受      |
| E-G3 | STOPPED时首个空包backup，随后一次E3恢复      | COMPLETE、恢复相等、来源不写、临时PID/端口/文件清理读回通过                | 仅空基线复制，无个人材料；既有ops能力，Codex执行        |
| E-G4 | E-G1–3通过后启动E4；分别HTTP/API/浏览器读回  | health/status PG006与实例相符，登录Owner/空界面/最近备份正确，测试会话退出 | 只建内存会话，不建业务用例；Codex执行、本人入口确认     |
| E-G5 | 本人实际试用覆盖一个工作日，按E5收尾         | 保存/原件/错误记录有实际证据，第二冷备完成，陈立明确观察结论               | 本人自有数据留本机；人工验收，不自动代签                |
| E-G6 | 每阶段检查PID/路径归属、证据及精确Git差异    | 无越界修改，恢复临时根清完，保留服务有PID/端口/停法，隐私数据未入库        | 脱敏元数据可提交，敏感内容不得提交；Codex留痕、陈立评审 |

本轮没有新增行为测试或业务CRUD测试数据。后续E2/E3使用首次空初始化事实，预期值已在上文定义，不能额外seed演示数据；如需新CRUD/负例工厂、故障注入或修改测试程序，另提精确实现包。既有100需求/200附件恢复结果只证明隔离负载测试，不证明本次空业务库含有这些记录。

## 五、异常、回退与残余风险

首次初始化之前没有业务数据可备份，因此当前可恢复依据为已验收Git源码、空目标证据及初始化attempt/OID。初始化中断时保留现场，只允许E2所述确权续接；不能为了回到“空”而自动DROP schema或删除私密目录。

空包形成后，回退优先停止本次服务、保留schema/原件/配置与已完成备份，并把入口状态说明为未启用或已暂停。stop须通过既有实例身份核验，不按5188端口杀未知进程。停止失败或PID身份异常时保留证据和未退出PID，交付明确阻塞；仅对E3已确权临时进程/目录按工具清理。实际业务覆盖恢复、回切、DROP、重置密钥、清空用户数据均不在本包内，事故时另审精确目标。

备份在同一磁盘，整盘损坏仍会丢失数据；团队身份/LAN/TLS、长期负载/断电、模型费用和真实外部执行仍未覆盖。观察只接受这版单人本机运行的具体结论，不能据此改正式CAP/Unit状态或宣称平台生产级完成。试用发现的缺口需记录触发/预期/实际/证据后再设计修复。

## 六、本轮差异、验证与下一步

本轮验收追加六份文档与一份回执，已收尾ec23475、主干a20e16e并核对远端。方案开工97c1026；方案阶段仅六份Markdown及指定新证据根。与40/41相比，新增的是实际空目标初始化、首个空包恢复、运行smoke、本人观察与收尾备份的具体操作包，没有扩大产品功能。

文档复审使用中文需求/验收说明标准：核对目标、权限、状态、数据、回退与验收。已消除“父ACL等于新目录安全”“start是只读”“空库恢复等于100/200业务验证”“自然时间经过等于观察完成”的误读。当前未决事项只有是否批准E1–E6；实际目录ACL、启用smoke及本人观察结果必须由执行产生，不能在方案中补写PASS。

已执行固定Node文档核验：六份Markdown格式及本地链接通过，68源码、3009保护文件、9项原授权/最终证据SHA256相同，入口43脚本和操作包迁移校验和一致。node scripts/check-config.mjs返回BOOTSTRAP_CONFIG_OK；node --import tsx scripts/check-delivery-governance.ts返回DELIVERY_GOVERNANCE_OK，5 CAP/26 Unit、M2及唯一next route保持。验证命令、原输出和可复用的stdin核验源码见[文档核验回执](../../quality-gate/reports/local-activation-20260914/verification-proposal.json)。Quick dry-run为READY_TO_RUN，不能当测试PASS；本轮无产品源码变化，原35闸沿用同指纹实测，不重跑写数据测试。

所有原35闸和失败记录保持历史原值。本轮无新常驻进程，原PG13528/5432、API31152/3001、Vite37744/5173继续原状。仅生成本方案的脱敏元数据证据，保留在指定新根供版本审阅；业务目录/数据、访问密钥、浏览器profile和恢复cluster均未创建。后续保持原进程，关闭命令不针对这些历史服务。

确认E1–E6后连续执行到空基线恢复检查和本机就绪交付，续43号保留OBSERVING；待本人完成一个工作日使用再收观察结论。之后回到父POD，准备真实AI对话、材料理解和原型/PRD/AC共同生成的具体方案，继续承接同一需求的材料、决定和版本。正式next route保持前文值。

本次请求的是**E1–E6全包**：固定已验收来源、5432唯一业务schema与三个人目录、一次空包5548/5198恢复及确权清理、5188本机启动并保留、一个本人工作日观察及最多两个完成备份、七文档与指定脱敏证据、feature提交推送。精确参数同步见[待确认操作包](../../quality-gate/reports/local-activation-20260914/operation-package.json)，状态PENDING_USER_CONFIRMATION。[40号第七节](40-本地可用基线接入方案与范围确认-20260913.md)要求“此时才由陈立批准实际业务副作用”，[41号操作说明](41-本地可用基线实施与验收-20260913.md)要求“提出一次业务启用包”；因此本包完成后再请求确认，不能把源码验收替代业务启用批准。
