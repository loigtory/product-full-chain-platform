# 26 · M2c-1 存储基础实施与验收

> 当前状态：LOCAL_VERIFIED / READY_FOR_REVIEW，2026-09-12。陈立已回复“同意”，确认 25 号方案 B、M2c-1 精确白名单、单个隔离 PG schema 的测试/清理及必要的临时服务启动/恢复。十条闸与两项受控时序验证已通过，人工验收待陈立评审。

## 本轮改动（文件级清单）

本轮为 strict 系统迭代，父 POD-PFC-001 / 原型 M2 / M2c-1；只交付存储组件和测试稳定性改进，既有业务 API/WS/前端继续原路径。正式 CAP/Unit 状态不变，POD next route 仍为 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`。

采用 25 号已确认的逐项白名单：新建 connection/transaction/identifiers/requirements/migrations 五个存储模块、001 SQL、迁移 CLI、PG 测试工厂和专项脚本；修改 verify-m1-e2e / verify-m2b2-browser 两个测试入口；更新 AGENTS、19/20/25/26、两处 README 和本轮质量报告/证据目录。禁止修改 12/13、旧 m1-schema.sql、db.js/index.js/service/WS、业务前端、依赖、旧工程及交付台账。

步骤：环境核验与开工留痕 → 时序受控复现/PG 失败测试 → 最小实现 → PG 专项与九条原型闸 → 差异自审/独立清理回读 → 收工提交与评审。无并行代理。

Git：origin/main 已 fetch 核验为 `12d1733`；已确认方案为 `bc79cd4`。自动审批拒绝主干合并/推送（判定此次同意仅授权实施和隔离测试），未执行该操作。安全替代是在从 origin/main 创建的 `feat/m2c-storage` 本地纳入方案（`7525548`），main 保持不变。该分支之后的实现仍须陈立评审后才可合并。

## 验证结果（命令 + 输出摘要 + 证据路径）

开工：Node 24.20.0 / npm 11.19.0；根/server 离线依赖检查退出 0；Quick dry-run 为 READY_TO_RUN，非实际 PASS。已读取 AGENTS、19/20/21/23/25、交接 README 与质量配置。适用 fullstack-quality-gate / frontend-testing-debugging；Browser plugin not available，使用已有 Playwright + Edge，不安装依赖。

开工时计划：25 号十条命令，以及两种受控时序场景。M1 延迟登录超过原固定等待；M2b-2 在截图前受控重绘终端 DOM。保留原功能断言和有限等待，先取失败，再验证修复，禁止无界重试。PG 专项从缺失实现的红灯开始。

## 测试数据与进程

PG 目标：loopback PostgreSQL 18，`pfc_local`，本轮唯一 schema **`codex_test_m2c_20260912_storage`**。每次建 schema 前确认不存在，清理完成后才允许同名再次验证，不覆盖旧数据。仅该 schema 内不超过 200 行合成数据，名称前缀 `CODEx_TEST_M2C_20260912_storage`；两个虚构租户、需求/版本/审计、并发/失败/重复/非法引用案例按 25 号工厂设计构造，记录全部 createdIds 与清理结果。禁止业务 schema、数据库/角色/扩展修改。

既有忽略 `.env.local` 仅用于读取本机连接配置，配置必须校验 loopback/pfc_local；凭据仅在进程内传递，报告只写安全类别。测试脚本/连接池由 Codex 拥有，验证结束退出并回读 schema 不存在。服务初始状态 Stopped，若临时启动则结束恢复；不停止用户先前运行的实例。浏览器内存闸显式清空 DATABASE_URL，finally 关闭 Node/Edge。合成报告保存至 `docs/quality-gate/reports/m2c-1-storage-20260912/`；临时输出保留在忽略 `.local/m2c-1-storage-20260912/` 供审查，无真实敏感数据。

## 与交接包的差异（超范围项必须列出）

实施范围与 25 号一致。方案 main 合并被自动审批阻断，改为仅开发分支保留方案历史；没有借此改变源文件范围、DB 目标或依赖。后续契约缺口仍保留至 M2c-2/3/4 确认。

## 诚实边界（模拟/未做/待确认）

本轮不能宣称业务系统使用 PG、正式 CAP/Unit 完成、业务集成完成、角色安全、生产就绪或已发布。自动化 PASS 后仅组件 LOCAL_VERIFIED / READY_FOR_REVIEW；工程、测试、安全与产品负责人为陈立，人工验收单独记录。没有发布/观察窗/生产 smoke 或真实负载数据。

回滚：依据 feature 历史撤回组件改动；测试仅销毁本轮 schema，无业务迁移回滚。资源/权限/配置目标异常立即停止并保留证据。下轮 M2c-2 的业务仓储接入、正式 schema、权限和迁移方式需另行确认。

## 下一步

本轮实现与验证已经完成，见下方收工结果及质量报告；请陈立评审后再决定主干合并。M2c-2 的业务仓储接入和正式数据库目标仍需单独设计确认。


## 实施检查点（16:32）

开工提交 5879752。已新增九个白名单存储/SQL/CLI/测试工厂文件，两个时序脚本取得同条件红→绿：4000ms 登录延迟原 4/8、修复 8/8；600ms 重绘原 18/19、修复 19/19。新 PG 专项先因模块未实现红灯，首轮实现 12/12；自审新增“迁移记录存在但业务表缺失”测试后得到预期红灯，已补表就绪检查，待复跑。最终回归和收工尚未完成。

环境纠正：Windows 服务显示 Stopped 不代表实例未运行。Start-Service 因服务控制权限不可用而失败；随后 pg_ctl status 发现同集群已有手动进程 PID 13528，故启动指令在预检处停止，并未启动新实例。本轮只读预检确认 pfc_app_local 具备 schema 创建权限、loopback/pfc_local 及目标 schema 不存在；该已有实例不归本轮所有，最后保留。

首轮 PG 测试 29 行、就绪红灯轮 3 行合成数据均清理，独立 schema 读回为零。新增最终证据包含全部测试表行、UUID/public_id、独立进程 PID。当前无业务入口切换，无 main 变更。


## 收工结果与差异（16:38 核验后）

[完整质量报告](../../quality-gate/reports/m2c-1-storage-20260912.md)与[原始运行记录](../../quality-gate/reports/m2c-1-storage-20260912/final-results.json)已归档。十条闸全部退出 0：PG 13/13，原型六组通过，M1 layer/e2e 6/8，server M1/M2/M2b 18/19/7，M2b-2 model/domain/browser 6/8/19；4000ms 登录延迟和 600ms 重绘另行复验通过。新增源码语法、格式、五个存储模块 ESLint、配置与交付台账检查通过。十条入口运行合计 112.69 秒。

实际新增九个源码/SQL/测试文件：connection、transaction、identifiers、requirements、migrations、001 SQL、migrate-m2c CLI、m2c-pg-fixture、verify-m2c-pg；修改两个已批准测试脚本。对应白名单全部落实，无额外业务范围。38 个旧业务源文件与方案基线内容一致，依赖和旧工程未改。文件级新增/删除行数见证据包 file-changes.tsv，源码哈希见 source-manifest.json；README、19/20/25/26 与 AGENTS 只更新当前状态和入口。

PG 新建存储组件支持需求/版本/审计原子写入、库内编号、UUID 映射、租户复合外键、事务回滚、独立进程重启、连接/SQL 超时、版本/checksum 迁移和表就绪检查。这里只是组件真实 PG 验证，业务路由尚未使用组件。为防 Windows 换行转换误报，迁移 checksum 与证据同时采用明确的 LF 归一化规则；未修改 Git 全局设置或旧 SQL。

最终 PG 样本共 29 行，包含全部合成记录、迁移与计数；退出后 schema 删除，独立 SQL 回读 remaining=0。保存了每张测试表清理前的数据、UUID/业务编号与两个重启进程 PID。16:38:53 独立系统审计确认本轮 Node/Edge helper 为零、临时端口无监听，未留下本轮常驻进程。原有 PostgreSQL PID 13528 / 5432 保留，本轮没有启动或停止它。

自审新增缺表场景先红后绿；其他两项时序复现和最初组件缺失红灯原文均保留。阶段、既有 API/WS、原型业务界面和角色边界没有改动；已查看正常双终端及长日志截图。无已私自落地的新契约。相对 25 号的实施偏差只有 Git 路径：主干变更被自动审批拒绝，保留在 feature 分支；没有绕过拒绝。

本轮结论为组件 LOCAL_VERIFIED；应用 INTEGRATED、陈立 ACCEPTED、RELEASED 均未成立。父 POD/正式 CAP/Unit 与正式 next route 不变。回到原型 M2c 的下一路线：陈立评审 M2c-1，随后确认 M2c-2 接入与迁移方案。

过程记录：开工提交 5879752（16:15:28）至独立清理核验（16:38:53）约 23 分钟，不含前置核实与最终报告。最终闸首轮通过；无发布后指标。经验：异步测试等待完成条件、迁移就绪同时核对表、检查服务也检查实际实例、同一测试 schema 精确回收并独立读回。
