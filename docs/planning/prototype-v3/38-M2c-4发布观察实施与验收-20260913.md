# 38 · M2c-4 发布观察实施与验收

> 最新验收：陈立于2026-09-13对2642524具体交付回复“确认”，本轮原型为SCOPED_ACCEPTED，包含历史验收恢复和缓存恢复修复；授权按23合并、推送main并继续40号方案。下方PENDING_REVIEW/DECISION_PENDING为历史状态。

## 已确认的恢复补充及执行计划

本次为原M2c-4的system iteration / strict续修，分支feat/m2c-release-observation，起点31f50e7；刷新origin/main仍为0689723。已核实固定Node24.20.0/npm11.19.0、根/server离线依赖，复用现有安装；Quick dry-run READY_TO_RUN，仅为计划。沿用fullstack-quality-gate和frontend-testing-debugging；Browser plugin not available，使用现有Playwright/Edge。

采用用户同意的方案：现任Owner对原冻结计划追加重评，随后仅核实原UNKNOWN尝试。原批准、验收、尝试及历史成员姓名不改写；服务端专用路径可忽略历史成员已失效的当前权限，但必须重新验证内容/版本/epoch/证据/时间/当前权限和前序链。普通准备输入、新计划和新尝试仍要求当前有效验收。评审及核实结果明确记录沿用的验收快照和重评依据。以服务内部可选校验参数复用原规则，默认关闭，不从HTTP载荷启用；不增加SQL、迁移、接口或依赖。

计划：1）扩充确定性factory和API失败回归，覆盖降权、停用、未重评拒绝及原尝试恢复；2）最小服务校验和评审/结果提示；3）浏览器实际重评→核实→观察与负向恢复；4）原30命令、静态/配置/治理、文件差异及独立清理，feature提交推送后交陈立验收。本次不另开子代理或常驻服务。

测试数据继续使用第6节六精确schema及各自限额/文件根，CODEx_TEST_合成数据；新情形由原有效业务/设计/开发/测试/验收/批准API链生成，再经成员API降权或停用。覆盖Owner/Executor/Viewer、同/跨需求、原/新尝试、证据撤权、业务版本变化、前序/时间冲突；断言一条attempt、原review_id不变、历史验收不改写、新review和追加核实事件可读。SQL仅隔离故障注入/只读断言；finally恢复成员并清理本次确权schema/原件，独立读回。旧证据原文保留，新结果追加命名，不覆盖旧通过或失败报告。

父POD-PFC-001 / 正式M2 / 映射05-03/04/05，5CAP26Unit和唯一POD-PFC-001/M2/R3a/artifact-collaboration-tdd不变。开工时实现IN_PROGRESS、隔离集成待新增回归；当前结果见下节，人工验收PENDING_REVIEW，业务集成/真实发布NOT_ENABLED。恢复策略只用于历史事实核实，新增部署、业务PG、main合并均未授权。回退至31f50e7会重新阻断此例外；不涉及schema回退。沿用原有容量/超时限制，无外部模型/工具调用。

### 恢复补充完成与验收入口

实现LOCAL_VERIFIED、集成ISOLATED_PG_VERIFIED、验收PENDING_REVIEW、业务集成NOT_ENABLED、真实发布NOT_EXECUTED。本次继续同一M2c-4分支和原开工记录，不另起需求或台账路线；较31f50e7实际修改10个源码/测试路径，均在原63文件清单内。完整文件级差异、原/现行数、30条命令和输出见[更新后的质量报告](../../quality-gate/reports/m2c-4-release-observation-20260913.md)。

服务只在现任Owner重评同一未变计划、核实原UNKNOWN的内部路径沿用历史成员具名记录。普通release-inputs仍显示历史成员失效，不为新发布放行；原attempt.review_id、验收ID/内容和原批准不改写。重评意见注明冻结验收与原记录，追加结果带验收ID和重评ID，UI在评审意见附近显示适用边界，结果详情可追溯。测试证实连续UNKNOWN只能追加原尝试，版本/epoch/证据变化、旧前序和未重评均拒绝。

首次完整回归于10:44:34–10:50:46 UTC执行，30条一次全通过，运行前后63指纹一致；静态42脚本/39模块、10份改动格式及5份服务ESLint、配置与交付治理通过。独立只读连接确认六schema不存在、五原件根0文件；本轮工具进程关闭，预存PG13528/API31152/Vite37744保留，停止命令见本文末尾。原固定文件名报告和截图由执行器另存本轮输出后恢复原文，旧证据未改写。

失败留痕：先复现旧逻辑重评409（RELEASE_PREPARATION_REQUIRED），再实现并通过；纯确认选择规则也有缺函数失败→11组通过。另三次是测试驱动问题：GET误传null请求体、停用成员不支持经现有API重启、从观察页直接寻找发布页按钮。分别纠正测试请求、仅在确权隔离库恢复成员、沿现有导航回查；未扩展产品接口或放宽超时。原始失败JSON、失败截图和最终结果均保留在本轮证据根。

人工验收建议：按[重评画面](../../quality-gate/reports/m2c-4-release-observation-20260913/release/recovery-review-1440.png)核对冻结验收提示与现任Owner意见，再按[核实结果](../../quality-gate/reports/m2c-4-release-observation-20260913/release/recovery-result-1440.png)核对原UNKNOWN及新结果/重评依据。具体feature提交推送后由陈立确认；这次“同意”仅确认规则，不预写产品验收通过。真实发布、长期观测和多人负载尚无实测样本。

新增API失败回归至首次完整门禁约19分钟（10:31:54–10:50:46 UTC）；末次UI复核和受影响复测于11:11:05 UTC汇总结束。此前读取与方案记录耗时未单独采集。完整门禁约6分13秒。业务规则漏项来源为D2/D3组合状态未先列齐，补充的停用/降权、同尝试、指纹与证据回归用于防止复发；前置三次测试驱动返工与末次QA调整独立记录，缓存失效阻断是已修复的产品缺陷。陈立继续兼任产品/工程/评审/测试/安全责任人，各角色结论仍各自留痕。

### 最终UI复核与当前证据

最终读图发现旧身份会话遗留401提示，恢复场景改用独立浏览器上下文及现任Owner。随后确定性清空releaseClient工作区缓存，复现“核对最新依据”因缓存不存在而阻断；已在原白名单release-actions.js内改为先校验当前需求/表单身份再主动读取，409草稿比较继续保留。动态页面标题按app.js的实际路由标题断言，没有修改产品标题。

[最终30项汇总](../../quality-gate/reports/m2c-4-release-observation-20260913/gates-recovery-final.json)保留首次1–28原始输出，记录最后两份UI文件变化及静态/29/30复跑来源，当前63指纹无漂移。[最终浏览器14组](../../quality-gate/reports/m2c-4-release-observation-20260913/release/browser-2026-09-13T11-08-47-075Z.json)通过；新会话console警告/错误0、HTTP异常0，键盘Enter重评与实际路由标题通过。最终1440宽重评/结果截图已读图核对，无此前401提示。历史失败截图与JSON全部保留，业务规则待决定项为零。最后浏览器关闭后，再次独立核查六schema/五原件根和进程，均无本轮残留。

### 31f50e7历史检查点（已被上文完成记录替代）

> 历史：已实现范围30/30门禁通过，模型10/API17/浏览器13；隔离清理完成。整轮DECISION_PENDING：UNKNOWN兼容规则有一个待用户决定项，人工验收待具体交付。

## 设计确认与范围

用户对37号方案提交5ee58faa0ef244f93e9db09acdee8a399c01c3ec回复“确认”，批准A/D1–D8、63源码测试文件、七文档、六精确隔离PG目标和清理包、30条门禁。批准方案归一化SHA256：7b72c4e044bfbfdc067581ff43d5d48f34f8ee473c4b0d1fa542003d4a1ef6ab。授权记录见[确认包](../../quality-gate/reports/m2c-4-release-observation-20260913/authorization-20260913.json)。

父POD-PFC-001 / 正式M2；映射05-03/04/05（正式M3成员），5CAP/26Unit及唯一POD-PFC-001/M2/R3a/artifact-collaboration-tdd不变。分类system iteration / strict；实现IN_PROGRESS、集成NOT_VERIFIED、验收PENDING_DELIVERY、发布NOT_AUTHORIZED。feature为feat/m2c-release-observation，从刷新main0689723起；已批准六份方案文档准确带入。

## 实施计划与检查点

- C1：失败测试 → 006/就绪 → 发布草稿/评审。
- C2：结果/核验/回退、观察/最终验收、发布后变更保护与API事务。
- C3：五前端模块、observe聊天/证据/知识预填、恢复和PC三档。
- C4：30闸、静态/配置/交付治理、全保护指纹和清理读回、差异报告、feature收工推送后人工验收。

开工环境：Node24.20.0/npm11.19.0，根/server npm ls --depth=0 --offline通过，复用安装；Quick --dry-run READY_TO_RUN，仅为计划。沿用fullstack-quality-gate与frontend-testing-debugging，Browser plugin not available，使用仓库Playwright/Edge。文档先读取AGENTS/README/19/20/21/23/37及质量profile。

## 数据与进程

37第6节及确认包为本轮数据设计：确定性factory、双租户双需求、角色/版本/时间/状态/并发/负向，六精确schema与各自上限；所有记录CODEx_TEST_，无客户数据。API构造有效业务前置；SQL仅隔离初始化/故障/独立断言。每次run记录createdIds/OID/标记/原件摘要，finally仅清本次确权目标，独立连接读回不存在/零残留及其他schema元数据不变。证据入批准新根，历史报告不覆盖。

串行API/Edge/模拟bridge，loopback5189–5196，单场景45秒/套件300秒。当前已有PG13528、API31152、Vite37744原位保留；本轮拥有的进程记录PID并关闭。无子代理或新依赖。

## 诚实边界、风险与回退

发布/回退/指标均USER_REPORTED；不执行真实Shell/CI/发布，不启用业务PG，不更新正式CAP/Unit台账。测试输出不能证明真实外部效果。原型交付需陈立对具体commit和PC截图/业务差异验收；不因同意方案就写验收通过。发布/部署后smoke/readiness/观察为NOT_EXECUTED，无生产样本。

回退只限未启用业务PG前回到已验收main；隔离006库按确权清理，不用005程序直接连接006降级。业务启用须另确认环境/负责人、数据库与文件同点备份、迁移、观测与回滚。本轮完毕回父POD评估原型能力矩阵，正式next route保持。耗时/返工/失败闸/缺陷来源在实际执行后记录，不编造。

### C1检查点

开工提交97c752d。模型8组先因release-policy缺失失败（08:10 UTC），实现后8组通过（08:12 UTC）。该结果仅证明纯规则，API/PG/浏览器待执行。继续006及服务，不据此声明交付完成。

### C2/C3检查点（持续实施，未交付）

006显式迁移与11张表已实现；12组API集成首轮全部通过（api-2026-09-13T08-37-00-692Z.json，实际12组，清理schema=0，文件=0，测试进程=0）。覆盖UNKNOWN同尝试核验、回退后修复、末端指标/遗留项、冻结附件版本与撤权、并发幂等和缓存前权限。新增五UI模块、42脚本已接入，浏览器测试仍在调试，不能标为通过。下一步：完成浏览器全链路和失败恢复，适配旧26闸到006及新证据根，跑完整30闸与静态检查、独立清理、保护指纹、差异评审、feature收工提交推送。无main合并/业务PG启用。

已定位修复：006 ALTER外键语法、旧详情只识别005、false来源绕过、UNKNOWN未执行冒烟项被错误要求实际结果；附件替换接口的200误期望201仅修改测试。当前没有遗留的本轮API测试进程，浏览器验证临时进程在本套件finally清理。

### C3进一步检查点

浏览器8组通过：准备表单/送审/审批、UNKNOWN核验、观察/最终验收、独立知识保存、三档PC截图、503同command恢复、409显式比较、切需求晚响应隔离。42脚本语法与39模块ESLint通过，当前白名单/2806保护指纹通过。视觉读图发现结案后误显示“指标未完成”和窄画布四列过密，已改为结案不显示新验收阻断、两列依据/内容表（后续完整闸再次复测）。旧26闸正迁至新证据根/006；R3历史scope固定accepted b07dadf，当前scope由M2c-4架构闸负责，不弱化历史断言。

待用户对齐（已用异步问题提出，不能以等待时长视为同意）：UNKNOWN时原批准人停用/降权，当前Owner重评同一冻结计划后核实原尝试，是否允许沿用原产品验收快照。推荐允许，以恢复历史事实，不新建发布尝试/不执行部署。该分支尚未实现；其余授权工作持续推进。

### C4回归中检查点

模型增至9组、API增至15组通过，补入早于产品验收的准备、站内通知、非阻断遗留项结案后续记、派生报告不可改写、规模边界。旧门禁1–12通过；13初次file导航10秒超时（无API请求/页面错误），不改源码和超时参数原命令复跑15组通过，失败证据保留domain/browser-navigation-timeout.json。随后新增“发布后回查上游测试/成果再返回”浏览器回归，先因入口缺失失败，修复新发布视图与上游动作可用性后浏览器9组通过。暂未据此宣称30闸通过；14–26正在串行执行。

局部API/浏览器曾遇沙箱spawn EPERM，转交自动审核执行已批准的同一隔离门禁，非业务故障。未改依赖、历史SQL或保护文件；UNKNOWN权限恢复规则仍待确认。

### 对照方案后的修复与当前冻结

新增实测暴露D4观测前序未生效（错误前序收到201）；已补前序/序号、同观察同指标或同遗留项复合约束、每观察合计2000事件计数，失败事件原文保留。具名记录落库冻结姓名，当前权限单独读回；分页改为SQL分页，新增接口响应限制512KiB。准备版本必须对应交付版本，已提交准备列表不混入草稿。观察显示服务端剩余时间，最终验收前展示冻结目标/AC、指标实际差异和遗留负责/期限。

非例外的Owner重评已实现并验证：只变更准备批准人且上游产品验收仍有效时，当前Owner可重评同一冻结计划，UNKNOWN仍只有原尝试，历史评审和尝试绑定不改。只有“上游产品验收也因离职/降权失效”是否允许沿用快照尚待用户选择，未绕过该阻断。17组API包含显式修复后新开发版本、新测试、新产品验收和第二轮发布，旧观察/成功结果仍在。

浏览器13组通过，新增observe结案后上传/报告引用与刷新、写成功但GET失败的原command恢复、回退UNKNOWN核实/显式修复、用户及地址晚响应隔离。首次恢复回归发现空数组的新草稿无法恢复旧表单的动态冒烟/指标行；已依草稿元数据重建结构，显式比较后同步父计划，再复跑通过。旧15门禁原源码原命令复跑10组通过；其失败轮无页面错误，记录已提交且清理为零，暂列浏览器间歇超时，不修改业务逻辑或扩大超时掩盖。

该检查点完成62份JS/MJS/HTML格式化和静态42脚本/39模块验证；随后进入完整30闸。仍无业务PG启用、真实执行或main合并。

### 验证执行补记

原型全量第25条R3历史架构逐文件启动5178次Git，Windows耗时过长；为避免持续占用，确权终止该核验子进程12648，保留失败轮。改为两次git ls-tree批量比较同一2589保护文件的Git对象，断言范围不变，7.6秒复跑通过；当前2806原始字节仍由新架构闸负责。首24条未依赖该测试文件，证据聚合显式记录唯一源码适配及受影响重跑25/29，未重跑无关用例。此时29/30通过，最终浏览器尚在进行；最终结论见下节。

补充ESLint原始通用配置产生对象省略/Express四参数等误报；采用标准对象rest省略和保留next签名语义，并逐项对比main。新增后端问题0，旧requirement/conversation共有6条未使用绑定为已存在债务，本轮不顺手清理。原始诊断与基线比较都保留在supplemental-source.json和supplemental-backend-lint.json。37份JS语法、62份源码格式、配置和正式交付治理通过。

## 31f50e7历史交接结论（当前结果见本文开头）

最终证据：[30条门禁](../../quality-gate/reports/m2c-4-release-observation-20260913/gates-final.json)、[文件级差异与报告](../../quality-gate/reports/m2c-4-release-observation-20260913.md)、[独立清理](../../quality-gate/reports/m2c-4-release-observation-20260913/cleanup-independent.json)、[进程读回](../../quality-gate/reports/m2c-4-release-observation-20260913/process-readback.json)。最后503恢复按钮未立即重绘缺陷已复现并修复，13组浏览器、静态42/39和新架构重新通过。

实现：除待决定的历史验收兼容分支外已实现；集成：ISOLATED_PG_VERIFIED，标准业务集成NOT_ENABLED；验收：PENDING_DECISION_AND_REVIEW；发布/部署：NOT_AUTHORIZED/NOT_EXECUTED。父POD-PFC-001/正式M2/映射05-03/04/05、5CAP26Unit及唯一POD-PFC-001/M2/R3a/artifact-collaboration-tdd保持。不得从30闸PASS推断本轮完整业务验收、正式M3完成或外部发布。

按23提交并推送feature检查点供继续审阅，提交必须明确“规则待确认”，不合并main。用户决定后仅在现有63文件/六隔离目标包内补相应分支及回归，再提交最终人工验收。后续方案返回父POD核对能力矩阵，不自动推进真实工具/CI。

本轮测试进程均关闭，六schema不存在、五原件根文件0。预存服务继续保留：PG13528/5432（停止：`.tools/postgresql-18.6/bin/pg_ctl.exe -D .local/postgres-data stop -m fast`）、旧API31152/3001（`Stop-Process -Id 31152`）、Vite37744/5173（`Stop-Process -Id 37744`）。无子代理、新依赖或新常驻服务。

## 2642524验收收尾记录

接受人为陈立，确认方式为本轮明确问题后的“确认”；范围是37号及已同意恢复补充对应的原型交付26425240340ab813b8d9e0f60765df02f11f1140。新增[验收记录](../../quality-gate/reports/m2c-4-release-observation-20260913/acceptance-20260913.json)保留接受范围和验证引用；[39号](39-原型能力核对与后续路线建议-20260913.md)记录能力核对及后续A方向。

实现LOCAL_VERIFIED、隔离集成ISOLATED_PG_VERIFIED、原型验收SCOPED_ACCEPTED；标准业务集成NOT_ENABLED、真实发布NOT_EXECUTED。正式POD-PFC-001/M2/映射05-03/04/05、5CAP26Unit和POD-PFC-001/M2/R3a/artifact-collaboration-tdd保持。已披露的模拟回复、模拟执行、人工结果、日常PG及真实负载限制随本次原型范围接受，不代表生产风险已经消除。

本次为docs-only / light，修改AGENTS/19/20/38/39/两份README并新增验收JSON，原门禁和历史报告不改。复核30条PASS记录、63源码与2806保护原始指纹均一致；没有重跑业务用例或借用旧隔离数据写入授权。按23先feature提交推送，再从刷新main做--no-ff合并、推送及SHA读回；实际提交与远端结果以Git为准。合并后40号只做六文档方案，不进入源码/业务PG/工具实现。

没有新测试数据、服务、浏览器、子代理或依赖。预存PG/API/Vite保持，停止命令见上文。回退基线保留原main0689723；当前未启用新业务schema，不涉及数据库回退。正式部署、发布后smoke、长期观察及业务成效均未执行。
