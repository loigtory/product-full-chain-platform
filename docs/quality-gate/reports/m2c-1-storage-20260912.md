# M2c-1 存储基础验证报告

结论：**LOCAL_VERIFIED / M2c-1 组件交付 ACCEPTED**。2026-09-12，陈立在收到 `af709eb` 交付及明确的主干合并推送问题后回复“同意”，确认本轮交付并授权该 Git 操作，详见 [26 号人工验收记录](../../planning/prototype-v3/26-M2c-1存储基础实施与验收-20260912.md)。下文保留自动化收工时的原始状态；[合并预检](m2c-1-storage-20260912/merge-preflight.json)记录本次新增检查。

PG 存储组件验证通过，现有业务 API/WS/前端尚未接入该组件；不是应用 INTEGRATED、正式 CAP/Unit ACCEPTED 或 RELEASED。父级 POD-PFC-001 / 原型 M2 / M2c-1，正式 CAP/Unit 与 POD next route `POD-PFC-001/M2/R3a/artifact-collaboration-tdd` 保持不变。

陈立在 25 号方案后回复“同意”，授权本轮精确文件范围、唯一隔离 schema 的本地测试/清理及必要的 PG 临时启动/恢复。工程执行与自审为 Codex；产品、工程、测试、安全及最终验收负责人为陈立，人工验收待评审。本轮没有发布目标、部署、生产 smoke 或观察期。

## 改动与风险覆盖

- 新增五个 PG 存储模块、一份最小 SQL、迁移 CLI、确定性测试工厂和专项脚本。实现显式模式/目标检查、连接超时、事务、业务编号到 UUID 的映射、需求/版本/审计、版本/checksum 迁移和表就绪检查。
- 修改两个既有浏览器测试：M1 等待登录/初始化/对应保存响应，使用临时随机测试密钥并增加 finally 清理；M2b-2 在同一个 DOM 执行任务内滚动，保留运行完成前双终端已输出断言。分别提供 4000ms 登录延迟和 600ms 重绘场景。
- 没有变更原型业务前端、既有 db.js/index.js/service/WS、API 契约、依赖或旧工程。[38 个既有业务文件比对](m2c-1-storage-20260912/preserved-business-source.json)与 `bc79cd4` 内容一致。[11 个已测源码哈希](m2c-1-storage-20260912/source-manifest.json)同时保存原始字节与 LF 归一化哈希，避免 Windows 换行转换造成歧义。

## 执行环境与数据

固定 Node 24.20.0 / npm 11.19.0，已有依赖离线核验。Browser plugin not available，使用已有 Playwright + Edge，file 协议原型、loopback 临时服务、PC 1280/1440/1920×960。没有新增依赖或真实 Bridge/Shell/CI 调用。旧 Quick/Core/Full 没有被本报告冒充执行；本轮按 Active Handoff Scope、23/25 号执行原型闸与 PG 专项。

PG 使用已存在的本机集群、`pfc_local`、`pfc_app_local`。连接配置从忽略 `.env.local` 只读解析，凭据仅在进程中传递，没有输出或提交。唯一 schema：`codex_test_m2c_20260912_storage`；创建前独立确认不存在，所有业务样本前缀 `CODEx_TEST_M2C_20260912_storage`。两个合成租户、需求/版本/审计、并发、重复版本、非法引用、缺表、checksum 错误和不可用端口均为受控本地场景。

工厂按 25 号明确的业务有效性和预期断言构造数据；UUID 由组件生成，业务编号由事务计数器分配。最终 PG 运行留存 29 行（含租户、计数与迁移记录），远低于 200 行上限，随后全部销毁。外部数据库写入、外部业务调用和角色/扩展修改均未发生。

## 验证结果

[12 条实际运行记录](m2c-1-storage-20260912/final-results.json)全部退出 0，其中十条正式入口加两条受控压力变体；十条入口合计 112.69 秒。以下命令相对仓库根，均使用固定 Node PATH。原型九条为 `PFC_DB=memory`、空 `DATABASE_URL`；PG 专项独立解析并校验隔离目标。

| 实际命令 | 结果 |
| --- | --- |
| `node server/verify-m2c-pg.mjs` | PASS 13/13，详见 [PG 原始报告](m2c-1-storage-20260912/pg-report.json) |
| `node output/pfc-workbench-prototype/verify-prototype.mjs` | PASS 6 组：静态、完整性 8、模型 11、定向 46、浏览器 13、scope7 12 |
| `node output/pfc-workbench-prototype/verify-m1-layer.mjs` | PASS 6 |
| `node output/pfc-workbench-prototype/verify-m1-e2e.mjs` | PASS 8 |
| `node server/verify-server.mjs` | PASS 18 |
| `node server/verify-server-m2.mjs` | PASS 19 |
| `node server/verify-m2b.mjs` | PASS 7 |
| `node output/pfc-workbench-prototype/verify-m2b2-model.mjs` | PASS 6 |
| `node server/verify-m2b2-domain.mjs` | PASS 8 |
| `node output/pfc-workbench-prototype/verify-m2b2-browser.mjs` | PASS 19，三种 PC 宽度、9 张合成截图 |
| `PFC_E2E_DELAY_LOGIN_MS=4000` 后运行 `verify-m1-e2e.mjs` | PASS 8，保持同一延迟条件 |
| `PFC_E2E_REPAINT=1` 后运行 `verify-m2b2-browser.mjs` | PASS 19，保持同一重绘条件 |
| 固定 Node `--check`（全部 10 个新增/修改 JS/MJS）；现有 Prettier `--check`（8 个匹配的 server 文件） | PASS；既有 ignore 排除原型脚本，没有修改 ignore |
| 现有 ESLint recommended（5 个存储模块，CommonJS/Node 全局） | PASS，0 errors |
| `npm run check:config` / `npm run check:delivery-governance` / `git diff --check` | PASS；5 CAP/26 Unit，正式 next route 不变 |

PG 测试覆盖目标在副作用前拒绝、连接不自动 DDL、迁移重复/checksum、缺表时拒绝就绪、SQL 字段回读、需求/版本/审计及计数一起回滚、并发编号与版本唯一性、租户上下文/复合外键、两个独立进程重启回读、连接/SQL 超时、CLI 范围限制和组件/业务入口边界。原始报告的 `observations` 保存两个进程 PID，`cleanup.readback` 保存删除前各表完整合成数据，便于复核实际 UUID、业务编号和关系；`remaining=0` 为清理后独立目录查询。

## 失败、修复与自审

失败证据全部保留，未通过覆盖原文获取“全绿”：

| 复现 | 旧结果 | 修复与验证 |
| --- | --- | --- |
| 登录请求受控延迟 4000ms | 4/8，固定 2500ms 等待后 token 为空；后续回读随之失败 | 等待 token/WS 初始化与具体保存响应；压力条件下 8/8。见 red-m1-delay / final-stress-delay 日志 |
| 终端持续重绘 600ms | 18/19，`scrollIntoViewIfNeeded` 遇节点脱离 | 查询并在同一 DOM 任务内执行滚动，未弱化输出断言；压力条件下 19/19。见 red-m2b2-repaint / final-stress-repaint |
| 新存储尚无实现 | `MODULE_NOT_FOUND`，未创建 schema | 新增组件后首轮 PG 12/12。此红灯只说明实现缺失，不冒充已复现某个存量 PG 业务缺陷 |
| 自审新增缺表场景 | 迁移记录仍在，旧就绪判断错误通过，新增断言红灯 | 就绪判断增加全部基础表存在性；最终 PG 13/13。见 red-readiness / pg-report |

自审重点：显式 DSN 字段防止连接串参数覆写目标；schema 与授权精确相等；schema 所有权与标记校验；事务内 schema 搜索路径；复合租户外键；迁移 checksum 换行归一化；失败释放连接；没有业务入口切换。没有未解释的最终失败。当前 `requireReady` 校验迁移身份与表存在性，不是数据库任意手工 DDL 漂移审计工具；CLI/仓储仅面向本轮本地验证用途。

Codex 已查看正常运行双终端和长输出截图，确认模拟标识、输出可读、左侧历史阅读位置与右侧尾部跟随。截图由连续合成用例产生，会含测试提示；本轮没有修改业务 UI，不将截图或测试通过作为新业务视觉验收。[截图路径与哈希](m2c-1-storage-20260912/screenshots.json)保留在本机忽略目录，JSON/日志已入证据包。

## 清理与残余边界

[16:38 独立系统核验](m2c-1-storage-20260912/final-independent-cleanup.json)：目标 schema 不存在；5189/5191/5192/5193/5194 无监听；本轮 Node/Edge helpers 为零。最终常规浏览器进程 46952/3328、压力变体 45100/29608 均已退出，所有合成内存数据随进程销毁。本轮没有留下常驻进程。

服务管理器最初显示 Stopped，Start-Service 由于服务控制权限不可用而失败。随后 `pg_ctl status` 发现本机 PostgreSQL 已手动运行（原有 PID 13528、端口 5432），故没有继续启动或停止它。该实例非本轮所有；结束时保留，Windows 服务仍 Stopped，不能由此推断数据库未运行。

SQL 新增组件尚未接入既有 HTTP/WS。角色权限、SSO、多租户生产隔离、材料文件落盘、历史导入、业务库迁移、发布/CI、真实工具执行与实际容量性能均未验收。下一步回到原型 M2c-2 的方案确认；正式 POD next route 不变。没有应用数据回滚或发布，代码回退通过未合并 feature 历史完成。

## 版本与过程记录

远程 main 基线 `12d1733`，方案 `bc79cd4`；本地开发分支从 main 建立后纳入已确认方案（`7525548`），开工提交 `5879752`。自动审批拒绝了方案合并并推送 main，理由是此次同意只覆盖实现与隔离测试，主干变更缺少单独授权。该拒绝没有被绕过，main 保持不变；实现以 feature 分支交评审。

开工提交 16:15:28，独立清理核验 16:38:53，约 23 分钟，不含前置核实和最终文档/提交。两项存量测试的红灯为受控复现，PG 缺表红灯来自自审；最终正式闸首轮全部通过。回顾：不以 Windows 服务状态代替实例状态；等待实际异步完成；schema 迁移版本与可用表需同时核验；保存 LF 与原始哈希以便 Git 换行转换后复核。不虚填人工验收、性能或发布后指标。
