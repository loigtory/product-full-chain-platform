# 28 · M2c-2 实施与验收

> 当前状态：LOCAL_VERIFIED，13/13 验收入口通过；本轮原型交付已由陈立于 2026-09-12 回复“同意”验收，并授权合并推送 main。评审对象为 `11d1fdf1ed57a3fb6fdaf965b626eb9dc4fd2eff`。本次确认不包含业务数据区启用、部署或后续迭代。

## 验收与 Git 收尾记录

本次为 docs-only 验收记录及已授权 Git 集成，不修改行为、依赖、数据库或测试数据。此前已展示本文件的差异、13 项输出、截图和残余限制，并明确询问“是否同意合并并推送 main”；陈立回复“同意”。按 23 号记录为本轮原型交付人工验收通过，保留模拟执行、未启用业务 PG、收据体积增长、容量及兼容性未全测等限制。确认方式为对交付包的会话评审，没有新增现场操作或独立角色复测。

执行计划：补录本次确认 → feature 文档提交和推送 → `--no-ff` 合并 main → 推送并独立读回远程 SHA。收尾前已执行 `git fetch origin`：main 与 origin/main 均为 `05af5ded7f5e3f192f1b7959397f2d46d9d90edd`，feature 本地及远程均为上述评审提交，工作区干净。

本次文档白名单为 AGENTS、19、20、本文件、两处 README 与本轮质量报告，共 7 个文件。原实施差异清单和原始验证证据保持原样；本次追加差异仅说明验收与 Git 授权，不改变原实现结论。实际合并结果以 main 的双父提交、feature 与 main 的树一致性检查及远程 SHA 读回为准，提交号在最终交付消息和 Git 历史记录。

核验口径：固定 Node 24.20.0 / npm 11.19.0；根和 server 的离线依赖检查退出 0，Quick dry-run 为 READY_TO_RUN。逐文件重算 UTF-8/LF SHA256，58 项均匹配已评审源码；13 条原始门禁记录均为 PASS、exitCode=0 且原文存在。重跑 `npm run check:config`、`npm run check:delivery-governance`、`node server/verify-m2c-architecture.mjs`，均通过。7 个文档的 96 处本地链接及 `git diff --check` 通过；合并后须验证与验收分支树一致、工作区干净、远程 SHA 相等。本次未重复执行 PG/浏览器全链路，未把证据复核写成新一轮 13 闸执行。

## 本轮改动（文件级清单）

父级 **POD-PFC-001 / 原型 M2 / M2c-2**，strict 系统迭代。正式 CAP/Unit 实施、集成、验收和发布状态均未提升；唯一 POD next route 仍为 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`。当前任务只完成已确认的原生 JS/Express 领域持久化范围。

开工远程 main 为 `05af5ded7f5e3f192f1b7959397f2d46d9d90edd`，已确认方案为 `64c1590`。从 origin/main 建立 `feat/m2c-domain`，方案并入 feature 的提交为 `f90d2bd`，开工提交为 `336e7cb`（17:47:10）。收工前重新 fetch，main 与 origin/main 仍为 05af5de。未覆盖他人改动。

按 [27 号第七节](27-M2c-2接入方案与范围确认-20260912.md) 白名单实施。完整文件路径、增删行数和源码指纹见 [文件差异清单](../../quality-gate/reports/m2c-2-domain-20260912/file-diff.md)；没有删除旧工程、历史备份或证据。

| 改动位置 | 实际行为与目的 |
| --- | --- |
| server/src/runtime.js、access.js、db.js、auth.js、index.js | 显式 memory/pg；PG 连接、迁移、私有账号配置不就绪即失败；禁止启动 DDL、seed、快照写和静默降级；actor/tenant/角色取服务端上下文 |
| server/sql/m2c/002-domain-workspace.sql、persistence/ | 新增本轮领域表和约束；001 不变。仓储负责 SQL，事务同时提交编号、版本、审计、通知、命令收据和事件 |
| domain/ 五个职责服务、dto/templates/events | 原 service 成为模式门面；需求、材料、会话、执行、通知分开。旧 memory 行为保留在各职责适配器；route/WS 不再直接写 Map 或 SQL |
| requirements / questions / versions | 创建同时写 idea v1、两条未回答问题和原始想法材料；回答和新版本同事务；版本确认、阶段顺序、材料影响与旧基线冲突在服务器校验 |
| files/local-files.js、material-service.js | 合成文件实际落盘，按 tenant/hash 去重；原件下载校验关联与 SHA；替换保留历史。回滚只清理本次创建且无引用的原件，并重新取得文件锁防止误删共享文件 |
| conversation-service.js、messages.js | 保存用户/模拟回复、停止/中断/重发关系、具体版本引用、Diff 采纳/拒绝；模拟文本逐步持久化，进程重启不会伪造回复完成 |
| execution-service.js、ws.js、sim-bridge.mjs | 计划基线与审批、唯一 dispatch、序号/事件去重、取消晚到结果保护；行/回放/质量记录/租约/通知入 PG；重启后的不确定作业为 UNKNOWN，旧租约 lost，不重复派发 |
| original/domain-view/actions/conversation.js | 新领域适配模块恢复草稿、材料、消息和作业；PG 只在浏览器保存视图与未提交草稿；文本/图片预览、旧附件版本下载、每页 100 条消息及最新 200 行终端 |
| original/api-client/domain-client/ws-client/model/app/actions/attachments.js、index.html | 保留原三栏和双终端，只调整模式/动作入口；PG 未接入操作禁用并给出原因；memory 的原 token 键及 local/mock 行为保持 |
| 新增与现有 verify 脚本、m2c-domain-fixture.mjs | 真实 PG/API/WS/浏览器验证、精确数据归属和清理、职责边界与旧主线回归；原型载入 21 scripts / 16 lint modules |
| AGENTS、19/20/27/28、两处 README、本轮报告 | 更新授权、范围、证据和下一步；不改 12/13、正式交付台账或旧工程规则 |

四个检查点为：仓储/权限 → 需求/文件/会话 → 执行/事件恢复 → 完整回归/证据/差异评审。没有新框架、依赖、外部资产或并行代理。

## 验证结果（命令 + 输出摘要 + 证据路径）

使用 fullstack-quality-gate、frontend-testing-debugging。当前 Browser plugin 不可用，使用已有 Playwright + Edge；未安装任何依赖。固定 Node 24.20.0 / npm 11.19.0；根及 server 的 `npm ls --depth=0 --offline` 退出 0，开工 `npm run test:gate:quick -- --dry-run` 为 READY_TO_RUN，只代表执行计划就绪。

13 条入口的逐项结果及原文见 [本轮验收报告](../../quality-gate/reports/m2c-2-domain-20260912.md) 和 [命令记录](../../quality-gate/reports/m2c-2-domain-20260912/gates.json)。前九项明确 `PFC_DB=memory` 且 DATABASE_URL 为空；PG 两项和新浏览器由工厂校验目标后串行执行。修改源文件另做语法与静态检查，根配置/交付台账检查通过。旧工程 Quick/Core/Full 没有被原型闸代替或冒充。

### 数据设计与清理

- 新领域工厂：精确 schema `codex_test_m2c_20260912_domain`，runId `CODEx_TEST_M2C_20260912_domain`，上限 2,000 行 / 100 MiB。两个合成租户及 owner/executor/viewer/bridge，覆盖版本、状态、权限、并发、负面与故障场景；业务编号由真实分配器产生并记入 createdIds。
- 原 001 回归：精确 schema `codex_test_m2c_20260912_storage`，仍遵守其原 200 行上限。两个专项不并行、不互相清理。
- 文件位于忽略目录 `.local/m2c-2-domain-20260912/files/<runId_attempt>/`；原件、账号配置与 manifest 均为测试工厂所有。配置从忽略 .env.local 读取，不输出密码、URL 或 JWT。
- 数据包含合成文本、1 像素 PNG 和明确不作语义解析的合成二进制；用 10 MiB 原件的多个引用验证 50 MiB 逻辑边界，不能以物理去重绕过单消息上限。无用户资料、真实客户数据或外部写入。
- finally 关闭本轮 API/浏览器/池；按 OID、marker、表白名单、行数、文件清单及绝对路径核归属后清理，独立回读 schema/文件/进程剩余。详细 SQL/文件事实见 domain.json 和 browser.json；只记录合成数据，作为 Git 审计证据保留，运行目录清理。

### 修复及自审记录

本轮使用 TDD/回归驱动。最初实际红灯是旧 PG 自动 DDL、快照写、快照删三个场景，使用 fake Pool 隔绝真实数据库；与 27 号最初拟定的“三个红灯”组合有差异，HTTP 重启和取消晚到随后由真实集成测试覆盖，没有伪造它们先红的记录。

执行中修正：PG 阶段按钮仍受未接入 Unit 阻塞；memory submit 包装改变同步抛错语义；memory token 键兼容；独立 WS 模块恢复调用；大 base64 正则导致栈溢出；旧消息错误套用新附件版本；文件回滚清理与并发复用竞争。对话分页和图片原件预览在交付前补齐；同时修复实时回读丢分页位置、初次健康请求失败时重试动作未挂接，以及连接重试的加载/错误状态。测试自身修正包括 chooser 等待顺序、状态先于 DOM 更新、弹窗遮罩定位、故障注入 SQL 引号和模拟 snapshot 的 stepNo；这些修正均不作为产品功能证据。

曾有上传测试未捕获 Promise 导致提前退出。独立核对 OID 790619、marker 和唯一 attempt 后清理 53 行合成数据，文件/schema/测试进程为 0，见 [异常清理证据](../../quality-gate/reports/m2c-2-domain-20260912/aborted-browser-cleanup.json)。工厂增加脱敏 manifest，以便异常后按事实恢复。没有按前缀批量删除。

代码自审重点是：事务提交顺序、跨租户外键与鉴权、文件清理锁、命令重试/Bridge 派发、取消与 UNKNOWN、PG 禁止整树写、原始附件版本和前端迟到响应。具体最终门禁和截图结论以本轮报告为准；自审不等于陈立人工验收。

## 与交接包的差异（超范围项必须列出）

27 号已确认增补是本轮依据，12/13 保留。业务 schema 例外仅适用于原生 JS/Express 的未来 `pfc_workbench`；旧工程 `pfc`、CAP/Unit 和 M2c-3/4 未被改动。已实现未来精确目标的配置/所有权校验，但本轮没有应用业务 SQL 或初始化账号元数据。对话分页采用最新 100 条 + 上/下一页；行流初始最新 200 条，API 支持 afterSeq 分页。

27 号表格把收据描述为“结果引用”；本实现为保证重试返回完全相同的结果，在 command_receipts.result 保存本条命令的单需求/作业 DTO 快照，而非整个 P.s。这是需本次差异评审知悉的存储实现取舍：版本和材料较多时收据体积会增加，当前不自动清理；长期容量与压缩/归档策略留待实测设计，不以本轮小规模数据证明可长期无界运行。

未增加白名单外的源码或产品契约。对话与 Bridge 均为明确模拟；质量门是模拟记录，没有把 pending 或模拟 pass 视作真实 CI。原有页面仍为唯一入口，不新建替代原型。

## 诚实边界（模拟/未做/待确认）

本轮实现为 **LOCAL_VERIFIED 隔离验证交付，原型交付范围已人工验收**；标准业务仍未 INTEGRATED，正式 CAP/Unit 未 ACCEPTED 或 RELEASED。真实 LLM、Shell/Codex/Zed、CI、PDF/Word/Excel 语义解析未接入；开发登录不是 SSO。PG 未接入治理、项目、知识、测试/缺陷、产品验收、发布、观察及 CAP/Unit 写入，页面明确禁止；local/mock 保留原演示。

本次只验证有界本机数据量、文件边界和 205 行模拟输出，不作生产容量、长期积压或跨浏览器兼容承诺。磁盘故障用不可用目录和 DB 回滚注入，未进行断电/磁盘硬件故障；提交后响应丢失通过同 commandId 重试验证收据语义，未覆盖真实代理网络的全部断点；慢客户端限流有实现保护，未进行真实生产负载验收。正式启用仍需独立观察。

陈立兼任产品、工程、测试和安全负责人已被接受；本次人工结论来自陈立对已展示交付包回复“同意”，不以自动化 PASS 代签，也不推定新增独立安全审计或生产验收。视觉证据为 1280/1440/1920 PC、图片预览和连接失败态；不含移动端验收。

## 下一步（下轮任务参数建议）

1. 本轮差异评审已获“同意”，按本次授权完成验收文档、feature 提交推送、`--no-ff` 合并 main 及推送读回；不再重复索取同一 Git 授权。
2. 日常 PG 启用另行批准下述具体包；未批准前保持原启动方式，不把隔离测试实例留作日常服务。
3. M2c-3 的治理/项目及测试验收等剩余契约按 19/25 单独设计确认，不随本轮评审自动开工。

### 可评审的业务启用包（未执行）

19:11 的只读预检确认本机 PostgreSQL 18、pfc_local、pfc_app_local、127.0.0.1，pfc_workbench schema 与 .local/pfc-workbench-files 目录均不存在，写入次数为 0，见 [预检](../../quality-gate/reports/m2c-2-domain-20260912/activation-preflight.json)。执行前必须重新预检；目标前进则停止，不复用旧“空库”结论。

拟议步骤：在精确 pfc_workbench 创建归属于 pfc_app_local、marker 为 PFC_WORKBENCH_M2C_002 的空 schema；显式运行已测试的 001→002 迁移；单独初始化一个经陈立确认的租户及忽略文件中的本地账号；创建精确本地文件区，容量上限 100 MiB；注入非默认 JWT 与精确授权 schema 配置后启动 loopback 服务。应用启动本身不建表、不建租户、不种业务数据。CLI 为 `node server/scripts/migrate-m2c.mjs --schema pfc_workbench --target-version 002`，所需键见 server/.env.example；001/002 是唯一迁移来源。

该包尚缺业务租户/账号元数据的最终确认，所以不执行，也不在本轮创建替代值。启用前共同备份数据库与文件，旧 pfc/localStorage/历史备份保持原位。建议观察窗 30 分钟，至少一次服务重启和浏览器重开，检查需求/版本/文件 hash/消息/作业恢复及 503、事务回滚、UNKNOWN、事件积压、文件缺失。目标变化、迁移不符、意外写入、权限异常或文件损坏即停。

回退先停写、保留现场。代码回到已验收提交不等于数据回滚；切回 memory 仅为演示。业务 schema 不自动 DROP，数据恢复必须使用同一时点数据库与文件快照并另获授权。首次空库启用出现错误，也先保留证据再决定是否重建。

### 过程与收工

开发开工 17:47，收工时间、最终验证耗时、文件指纹与 Git 结果随本轮报告记录。失败闸与返工来源按上面的实际问题记录；没有发布后指标。无并行代理或依赖安装。本轮测试 helper 由 Codex 创建并负责关闭；原有 PostgreSQL PID 13528 / 5432 保留，本轮未启动新 PG、未删锁，也未停止原实例。曾因沙箱进程探测不可见而启动失败，提升权限只读核实后恢复正确判断。

最终实测：13/13 入口 PASS；领域 36 项（1030 行）、PG 浏览器 15 项（372 行）、原 001 13 项（29 行），均完成精确清理。完整命令、残余风险和截图见本轮报告；feature 收工提交与推送读回在最终交付消息及 Git 记录中核对。

原有 PG 的手动停止命令（仅供用户后续需要时使用，本轮未执行）：

~~~powershell
& "$env:LOCALAPPDATA\PFCPlatform\.tools\postgresql-18.6\bin\pg_ctl.exe" -D "$env:LOCALAPPDATA\PFCPlatform\.local\postgres-data" -m fast stop
~~~
