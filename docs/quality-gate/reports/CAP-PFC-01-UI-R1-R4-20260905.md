# CAP-PFC-01 UI-R1～R4 前端体验优化报告

> 执行时间：2026-09-05 09:52～10:37 +08:00  
> 分类：System Iteration / strict  
> 环境：Windows、本项目 Node `24.20.0` / npm `11.x`、本地 PostgreSQL 18、Microsoft Edge  
> 设计确认：用户确认方案 A“明亮专业工作台”及 UI-R1～R4，并授权引入 `lucide-react`

## 1. 结论

- **UI-R1～R4 本地技术结论：PASS。** 最终 Full 输出 `GATE_PASS full`；1280×720、1440×900、1920×1080 的 24 项 PC E2E 全部通过。
- **真实预览：PASS。** `http://127.0.0.1:5173/` 返回 HTTP 200，1440×900 实拍显示空数据工作台正常渲染。
- **正式验收：BLOCKED。** 责任人现统一登记为陈立；本报告形成时尚未记录对应人工结论，自动化与 AI 自审不替代这些结论。
- **发布：NOT AUTHORIZED。** 本轮没有 commit、push、merge、部署、外部环境写入、API/领域/数据库语义变更。

## 2. 实施结果

| 波次  | 结果 | 主要内容                                                                                                                        |
| ----- | ---- | ------------------------------------------------------------------------------------------------------------------------------- |
| UI-R1 | PASS | 建立三层语义色彩、字号、间距、圆角和动效令牌；改为系统中文字体栈；使用 `lucide-react@1.41.0` 统一操作图标；重构明亮 PC 应用框架 |
| UI-R2 | PASS | 重做筛选、搜索、视图切换、结果计数、更新时间、表格行和空/加载状态；状态统一显示中文，原始代码仅保留在辅助 title 中              |
| UI-R3 | PASS | 重做详情摘要、13 阶段生命周期、门禁/材料/问题/时间线面板、对话框和抽屉；已完成/当前/阻断/未来状态可区分且不只依赖颜色           |
| UI-R4 | PASS | 覆盖键盘焦点、Escape、长文本、无权正文、UNKNOWN 恢复、三档 PC 几何与截图读回；增强文字裁切诊断，排除纯装饰伪元素误报            |

API、领域状态机、数据库 schema、权限和外部集成行为未改变。

## 3. TDD、评审与视觉证据

| 检查      | 结果                                                                                                                      |
| --------- | ------------------------------------------------------------------------------------------------------------------------- |
| TDD RED   | 新增中文状态、结果计数和生命周期状态断言后，因缺少 `共 1 条需求` 按预期失败                                               |
| TDD GREEN | 4 个受影响 UI 单测文件最终 24/24 PASS；Full 中 unit/contract 26 files、133/133 PASS                                       |
| 代码自审  | 按正确性、回归、交互语义、长文本和状态映射复核；修正 2 处过期 E2E 文案断言，未发现未解决的高/中风险代码问题               |
| 安全自审  | `SECURITY_CHECK_OK checks=10 findings=0`；安全测试 5 files、24/24 PASS；依赖审计 0 vulnerabilities                        |
| 视觉读图  | 已读回改造前截图、最终 1280/1440/1920 工作台与详情、1440 影响记录、真实 1440 空数据首页；未见横向溢出、文字截断或控件遮挡 |
| 可访问性  | 关键操作具备可访问名称、focus-visible、Escape 关闭及焦点恢复；状态有文字表达；未执行 axe 或屏幕阅读器人工验收             |

`CAP-PFC-01-T6` 的 full-page 影响记录图会把固定侧栏拼接在浏览器当前滚动位置，并显示被拼接的跳转链接，这是 Playwright 全页截图伪影；实际视口截图和几何断言无重叠。

## 4. 最终门禁

所有 npm 命令均将 `.tools/node-v24.20.0-win-x64` 放在当前 PowerShell `Path` 首位。

| 命令                      | 最终结果                                                                                                                       |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 4 个受影响 UI 单测文件    | PASS，24/24                                                                                                                    |
| `npm run test:gate:quick` | PASS：config、lint、format、types、unit/contract 26 files、133/133                                                             |
| `npm run test:gate:core`  | PASS：Quick、migration plan、integration 9 files 49/49、cleanup `remaining=0`、build、PostgreSQL loopback 读回                 |
| GateRun 三视口定向 E2E    | PASS，6/6                                                                                                                      |
| `npm run test:gate:full`  | **PASS**：权限 44/44、并发 20/20、恢复 31/31、安全 24/24、性能 3/3、Edge 24/24、audit 0 vulnerabilities，输出 `GATE_PASS full` |

生产构建结果：CSS `38.03 kB`（gzip `6.43 kB`），JS `255.49 kB`（gzip `77.26 kB`）。这是本地构建体积，不是生产性能 SLA。

## 5. 浏览器与截图

- Browser 插件未在当前环境提供，按项目规则使用本机 Edge + Playwright，未下载浏览器。
- 自动化视口：1280×720、1440×900、1920×1080；每档 8 项，共 24/24 PASS；无 console error，关键工作流与键盘焦点通过。
- 脱敏截图：`docs/quality-gate/reports/assets/CAP-PFC-01-T3/` 下 6 张工作台/详情图；`CAP-PFC-01-T6/` 下 3 张影响记录图。
- 真实首页截图：`%TEMP%\pfc-platform-dev\ui-r4-live-1440x900.png`，合成本地空数据，不含客户信息，保留至系统临时目录清理。
- Image Gen 工具未在当前会话提供；因未获得使用 API Key CLI 的授权，没有生成概念图。因此本轮可确认代码设计规范与实拍一致，不能声称与生成概念图逐像素一致。

## 6. 依赖与供应链

- 新增唯一依赖 `lucide-react@1.41.0`，精确锁版，ISC；React peer 覆盖 16.5～19；无新增运行时依赖。
- 安装前通过 npm 官方 registry 读取版本、许可证、peer、scripts、仓库和包体积；未运行远程脚本或未固定版本升级。
- 最终 `npm audit --audit-level=high`：0 vulnerabilities。回滚只需移除图标引用、该 workspace 依赖和对应 lockfile 记录。

## 7. 测试数据与外部影响

- unit/API/UI 使用合成 fixture；E2E 使用 loopback Vite 和页面 route mock，ID 遵循 `CODEx_TEST_` + runId。
- PostgreSQL 测试只使用隔离 schema，最终 `TEST_SCHEMA_CLEANUP_OK remaining=0`；未迁移或写入外部数据库。
- 未使用客户数据、真实凭据、cookie 或授权头；日志与截图不含数据库连接串或敏感正文。
- 本轮没有子代理，没有 commit、push、merge、发布或部署。

## 8. 过程记录与残余门槛

- 预期 TDD 红测 1 次；Quick 因 React effect 内同步 loading 更新触发 lint 1 次，已改为事件入口更新。
- 首次 Full 在 6 个 E2E 实例命中旧标题断言；修正后，定向几何测试暴露生命周期伪元素导致的 `scrollWidth` 误报，测试改为按真实文本 Range 检测，未放宽页面横向溢出要求。
- 一次定向 Playwright 命令因重复 `testDir` 路径未找到测试，按仓库配置改用文件名后执行；不计为 PASS。
- 正式接受仍需具名代码评审、测试、安全责任人；PFC-02/04 真实联调、G10 产品验收、G11 发布授权、G12 业务验收和一个工作日观察仍未开始。
- 综合状态：`UI_R1_R4_LOCAL_PASS / FORMAL_ACCEPTANCE_BLOCKED / RELEASE_NOT_AUTHORIZED`。

## 9. 保留进程

| PID                         | 端口             | 用途                           | 停止命令                                          |
| --------------------------- | ---------------- | ------------------------------ | ------------------------------------------------- |
| `21868`（launcher `42744`） | `127.0.0.1:5173` | 用户要求保留的 Vite 前端预览   | `Stop-Process -Id 42744,21868`                    |
| `26532`                     | `127.0.0.1:3001` | 既有本地 API                   | `Stop-Process -Id 26532`                          |
| `24620`                     | `127.0.0.1:5432` | `pfc-postgresql-18` 本地数据库 | 管理员执行 `Stop-Service -Name pfc-postgresql-18` |

前端日志位于 `%TEMP%\pfc-platform-dev\web-ui-r4.stdout.log` 和 `web-ui-r4.stderr.log`。除上述明确保留进程外，Playwright、测试 runner 和临时 Vite 均已退出。
