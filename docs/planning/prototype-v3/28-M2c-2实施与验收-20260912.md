# 28 · M2c-2 实施与验收

> 状态：IMPLEMENTING。陈立在收到 27 号方案 A、逐文件白名单和隔离测试范围的具体确认问题后回复“同意”。本记录承接该授权，未完成部分不得视为通过。

## 本轮改动（文件级清单）

父级 POD-PFC-001 / 原型 M2 / M2c-2；属于 strict 系统迭代。正式 CAP/Unit 状态和台账不变，唯一 POD next route 为 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`。实现进行中，标准业务集成未启用，产品验收与发布未进行。

方案选择 A：当前 Express 使用独立 schema 的设计已确认；本轮仅在隔离测试区实现、验证。白名单逐文件采用 [27 号第七节](27-M2c-2接入方案与范围确认-20260912.md)，不扩目录，不引入依赖。27 的事务、权限、文件、消息和协议增补一并确认。

开工远程 main `05af5ded7f5e3f192f1b7959397f2d46d9d90edd`，方案 `64c1590cc3c3b9a460165db421a227fb98d11f19`；工作区干净，从 origin/main 建立 `feat/m2c-domain` 后将已确认方案并入该 feature。main 不变。

串行计划：

1. 仓储、事务、编号、权限、运行时及 HTTP/SQL 验证。
2. 需求、版本、材料原件、消息引用/Diff 与浏览器刷新恢复。
3. 作业、模拟 Bridge、质量门、回放、租约、通知、提交后事件及重启恢复。
4. 13 条验收入口、独立回读、三档 PC 证据、差异自审、文档、feature 提交推送，交陈立评审。

无并行代理。测试 helper 由本轮 Codex 管理，目的为隔离验证，结束条件是对应命令退出；API/Bridge/浏览器/池在 finally 关闭并回读端口。原有 PG 进程不停止；必要时仅临时启动并恢复本仓库实例。不得把内存演示、隔离验证或模拟输出记为标准业务集成或真实工具效果。

## 验证结果（命令 + 输出摘要 + 证据路径）

开工已读取 AGENTS、19/20/21/23/27 与质量配置，应用 fullstack-quality-gate 和 frontend-testing-debugging。现有 Browser plugin 不可用，采用已有 Playwright + Edge，不安装依赖。Node 24.20.0、npm 11.19.0；根与 server 的 `npm ls --depth=0 --offline` exit 0；`npm run test:gate:quick -- --dry-run` READY_TO_RUN，不是 Quick PASS。

数据设计沿用 27 第八节：两个合成租户、owner/executor/viewer/bridge，正常及边界、冲突、取消、断线、文件/数据库失败场景；确定性工厂生成，API/SQL/hash 独立断言。新测试唯一 schema `codex_test_m2c_20260912_domain`，runId `CODEx_TEST_M2C_20260912_domain`，最多 2,000 行及 100 MiB 文件；根 `.local/m2c-2-domain-20260912/files/`，每次 runId/attempt 子目录必须不存在。旧 001 回归只用 `codex_test_m2c_20260912_storage`，仍限 200 行。目标仅 127.0.0.1 / PostgreSQL 18 / pfc_local / pfc_app_local。

配置从忽略 `.env.local` 读入，不记录凭据；新测试登录配置、文件原件在忽略目录。工厂先核 schema 不存在，再保存 OID/marker；清理前核行数、表清单、文件 manifest 和精确绝对路径，独立读回 remaining=0。报告只含合成数据与脱敏错误。失败保留证据，不清理非本轮对象。

先运行三个预期失败场景：PG 反 seed/快照写、HTTP 创建后跨进程读回、取消晚到事件终态保护。后续执行 27 中 13 条入口和根配置/台账、源码静态检查。当前尚未执行新增测试，不预填 PASS。

## 与交接包的差异（超范围项必须列出）

27 号已确认增补是本轮依据；12/13 保留原文。旧工程、001 SQL、历史备份/证据、依赖和正式台账不改。若必须新增未列文件或语义，先说明具体差异再对齐。

## 诚实边界（模拟/未做/待确认）

开发登录不是 SSO；会话和 Bridge 仍是模拟；二进制原件保存不等于语义解析。PG 未接入的治理/项目/测试验收/发布观察等写入口按方案禁用，不以本地快照兜底。日常 pfc_workbench、真实 LLM/工具、外部环境、main 合并均未授权执行。

## 下一步（下轮任务参数建议）

完成上述四检查点后，提交具体 SQL/差异/回退和隔离验证证据供陈立分别进行产品、工程、测试、安全评审。业务启用需要最后的目标预检及确认；启用、观察窗、发布状态不得提前填报。

过程指标：当前开工，返工/失败闸随真实执行记录；未产生发布后指标。
