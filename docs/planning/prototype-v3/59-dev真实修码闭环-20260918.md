# 59 号：dev 阶段真实修码闭环（验收）

- 日期：2026-09-18
- 目标：命中 codex 六项纠偏第 1 项验收标准——「实际修改 → 测试失败 → 修复 → 通过」的真实闭环
- 状态：**PASS**

## 一、闭环设计

在授权工作区搭建**带缺陷的最小 Node 项目**，由真实 codex 会话按冻结计划完成修复：

| 文件 | 初始（缺陷） | 修复后 |
|---|---|---|
| `src/add.js` | `module.exports = (a, b) => a - b;`（错误） | `export default function add(a, b) { return a + b; }`（正确） |
| `test/add.test.js` | `node:test` 断言 `add(1,2) === 3`（会失败） | **未被修改**（防作弊校验） |

冻结计划：`allowedFiles=['src/**','test/**']`、`allowedCommands=[['node','--test']]`、严格模式 + 基线哈希。

## 二、执行证据（verify-59-fix-loop.cjs，FIX_LOOP_VERDICT PASS）

真实需求 **R-1066 / CODEx_TEST_59_FIX_292002**，全链路：创建 → 澄清（SQ-57/58）→ 版本确认 → proposal(AP) → adopt(AG) → confirm-business（→design）→ confirm-design（→dev）→ **EXEC 真实作业**（jobId 7ae79c07-b1e2-493d-a0f9-a8355f982866）。

```
pre-check 缺陷测试 exit=1（红，符合预期）   ← 测试先失败
EXEC 作业：真实 codex 会话按计划执行
VERIFY:
  msgStatus:   ok        ← AI 消息回写完成
  addChanged:  true      ← src/add.js 被真实修改（哈希变化）
  addCorrect:  true      ← 修复为正确实现（a+b，非 a-b）
  testUntouched: true    ← 测试文件未被作弊修改
  greenExit:   0         ← 本地独立复跑 node --test 真实通过
```

## 三、平台能力证明（工具链闭环）

1. **pfc_read_file**：真实读取 src/add.js 与 test/add.test.js，返回内容 + sha256（作为后续写入 expectedHash）
2. **pfc_write_file**：基线校验（expectedHash == 当前哈希）→ 写入正确实现 → 写后哈希复核（FILE_WRITE_UNVERIFIED 防伪）
3. **codex_cli**（terminal 分支）：执行计划内命令向量 `['node','--test']`，deny 优先 + 精确匹配
4. **消息回写**：真实会话结果写入 AI 消息（SMSG-38，status=ok）

## 四、调试中定位的边界（已固化到脚本）

- 工作区目录名必须匹配授权模式 `CODEx_TEST_AI_FIX_20260916_<uuid>`（exec-control.checkedWorkspace），否则 WORKSPACE_NOT_AUTHORIZED
- 项目根 `package.json` 为 `"type":"module"`，工作区 `.js` 被按 ESM 解析：测试文件须用 ESM 语法（`import`），否则 `require is not defined`
- Node 24 `node --test test/`（带目录参数）会把目录当模块加载失败（MODULE_NOT_FOUND）→ 用 `node --test` 自动发现
- 模型可自主调整实现风格（CJS→ESM），独立验证按语义校验（正确实现 + 测试未动 + 真实通过），不绑定初始风格

## 五、运行方式

```
前提：5188 全套 env 运行中（含 PFC_CODEX_* 真实连接）
node server\verify-59-fix-loop.cjs
```

预算：单次 EXEC 消耗 1 turn（上限 80，快照 remaining 见 budget-snapshot.cjs）。
