# 原版工作台原型复核与微调

日期：2026-09-11。结论：**采用用户指定的原版 `index.html` 作为后续交互基线，在原文件就地修复；定向浏览器回归 13/13 通过。**正式平台继续沿用该交互接入真实服务，V3 保留为对照稿。

## 范围与基线

- 用户依据：指定 `output/pfc-workbench-prototype/index.html`，认可截图中的交互，并要求评估直接沿用、有问题在原版微调。
- 分类：独立演示原型的局部 UI/交互修复，同时更新规划文档中的基线指向。没有恢复正式平台业务开发。
- 采用方式：保留原版三栏布局、四个导航入口、八阶段浏览、结构化对话卡片、流式回复、AgentRun 进度、Zed 对话流、右侧终端/画布/证据及治理中心。不将原版替换为 V3 页面。
- 修复前先复现：10 组检查中 9 组失败、桌面布局检查通过；随后按相同场景回归，并增加运行完成、连续发送和原交互保留检查。
- 文件所有者：本次由 Codex 单线程修改 `index.html` 和规划/核验文档；未使用子代理，未改应用源码、数据库、迁移、配置和交付台账。
- 备份：`output/pfc-workbench-prototype/_backup/index-original-before-20260911.html`，与修改前源文件字节一致。回退时可将该文件复制回 `index.html`；本次未执行回退。

| 文件             | SHA-256                                                            |
| ---------------- | ------------------------------------------------------------------ |
| 修改前原版及备份 | `640769ee9ab8a5c9338c733f7932e2b29c4e8f5e6782d9ccedb3ad4548f6ccb8` |
| 最终原版         | `441230ba98ec8da6cd0ac8fa5415f2ef35996ce9da10ae41275802cbeb09602e` |

V3 的 HTML、JS、数据和 CSS 四个文件保持此前指纹，未被覆盖。

## 修复与可见结果

| 问题                                                         | 修复后行为                                                           |
| ------------------------------------------------------------ | -------------------------------------------------------------------- |
| 右侧标签 SVG 约 96px，文字被挤成竖排；其他位置图标缺失       | 统一基础 SVG 为 16px，保留已有局部尺寸，标签横排                     |
| 停止运行被改为完成、100%，终端仍可能追加输出                 | 显示“已停止”，保留百分比及步骤，停止进度和终端计时器                 |
| 运行步骤/完成标记未正确更新                                  | 使用已有卡片的实际定位，完成时更新状态；中央和右侧终端共用同一组输出 |
| 换需求后右侧仍固定显示 R-102 / pfc-notify                    | 终端、工作区、状态、证据和上下文随当前需求读取                       |
| 所有“查看完整 Diff”打开固定开发内容，确认按钮指向不存在的 d9 | 画布读取点击的当前阶段草稿，确认指向该版本；没有草稿时明确显示状态   |
| 输入草稿及阅读位置在切换面板时丢失                           | 草稿按需求隔离；同一需求/阶段重绘保留输入与滚动位置                  |
| 中文输入法确认回车触发发送，快速连发留下未完成回复           | 组合输入不提交，普通 Enter 发送；连续发送时完成前一条演示回复        |
| 澄清问题用 question ID 查找卡片 ID，点击无反应               | 根据问题归属查找卡片；点击和键盘激活均可选中演示回答                 |
| 浏览阶段时将所有前置阶段标成通过                             | 当前阶段来自需求记录，浏览阶段单独标识；查看页面不推进当前阶段       |
| 切换需求弹窗无法用 Esc 关闭                                  | Esc 关闭弹窗并返回工作区操作入口                                     |
| 提示文字覆盖了图标，提示层阻挡标签点击                       | 文字写入正确节点，提示层不拦截鼠标事件                               |
| 原型确认提示声称已写 PostgreSQL                              | 改为明确的当前页面演示结果                                           |

## 验证环境与数据

Windows，项目 Node 24.20.0 / npm 11.19，现有 Playwright 和独立无头 Microsoft Edge。使用本地文件入口；无依赖安装、平台服务或数据库连接。

Browser 插件未列出，按 `frontend-testing-debugging` 使用现有 Playwright。浏览器初次在沙箱内启动报 `spawn EPERM`；经自动审批在沙箱外执行同一本地检查。页面 HTTP/HTTPS 请求全部阻断。没有真实 Shell 业务作业、AI 调用或外部写入。

数据设计：复用原型内 R-1042 / R-1031 / R-1018 的合成剧情；新增输入均为 `CODEx_TEST_ORIGINAL` 前缀。最小确定性场景构造器 `fresh()` 每组重新载入原型，使用固定浏览器时钟控制运行、停止及流式回复；覆盖运行/停止/完成、需求隔离、版本关联、输入边界、键盘与三档宽度。无产品记录 createdIds，测试状态只在独立浏览器上下文中，最终销毁。执行与复核：Codex；视觉和产品确认人：陈立，微调结果尚待用户查看。

## 实际命令与结果

所有命令在仓库根目录使用项目 runtime 执行。

| 命令/检查                                                                                                                                             | 结果                                                           |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| `.\.tools\node-v24.20.0-win-x64\node.exe "$env:TEMP\pfc-original-review.mjs"`                                                                         | 最终退出码 0，13/13 组 PASS，页面/控制台错误 0，页面外部请求 0 |
| `node --input-type=module -e …`，用 `vm.Script` 解析 `index.html` 唯一内联脚本                                                                        | `INLINE_SCRIPT_SYNTAX_OK`                                      |
| `npm run check:delivery-governance`                                                                                                                   | PASS，5 CAP / 26 Unit，M2，下一路由仍为 R3a                    |
| `npm run test:gate:quick -- --dry-run`                                                                                                                | `READY_TO_RUN`；仅核对计划，不计 Quick PASS                    |
| `node node_modules/prettier/bin/prettier.cjs --check docs/planning/prototype-v3/*.md docs/quality-gate/reports/ORIGINAL-PROTOTYPE-REVIEW-20260911.md` | PASS                                                           |
| `git diff --no-index --check -- <backup> <index.html>`                                                                                                | 返回 1（存在差异），未报告空白错误；有仓库 CRLF 转换提示       |

没有重新执行整个平台 Quick/Core/Full；此前规划核验发现的 5 处 MCP 类型错误仍未修复，不能以本次原型结果关闭正式工程门禁。

| 组  | 场景、主要步骤和预期                                                                | 结果 |
| --- | ----------------------------------------------------------------------------------- | ---- |
| 01  | 进入开发页，标签图标不超过 32px且文字正常排列                                       | PASS |
| 02  | 8% 停止，再推进测试时钟；仍是已停止和原进度，终端不再增长                           | PASS |
| 03  | 输入草稿、滚动对话、切证据；输入与阅读位置保留                                      | PASS |
| 04  | 中文组合 Enter 不增加用户消息                                                       | PASS |
| 05  | 切换 R-1031，终端及证据显示 R-132，且无 R-102 / pfc-notify 串入                     | PASS |
| 06  | 在需求阶段点击 d2 Diff，显示该需求文档，不显示固定开发文件                          | PASS |
| 07  | 点击澄清问题 q1，选中数量变为 1                                                     | PASS |
| 08  | 查看发布阶段，实际阶段仍为开发，已通过节点仍为 3 个                                 | PASS |
| 09  | 打开切换需求弹窗，Esc 关闭                                                          | PASS |
| 10  | 演示运行完成为 100%，停止按钮消失，双终端输出相同且无运行光标                       | PASS |
| 11  | 跨需求草稿隔离；HTML 字面量安全显示；普通 Enter 和连续发送无挂起回复                | PASS |
| 12  | 临时能力面板、阶段绑定、审计、产品空间占位、首页和返回工作区仍可访问                | PASS |
| 13  | 1280 / 1440 / 1920，开发页、Diff、首页和治理中心可显示；页面不横向溢出，顶栏为 52px | PASS |

页面身份和非空内容已通过本地入口、标题源码、DOM及截图核对。原型是独立 HTML，无框架错误遮罩。已实际查看开发页、Diff、首页和治理中心截图；保留原版 252px 上下文栏、368px 右侧面板与既有内容宽度。图标尺寸是修复前后主要可见差异。

## 证据、限制与交接

- 最终证据目录：`C:\Users\hz19114673\AppData\Local\Temp\pfc-original-review-R7jmBk`，含 `report.json` 和 13 张截图。首次有效失败复现保存在 `pfc-original-review-h89NRP`。证据只含合成原型与测试输入，保留供用户复核，之后可清理，不纳入提交。
- 回归脚本：`C:\Users\hz19114673\AppData\Local\Temp\pfc-original-review.mjs`。本轮未增加正式业务测试程序。
- 浏览器在 `finally` 中关闭；独立进程读回结果为 `NO_RETAINED_PROTOTYPE_TEST_PROCESSES`。没有新增常驻服务、监听端口或需用户停止的进程。
- 保留的演示边界：刷新会重新载入剧情，AI 回复和测试/发布结果为模拟，产品空间为占位；全局搜索、附件、完整产物编辑、能力策略及发布状态流仍需后续设计与接入。临时能力的作用域和“审批后直接观察”的旧示例不能照搬为正式契约。不能将本次 13 组回归表述为全功能完成或原版所有交互已验收。
- 浏览器覆盖为 Edge PC；其他浏览器、移动端、真实输入法及真实 Bridge 未验证。
- 建议直接沿用视觉和交互，正式实现按组件接 API 与同一 AgentRun 事件源。Shell/文件/Git 经本地 Bridge 执行是目标基础能力，终端展示不能替代真实执行证据。
- Parent POD：`POD-PFC-001`；里程碑 M2。CAP/Unit 本轮无业务实现推进；原型为局部验证通过，平台集成/验收/发布状态不提升。唯一下一路由继续为 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`，等待恢复平台实施后承接；无提交、推送、部署和发布。
