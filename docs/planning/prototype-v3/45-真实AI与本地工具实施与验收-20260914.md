# 45 · 真实 AI 与本地工具实施与验收

> PARTIAL / BLOCKED_CONTEXT_CONFIRMATION。44号A方案已确认并进入开发。当前完成连接预检、材料解析和文本会话适配的局部实现；真实模型调用为0，数据库/API/UI集成及完整验收未完成。唯一待确认项是本页所列全局开发规则的额外外发范围。

## 已确认范围与基线

父级POD-PFC-001，里程碑M2；全部5CAP/26Unit保留，正式唯一next route仍为 `POD-PFC-001/M2/R3a/artifact-collaboration-tdd`。采用[44号](44-真实AI与本地工具闭环方案及范围确认-20260914.md)A/D1–D8/C1–C4：145条候选源码/测试/依赖路径、七份实施文档、007隔离迁移、三项固定解析依赖、20次合成模型调用、限定本地工作区/测试schema及43条验收闸。用户对该具体包回复“继续”，[授权记录](../../quality-gate/reports/ai-tools-integration-20260914/authorization-20260914.json)保存原方案与范围指纹。

设计提交2f90fa1；实施分支feat/ai-tools-integration从main a20e16e建立，开工8321c64。两次fetch因网络失败，同一origin的ls-remote随后确认main仍为a20e16e且本地对象齐备。带入43个既有文档/证据路径；42/43及历史证据字节保持，正式台账/根依赖/旧SQL只读。方案分支此前push被自动审批拒绝，未绕过；本轮仅保存局部实现WIP，不推送、不合并main。

## 连续实施计划

| 步骤 | 内容                                                                  | 当前状态                                   |
| ---- | --------------------------------------------------------------------- | ------------------------------------------ |
| C1   | bootstrap、供应链审查、协议/账号/隔离预检、状态/权限测试、007隔离迁移 | PARTIAL；额外指令外发范围待确认，007未开始 |
| C2   | 材料理解、真实对话、原型/PRD/AC联产及版本承接                         | 解析器和文本适配器已写，尚未接入PG/API/UI  |
| C3   | 项目/Skill装配、实际Shell/文件操作、可信测试结果与恢复                | NOT_STARTED                                |
| C4   | PC完整链路、原35+新8闸、差异评审、清理与交付                          | NOT_STARTED                                |

节点完成后连续推进，不按检查点重复请求整个开发包。当前实现PARTIAL、真实集成NOT_COMPLETED、验收PENDING；个人007启用、发布、烟测和观察均尚未发生。OCR、Zed双向接管、通用MCP、复杂任意HTML、团队身份、远程CI/部署等仍按44保留在完整产品范围，不暗自删减。

## 实现与验证事实

当前源码范围为两份server依赖文件及九份新增JS：连接配置/协议、材料解析、文本会话、确定性工厂、protocol/materials/model/real四个验证入口。尚未修改现有业务服务、HTTP路由、UI或SQL。

- `node server/verify-ai-tools-protocol.mjs`：9组PASS。版本/账号/连接指纹、MCP启动隔离、脱敏及配置来源检查。实际 `--live` 预检通过，CLI0.154.0、apiKey、gpt-6-astra和模型列表匹配；这不代表真实生成或工具能力可用。
- `node server/verify-ai-tools-materials.mjs`：13组PASS。实际提取TXT/MD/JSON/CSV/HTML/PDF/DOCX/XLSX合成内容，保留原件sha256及页/段/单元格位置；图片仅NEEDS_VISION。损坏、非法UTF8、外部引用、路径穿越、解压倍率/体积与截断均有断言，worker限128MiB堆和10秒。
- `node server/verify-ai-tools-model.mjs`：7组PASS，仅文本会话策略子集。覆盖输入限额、额外上下文拒绝、额度预留失败无请求、跨线程/迟到旧turn事件、失败关闭与终态约束。完整领域状态、权限、审批及持久化闸未完成。
- `node server/verify-ai-tools-real.mjs`（未传--generate）：实际非生成会话准备在THREAD_CONTEXT_UNVERIFIED处阻断，0turn。53个Skill逐项禁用、全部MCP和文本工具/记忆关闭，read-only且工具网络关闭；但线程仍载入1份全局AGENTS.md。

安装pdfjs-dist6.3.289、mammoth1.12.3、read-excel-file9.3.10，均使用固定版本、ignore-scripts、omit-optional；原依赖版本未升级。候选三包锁audit为0漏洞；实际server锁audit退出1，有原express/qs链2项moderate、0high/critical，新解析包无告警。未擅自升级旧依赖，不能称全依赖安全PASS。

初始缺失模块/功能、错误连接判定、配置字段来源及迟到事件覆盖均先有失败输出再修复。精确lint/format、配置/正式交付/旧协议源检查、文件指纹与进程读回见[检查点报告](../../quality-gate/reports/ai-tools-integration-20260914/checkpoint-report.md)。Quick dry-run只记READY_TO_RUN；原35+新8共43闸未完成，没有业务验收PASS。

## 已查明的兼容差异

空mcp_servers={}会合并本机配置，曾实际继承3个MCP。自动审批拒绝先启动AppServer再发现名单的方案；现已改为先通过纯配置mcp list --json提取名单，再启动唯一全部禁用的实例，更安全替代已获审查并实测0个启用MCP。原被拒动作未执行或绕过。

ToolsV2响应会省略view_image字段；不能把字段缺失当关闭。现在仅在有效origin确认为sessionFlags且配置层version一致、值确为false时确认关闭，并加入错误版本/缺来源回归。

此前仅因自定义provider而请求再次确认过严：44第六节本来就允许使用本机现有Codex身份。[授权口径复核](../../quality-gate/reports/ai-tools-integration-20260914/authorization-reconciliation.json)已撤回该重复问题；当前连接指纹为b585d723d61d18a815b77a2d81627cd71038d1f8cf7f4446f56e48eba4937292，任何连接变化仍阻断。

## 当前唯一待确认：全局指令外发范围

thread/start在project_doc_max_bytes=0后仍返回 `C:/Users/hz19114673/.codex/AGENTS.md`：28443字节，SHA256 `b77f388f6dcbbc729550c5a3ac73d91a68264e33421173ba02c337390d3f892c`。该文件是全局开发方法规则，未在原44合成材料清单中；它会成为模型上下文，不能仅靠提示词声称已隔离。因此当前在turn/start前停止，未发材料、未消耗模型turn额度。

推荐补充A：仅本次44、仅已批准现有连接，允许这份路径与SHA均匹配的全局规则随合成请求发送。它不扩大工具权限；每次发送前核对，内容变化或出现额外指令立即阻断。原20次/单次300秒/累计60分钟及其他数据边界不变。确认后补充路径/指纹回归与真实文本验证，再继续C1–C4。

备选B：维持纯合成材料边界，另拟独立运行身份方案；账号隔离与本人登录需另行明确，不复制凭据或修改现有全局规则。这属于实际上下文隔离的方案差异，不是未登录或供应商不可用。

两项均未代用户选择。可审查内容见[范围补充](../../quality-gate/reports/ai-tools-integration-20260914/context-exception-proposal.json)；复现为real-text-1789351007938.json。[0.154.0官方实现](https://github.com/openai/codex/blob/rust-v0.154.0/codex-rs/core/src/agents_md.rs)先接收宿主用户指令，再按project_doc_max_bytes读取项目指令，与实测一致。

## 数据、进程与交接

仅使用CODEx_TEST_进程内合成工厂，无测试schema或个人业务库写入、无模型turn。全局规则只读核对名称/大小/sha256，未外发、未修改；日志不持久化原始配置/凭据/规则内容。九份JS是局部候选实现，不部署到5188。

临时公共依赖锁/npm缓存/日志在被Git忽略的 `.local/ai-tools-integration-20260914/preflight/`，为同轮续作保留，最终由Codex按所有权清理；没有个人原件或凭据副本。各短时AppServer与材料worker均关闭并独立读回，未创建子代理或常驻服务。

个人5188仍为006、同一instanceId、realTools=false，保留API19412/controller14924；停止命令为固定Node执行 `server/scripts/local-workbench.mjs stop`。原PG13528/5432、旧API31152/3001、Vite37744/5173保留，本轮不操作其生命周期。

陈立继续承担产品/研发/代码评审/测试/安全/验收各角色，Codex已自审局部正确性、范围和敏感数据流。产品验收需待完整链路可操作后进行，不由局部测试或本地WIP提交代替。未测风险包括真实模型内容质量/费用、Windows工具沙箱、PG持久化与恢复、浏览器可用性及完整43闸。
