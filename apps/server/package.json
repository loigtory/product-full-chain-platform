{
  "name": "@pfc/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env.local --import tsx src/main.ts",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@pfc/contracts": "0.0.0",
    "@pfc/domain": "0.0.0",
    "@pfc/persistence": "0.0.0",
    "@pfc/protocol": "0.0.0",
    "@fastify/helmet": "13.1.1",
    "fastify": "5.12.1"
  }
}
