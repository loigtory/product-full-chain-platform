# 58 号：六项纠偏收口与端到端真实闭环（验收）

- 日期：2026-09-18
- 范围：58-A 源码级验证（六项）＋ 58-B 真实 skill 验证 ＋ 58-C 端到端真实闭环 ＋ 回归/文档/提交
- 状态：**完成**

## 一、背景

codex 接力评审提出六项纠偏（详见 44/45 号记录），58 号分三步收口：

| 步骤 | 内容 | 结论 |
|---|---|---|
| 58-A | 六项纠偏源码级证据（verify-58-controls.cjs） | 32/32 PASS |
| 58-B | 真实 skill 验证（rdc-code-review、cha-estimate-workload） | PASS |
| 58-C | 端到端真实闭环（真实需求→真实 codex 会话→命令真实执行→消息回写） | PASS |

## 二、58-A：六项纠偏源码级验证（32/32 PASS）

重跑命令：`node server\verify-58-controls.cjs`（cwd 项目根）。

| 纠偏项 | 证据（源码级断言） |
|---|---|
| 1.「能执行命令」≠「完成开发闭环」 | EXEC 作业经 deriveExecControl(54号) 派生受控计划；execution-policy 能力闸；tool_executions 全 SUCCEEDED 才允许回写；diff 越界拒绝 |
| 2. 真实执行绕过范围审批 | EXEC 完整范围审批链：冻结计划上下文哈希校验、审批人/租户/需求归属校验、基线哈希、越界 diff 拒绝、命令白名单精确匹配 |
| 3. 停止/恢复未接通真实任务 | requestCancel → worker.cancel → 300s 超时一致；取消/超时错误映射完整；恢复按消息状态 + 作业租约恢复 |
| 4. 预算两本账 | 账本统一到根 `.local` 单一路径（budget-snapshot.cjs 全量对账）；budget.js 不再依赖启动目录 |
| 5. 验收入口高估 | run-ai-tools-gates 带 `--pg` 执行数据库部分；`currentSourceVerified:false` 必须现场重算；无 KNOWN_BRANCH_DIFF 豁免 |
| 6. 交接/证据保护失步 | AGENTS、入口文档、统一范围清单同步更新；历史证据可从 Git 追溯 |

## 三、58-B：真实 skill 验证（PASS）

- **rdc-code-review**（公司 skill）：对 apps/web 83 文件全量审查 → RESULT PASS，L3×1（StageCapabilityConfigPanel useEffect eslint-disable）→ `docs/quality-gate/reports/58-skill-validation/20260918-rdc-code-review.md`
- **cha-estimate-workload**（公司 skill）：对 55 号需求拆 8 功能点合计 81H → `docs/planning/prototype-v3/工时估算_阶段能力配置中心_20260918.md`

## 四、58-C：端到端真实闭环（PASS）

### 4.1 全链路流程（verify-58-e2e.cjs，PASS）

真实需求 **R-1065 / CODEx_TEST_58_E2E_086586**：

1. 创建需求（idea）→ 自动澄清问题
2. 回答澄清（SQ-49/SQ-50）→ 确认版本 → 推进 req
3. artifact：proposal(AP-11) → adopt(AG-10) → confirm-business（自动推进 design）
4. confirm-design（自动推进 dev）
5. EXEC 作业（jobId 0fc22f34-576b-4c48-b7d7-00e76fa4dfd6）→ 真实 codex 会话执行 → AI 消息 SMSG-32 回写

### 4.2 命令真实执行证据（SMSG-32 内容抽查）

```
git status --short：退出码 0；发现 5 个已修改文件、7 个未跟踪项
node --version：退出码 0；版本为 v22.23.2
```

两条计划内命令均**真实执行并回写**（此前三轮被 `COMMAND_NOT_IN_PLAN`/无工具入口拦截，已修复，见 4.3）。

### 4.3 58-C 顺带修复的源码缺陷（5 处）

| 文件 | 缺陷 | 修复 |
|---|---|---|
| `server/src/persistence/agent-jobs.js` | 版本门 `['007','008']` 缺 009/010 → 真实作业 503 | 版本门扩到 010 |
| `server/src/agent/protocol.mjs` | VERSION=`0.154.0` 与真实 codex(0.153.4) 不符 → PROTOCOL_VERSION_MISMATCH | VERSION=`0.153.4`（与实装 CLI 绑定） |
| `server/src/agent/host-tools-registry.js` | core 命令工具（pfc_run_checks/pfc_git_status/pfc_git_diff）与 terminal(codex_cli) 并存，模型误选后固定向量与精确向量不匹配 → 命令被拒 | host 会话 core 工具仅保留 pfc_read_file/pfc_write_file；命令执行统一走 codex_cli（计划内命令向量） |
| `server/src/agent/worker.js` | `options.extraHostTools = resolveHostTools(names)`（对象数组）→ hostThreadParams 内二次 resolveHostTools 把对象 String 化丢弃 → codex_cli 未注入 | 传原始名称数组，由 hostThreadParams 统一白名单解析 |
| `server/seed-55-capabilities.cjs` | dev 阶段仅 tool「开发终端」（无 registry 映射）+ mcp codex-cli，enabledHostToolsForStage 只认 tool → codex_cli 不注入 | dev 阶段新增 tool `codex-cli`（priority 5，registry 别名映射 codex_cli） |

### 4.4 真实 codex 连接配置（环境变量）

```
PFC_CODEX_BINARY = C:\Users\hz19114673\AppData\Local\OpenAI\Codex\bin\8e5b6932251c2c1c\codex.exe
PFC_CODEX_BINARY_SHA256 = e5aa76d19c7c94e2e9ef9b707d590206a73ac0e97c8ddc8382181242494bef75
PFC_CODEX_PREFLIGHT_CWD = <授权根 .local\ai-tools-*\workspace>
PFC_CODEX_CONNECTION_SHA256 = b585d723d61d18a815b77a2d81627cd71038d1f8cf7f4446f56e48eba4937292
```

自检：`node server\.local\q-preflight.cjs` → `READ_ONLY_PRECHECK_READY` / providerConfirmed=true。

## 五、回归（全绿）

| 回归 | 结果 |
|---|---|
| verify-58-controls | 32 PASS / 0 FAIL |
| verify-55-worker | 6 PASS / 0 FAIL |
| verify-56-exec-tools | 15 PASS / 0 FAIL（断言同步 58-C 新语义：core 2 + codex_cli） |
| verify-57-exec-terminal | 14 PASS / 0 FAIL（同上） |

## 六、58-C 后的 EXEC host 会话工具语义（当前定稿）

- **core 恒在**：pfc_read_file / pfc_write_file（文件 IO 安全基线）
- **命令执行唯一入口**：codex_cli（terminal 分支）——携带冻结计划内命令向量，deny 优先 + 计划精确匹配 + 超时进程树终止
- **dev 阶段默认启用**：tool codex-cli（seed-55 已补）；页面「阶段能力」可热插拔（55 号）
- **版本绑定**：platform 协议版本 = 本地实装 codex CLI 版本（0.153.4）；更换 CLI 需重算 SHA256 + 连接指纹

## 七、已知边界

- EXEC 会话工具集为 3 个（read/write/codex_cli）；zed/vscode 终端仍 PENDING_HOST_SUPPORT（57 号后续）
- 真实命令仅在冻结计划 allowedCommands 精确向量内可执行；越界（含计划外新命令）由模型要求新计划（requiresNewPlan）闭环
- 命令输出中"允许清单外命令痕迹"警告由 command-runner.auditCommands 输出（如 `git status --short` 在工作区含清单外进程痕迹时）
