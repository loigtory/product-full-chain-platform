# PFC 工作台原型 V3 验证记录

日期：2026-09-11。范围：`output/pfc-workbench-prototype` 下新增的 V3 文件。结论：本轮定向验证 PASS，11 组浏览器回归全部通过；不代表平台 CAP/Unit 业务集成、验收或发布通过。

## 基线与环境

- 以原版 `index.html` 为视觉与演示材料基线，保留原文件。
- 原版开始与结束时 SHA-256 相同：`640769ee9ab8a5c9338c733f7932e2b29c4e8f5e6782d9ccedb3ad4548f6ccb8`，100992 字节。
- Windows、本仓库 Node 24.20.0 / npm 11.19、现有 Playwright 与 Microsoft Edge。无依赖安装。
- CUA 无可用浏览器，使用现有 Playwright 启动独立无头 Edge。浏览器启动经自动审批在沙箱外执行；页面只读取本地原型，HTTP/HTTPS 请求全部阻断。
- 本次没有调用应用 API、数据库、真实 AI、Git 远端或外部工具。

## 实际执行

以下命令均从仓库根目录执行。

| 命令 | 结果 |
| --- | --- |
| `.\.tools\node-v24.20.0-win-x64\node.exe output/pfc-workbench-prototype/verify-v3.mjs --baseline` | 预期 FAIL：原版停止后未显示“已停止”，复现回归问题 |
| `.\.tools\node-v24.20.0-win-x64\node.exe output/pfc-workbench-prototype/verify-v3.mjs` | 最终 PASS：11 / 11 组，页面错误 0，外部请求 0 |
| `.\.tools\node-v24.20.0-win-x64\node.exe --check output/pfc-workbench-prototype/prototype-v3.js` | PASS |
| `.\.tools\node-v24.20.0-win-x64\node.exe --check output/pfc-workbench-prototype/prototype-v3-data.js` | PASS |
| `.\.tools\node-v24.20.0-win-x64\node.exe --check output/pfc-workbench-prototype/verify-v3.mjs` | PASS |
| `.\.tools\node-v24.20.0-win-x64\node.exe node_modules/prettier/bin/prettier.cjs --check output/pfc-workbench-prototype/index-v3.html output/pfc-workbench-prototype/prototype-v3.js output/pfc-workbench-prototype/prototype-v3-data.js output/pfc-workbench-prototype/prototype-v3.css output/pfc-workbench-prototype/verify-v3.mjs` | PASS |
| `npm run test:gate:quick -- --dry-run`（使用项目 Node） | READY，仅检查计划，不计为门禁 PASS |

本轮为独立原型完善，未执行整个平台 Quick/Core/Full，也没有改变其结论。

## 场景与断言

共同前置条件：独立 Edge 上下文载入本地 V3；使用原型合成数据。共同步骤与断言由 `verify-v3.mjs` 执行。执行与证据整理：Codex；原型体验与视觉确认人：陈立，待确认。

| 组 | 场景、步骤与预期 | 结果 |
| --- | --- | --- |
| 01 | 8% 停止、刷新：保持“已停止”和 8%，不可直接推进 | PASS |
| 02 | 浏览未来发布与历史需求：实际阶段仍为开发，未来审批禁用，已完成阶段数量不变 | PASS |
| 03 | 切换需求：画布/证据关联 R-132 与 pfc-claim；刷新恢复对应需求、标签与输入，返回 R-1042 不串输入和任务 | PASS |
| 04 | 空回答不能保存；未回答不能确认；回答同步草案；确认后刷新不重复记录 | PASS |
| 05 | 修改生成 v2；v1 内容保留；对比展示正确版本；HTML 输入作为纯文本；已确认版本不可编辑；聚焦可退出 | PASS |
| 06 | 从想法到复盘完整走通；停止保留已完成步骤；前置条件阻断推进；批准后仍未执行；独立执行后方可观察；已完成事项移出首页待办 | PASS |
| 07 | 临时装载仅作用于当前需求；阶段绑定按钮可切换；全局停用不删除绑定但阻止装载；恢复默认正确生效 | PASS |
| 08 | 中文组合 Enter 不发送；Shift+Enter 换行；普通 Enter 发送；发送不执行任务；弹窗焦点留在弹窗且 Esc 返回；标签方向键、刷新、浏览器后退与搜索正确 | PASS |
| 09 | 1280 / 1440 / 1920 × 首页/工作区/文档/治理中心：无页面横向溢出，导航保持 52px 单行，图标不超过 32px；1280 抽屉可关闭及 Esc 退出 | PASS |
| 10 | 取消重置保留记录；确认重置仅删除 V3 存储；损坏存储恢复示例；不触碰其他 key | PASS |
| 11 | 原版文件 SHA-256 与基线一致 | PASS |

## 回归中修正的问题

原版停止语义先复现失败，再在 V3 验证通过。过程中发现并修正了标签方向键的目标计算、1280 上下文抽屉缺少可点击关闭入口、已完成需求仍出现在固定首页待办，以及宽屏对话与卡片未对齐。另修正了回归脚本在需求切换渲染完成前过早断言的问题；最终执行已等待目标需求标题出现。

已实际查看三档桌面的代表性截图，包括首页、开发工作区、验收文档、治理目录与聚焦阅读；图标、主要按钮、文档及输入区未见遮挡。自动检测和截图自检不能替代用户对视觉方向与交互体验的确认。

## 数据、证据与清理

- 测试需求：R-1042 / R-1031 / R-1018 / R-1056，均为原型合成数据。新增 R-1056 用于从想法开始走通流程。
- 测试输入前缀：`CODEx_TEST_PROTOTYPE_V3`；覆盖正常、空值、中文组合、换行、HTML 字面量、状态隔离、版本与错误存储边界。
- 存储仅在独立上下文的 `pfc.prototype.v3`。重置检查额外创建的 `CODEx_TEST_OTHER_KEY` 也在测试结束时清除。浏览器在 `finally` 关闭，独立上下文已销毁；无产品或数据库测试记录。
- 最终证据：`C:\Users\hz19114673\AppData\Local\Temp\pfc-prototype-v3-GSXegb\report.json` 与同目录 14 张截图。中间失败证据保留在系统临时目录 `pfc-prototype-v3-*` 下，内容仅含原型与合成输入，可在复核后清理，不纳入提交。
- 本轮所有测试浏览器均已关闭，无新增服务端口或常驻进程。

## 剩余边界

仅验证现有 Edge 的 PC 体验；其他浏览器和移动端未验证。中文组合输入由浏览器事件模拟，未连接真实输入法做人工检查。刷新保存依赖浏览器本地存储，不是数据库持久化，也不提供多窗口并发一致性；演示建议在单个标签页进行。

对话回复、检查结果、开发、发布和指标都是明确标记的演示，不构成真实 AI、测试执行、业务验收或外部效果。平台系统迭代继续暂停。本轮视觉确认仍待用户查看 V3。
