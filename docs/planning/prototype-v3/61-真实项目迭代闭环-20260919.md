# 61 号：真实项目迭代闭环（加载现有代码库 → 迭代 → 测试）

> 交接包：61（M3 验证链 · 真实项目迭代场景）· 2026-09-19 · 提交前文档

## 目标

验证「现有系统迭代」场景：需求形成若是现有系统迭代，应支持加载本地项目（真实代码模块），再由模型在受控 EXEC 工作区内真实改码、补测试并运行通过。

## 实现

- 验证脚本 `server/verify-61-project-iteration.cjs`（PASS）：
  1. 从真实代码库复制 `server/src/local/session-store.js`（125 行）与其依赖 `profile.js` 到授权工作区 `src/*.cjs`（**依赖连带复制**，首次单文件复制导致 `Cannot find module './profile'`，已修正并固化教训）；
  2. 创建需求并推进到 dev（复用 60 流程）；
  3. EXEC 作业指令模型：读取模块副本 → 新增 `publicId()`（`sess_` + randomBytes(8) 16 位 hex）→ 补 ESM 测试（项目根 `package.json` 为 `type:module`）→ `node --test` 真实运行；
  4. 独立验证：`profileExists / hasPublicId / hasCreateVerifier / hasCookieId / testExists / testImportsCjs / greenExit=0` 全真。

## 验收结果

```
ITERATION_VERDICT PASS
```

- 真实模块加载（含依赖）→ 模型真实写回（保留原 API、新增函数、修正依赖引用为 `./profile.cjs`）→ 真实测试输出 `ok 1 - publicId returns distinct sess_ identifiers…` → 独立复算全绿。

## 固化教训

- 加载现有代码库时**依赖要连带复制**（模块依赖图），不能只复制入口文件；
- 测试文件必须用 ESM import（项目根 `type:module`）；
- `node --test` 带目录参数会把目录当模块加载（Node24 行为），命令向量必须精确。

## 涉及文件

- 新增：`server/verify-61-project-iteration.cjs`
- 文档：本文（61）
