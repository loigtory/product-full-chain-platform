# AI-UX-R1 local verification report

> Date: 2026-09-07  
> Parent POD / milestone: `POD-PFC-001 / M2`  
> Scope: AI-UX-R1 A-D  
> Conclusion: `STANDARD_SCHEMA_READY / STAGE_D_VISUALLY_ACCEPTED / ACCEPTANCE_BLOCKED / RELEASE_NOT_AUTHORIZED`

## Delivered scope

- Versioned ProductWorkSession, ProductWorkTurn, ActionProposal, scoped transmission authorization, HTTP/ETag/idempotency/SSE, real local Bridge/Codex protocol, and fail-closed UNKNOWN recovery.
- Reusable AI workspace tokens and components in `packages/ui`, cataloged in `/ui-kit`, then composed by the modular PC route `/requirements/:requirementId/work`.
- Representative page reads server-owned requirement, stage, baseline, owner, contexts, turns, proposals, Skill, Bridge and audit sequence. MCP, Zed and Git are displayed as unavailable when evidence is absent.
- Standard `pfc` migrations 006/007 were applied under the confirmed local authorization package and independently read back; no business or test rows were created.

## Test data and environment

| Item           | Evidence                                                                                             |
| -------------- | ---------------------------------------------------------------------------------------------------- |
| Environment    | Local Windows, project Node 24.20.0, PostgreSQL 18 on loopback                                       |
| Data source    | Deterministic synthetic product facts only                                                           |
| Isolation      | One-time `codex_test_aiux_*` schemas                                                                 |
| AI execution   | Real local Bridge and Codex App Server; no MCP or dynamic tools                                      |
| Sensitive data | None; no customer, employee, credential or restricted material in prompts/screenshots                |
| Cleanup        | Final schema dropped and read back with 0 remaining tables; Web/API/browser/Bridge/App Server closed |

## Executed evidence

| Command                                                                                                                  | Result                                                                          |
| ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- |
| `npx vitest run tests/unit/ai-workspace-ui.test.tsx tests/unit/ai-workspace-api.test.ts tests/unit/ui-kit-page.test.tsx` | PASS, 3 files / 13 tests                                                        |
| `npx vitest run tests/unit/ui-design-system.test.tsx tests/unit/ui-governance.test.ts`                                   | PASS, 2 files / 12 tests                                                        |
| `npm run test:ai-ux:r1:d:browser`                                                                                        | PASS, real completed turn, Edge at 1280/1440/1920, 0 target-page console errors |
| `npm run test:gate:quick`                                                                                                | PASS, 75 files / 332 tests                                                      |
| `npm run test:gate:core`                                                                                                 | PASS, 26 integration files / 114 tests, build and PostgreSQL check              |
| `npm run test:gate:full`                                                                                                 | PASS, 36 PC E2E plus permission/concurrency/recovery/security/performance/audit |
| `npm run db:migrate:ai-ux-preflight`                                                                                     | PASS in a read-only transaction; no DDL/DML                                     |
| `npm run db:migrate:ai-ux` without the exact authorization argument                                                      | Expected BLOCKED before database access                                         |
| `npm run db:migrate:ai-ux -- --authorization=POD-PFC-001_AI-UX-R1_STANDARD_LOCAL_006_007_20260907`                       | PASS, 006/007 applied, 8 tables, 0 business rows                                |
| `npm run db:migrate:ai-ux-readback`                                                                                      | PASS, 2 migration records / 8 tables / 5 indexes                                |
| post-migration `npm run test:gate:core`                                                                                  | PASS, 75 files / 332 tests and 26 integration files / 114 tests                 |

Detailed browser evidence is in `2026-09-07-ai-ux-r1-d-browser.json`. Generated screenshots are:

- `output/playwright/ai-ux-r1-d/ai-workspace-1280.png`
- `output/playwright/ai-ux-r1-d/ai-workspace-1440.png`
- `output/playwright/ai-ux-r1-d/ai-workspace-1920.png`

## Findings and rework

- Fixed an acceptance-script browser serialization defect, separated login-transition errors from target-page errors, and then treated the real missing Skill API as a failure until the PostgreSQL Skill service was wired.
- Replaced misleading `runId 未产生` evidence with the real ProductWorkTurn ID when no separate AgentRun exists.
- Split authenticated route composition out of the oversized `App.tsx` after the architecture gate failed; the rerun passed.
- Normalized PostgreSQL's loopback CIDR readback (`127.0.0.1/32`) without allowing non-loopback targets.
- Failed gate count: one Quick architecture failure, corrected before final Quick/Core/Full PASS. No release issue exists because nothing was released.

## Acceptance and residual risk

- Product-owner Stage D visual acceptance was explicitly confirmed on 2026-09-07 after the three-width representative-page review. This does not replace named engineering acceptance or standard integration evidence.
- Code review, testing and security owners are registered as 陈立; this historical report did not yet contain their acceptance conclusions.
- Standard `pfc` 006/007 is applied and read back, so the schema is ready; AI-UX-R1 is still not standard-local integrated because normal business data, initial context/Skill selection and a complete first turn have not been verified in the standard schema.
- A newly created empty session has no confirmed product flow for selecting initial material targets and a compatible Skill. Existing-session operation is verified; first-turn setup remains a design/implementation gap and cannot be masked with seeded demo state.
- Zed handoff, MCP evidence, release, observation window and CAP-PFC-05 remain deferred.

## Retained process

- PostgreSQL 18 remains available on `127.0.0.1:5432`, PID `7964`, for the next standard-local integration design and later authorized verification. It was started in user mode from the repository runtime; the separate Windows service registration remains `Stopped / Manual`.
- Stop command: `& "$env:LOCALAPPDATA\PFCPlatform\.tools\postgresql-18.6\bin\pg_ctl.exe" stop -D "$env:LOCALAPPDATA\PFCPlatform\.local\postgres-data" -m fast -w` (run with the same elevated context used to start it).
- No listener is retained on 3001 or 5173; no project-owned API, Web, Bridge, Codex App Server, Edge, watcher or test runner remains.

The only next route is `POD-PFC-001/M2/AI-UX-R1/standard-local-integration-design-confirmation`.
